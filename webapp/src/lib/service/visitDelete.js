// ── ลบนัดเข้าบริการได้ไหม — กติกาเดียวของปุ่ม "ลบนัด" และของ `DELETE /api/service/visits/[id]` ──
//
// ⭐ มติเจ้าของ 24/09/2569 (หลังดูหน้าจัดคิว): *"ไม่ปิดปุ่ม นอกรอบหรอ และไม่มี ปุ่มลบรอบนอกรอบด้วย"*
//    ⇒ ปิดปุ่ม "+ งานนอกรอบ" ระหว่างพักเรื่องงานนอกรอบ (มติ 23/09) · และให้ลบงานนอกรอบที่มีอยู่ได้
//    ตรงที่ TS ทำงานกับนัด (การ์ดในรายการงาน · โมดัลแก้นัด)
//
// ⭐ **ลบได้เฉพาะ "งานนอกรอบที่ยังไม่มีใครไปถึงไซต์"** — ทุกชนิดอื่นมีทางที่ถูกกว่า (เปลี่ยนสถานะเป็น
//    "ยกเลิก" ในฟอร์มแก้นัด) เพราะมีของอื่นในระบบผูกกับนัดนั้นอยู่:
//    · **นัดของรอบบริการ** (`planId`) — `ensureVisits` เติมนัดของรอบจากวันที่ *ยังไม่มีนัด* ⇒ ลบทิ้งแล้ว
//      วันนั้นกลับมาเมื่อมีคนบันทึกรอบบริการอีกครั้ง (`?generate=1` — ทางเดียวที่เติมนัด ไม่ได้เติมเอง)
//      · นัดที่ "ยกเลิก" ยังนับว่ามี จึงหยุดได้จริง (rounds.js)
//    · **นัดของใบคำร้องประเมินพื้นที่** (`requestId`) — วันบนใบผูกกับนัดนี้ · ยกเลิกแล้วใบกลับไป
//      "ลงคิวใหม่" บนการ์ดคำร้องเอง (surveyQueueStep)
//    · **นัดถอนเครื่องจากเรื่อง "ไม่ต่อสัญญา"** (ชนิด `remove` ที่ไม่มีรอบ/ใบ — นิยามเดียวกับป้าย
//      "ถอนเครื่อง · ลูกค้าไม่ต่อสัญญา" ของรายการงาน) — ดูเหตุผลที่ `isRenewalRetrieveVisit`
//    · **นัดที่เจ้าหน้าที่ไปถึงไซต์แล้ว** (`canDeleteVisit` + `hasSiteVisitTrace`) — ประวัติการเข้าไซต์
//      ดูจากร่องรอยบนใบด้วย ไม่ใช่สถานะอย่างเดียว (ดูเหตุผลที่ `hasSiteVisitTrace`)
//
// ⚠️ **ตัวเดียวกันทั้งปุ่มและ API** — ปุ่มถาม `visitDeleteButton` (โชว์ไหม + เหตุตอนกด) ·
//    route ถาม `visitDeleteBlock` แล้วตีกลับด้วยข้อความเดียวกัน · ห้ามเขียนเงื่อนไขซ้ำที่จอหรือที่ route
// ⚠️ แอดมินยังข้ามได้ด้วย `?force=1` (มติ #1501) — เส้นนั้นอยู่ที่หน้าไซต์ **ไม่ใช่ปุ่มนี้**
// ⚠️ ไฟล์นี้ถูก import ฝั่งจอ — ห้าม import ของ server (supabase · entityCode)
import { approvalPrompt } from '@/lib/approvalPrompt';
import { canDeleteVisit, isClosedVisit, isLiveVisit } from './visitStatus';
import { REMOVE_VISIT_KIND } from './visitAssets';

/** งานนอกรอบ = นัดที่ไม่ได้เกิดจากรอบบริการและไม่ได้เกิดจากใบคำร้อง (ตัวเดียวกับที่ `originText` ใช้) */
export const isOutOfRoundVisit = (visit) => !!visit && !visit.planId && !visit.requestId;

/* ⭐ **นัดถอนเครื่องที่ระบบสร้างตอนฝ่ายขายปิดเรื่อง "ไม่ต่อ"** (`ensureRetrieveVisit`) —
   ชนิด `remove` ที่ไม่มีรอบ/ใบ · ป้ายของรายการงาน (`originText`) อ่านนิยามจากตัวนี้ตัวเดียว
   🔴 **ลบไม่ได้** เพราะลบแล้วของสองที่ค้างในสภาพที่ผิด และไม่มีอะไรซ่อมให้เอง:
      · เรื่องต่อสัญญาปิดเป็น "ไม่ต่อ" ไปแล้ว (`service_renewal_followups`) และระบบสร้างนัดถอน
        **ครั้งเดียวตอนปิดเรื่อง** — ไม่มีทางสร้างใหม่จากทะเบียนต่อสัญญา (เรื่องปิดแล้ว วันหมดเดิมไม่โผล่ซ้ำ)
      · ทะเบียนเครื่องขยับได้ทางเดียวคือปิดนัดถอน (`visitRetrieval`) ⇒ เครื่องค้างเป็น "ใช้งาน" ที่ไซต์
        ของลูกค้าที่เลิกไปแล้ว — ป้ายน้ำหอมใกล้หมดยังยิง · หาเครื่องว่างไปติดที่อื่นไม่เจอ
      · และไม่เหลือร่องรอยบนจอเลยว่าเรื่องนี้เคยถึง TS (เธรดของนัดถูกกวาดตามการลบ)
   ⇒ ลูกค้าเปลี่ยนใจ = **ยกเลิกนัด** (ยังอยู่ในประวัติ มีชื่อคนกด) ไม่ใช่ลบ
   ⚠️ แยกนัดถอนที่คนสร้างมือจากปุ่มงานนอกรอบ (ก่อน 24/09) ไม่ได้ — ไม่มีคอลัมน์ผูกเรื่องต่อสัญญา ⇒
      กติกาคลุมนัดถอนนอกรอบทุกใบ ซึ่งยกเลิกได้เหมือนกัน */
export const isRenewalRetrieveVisit = (visit) => isOutOfRoundVisit(visit) && visit.kind === REMOVE_VISIT_KIND;

/* ที่อยู่ของ "ฟอร์มแก้นัด" — ข้อความชุดนี้ขึ้นได้ทั้งบนหน้าจัดคิว (มีฟอร์ม) และหน้าไซต์/ยิง API ตรง (ไม่มีฟอร์ม)
   ⚠️ บทเรียนเดียวกับ `planDeleteBlocker`: ข้อความต้องเรียกชื่อตัวคุมที่มีอยู่จริง **และบอกว่าอยู่หน้าไหน** —
      "ในฟอร์มแก้นัด" เฉย ๆ บนหน้าไซต์ = คนหาฟอร์มที่ไม่มีบนหน้านั้นไม่เจอ แล้วกลับมากดลบซ้ำ
      (ชื่อหน้าต้องตรงหัวหน้า `/service/schedule` — ยามอ่านหัวหน้าจริงมาเทียบ) */
export const VISIT_EDIT_FORM_PLACE = 'ในฟอร์มแก้นัดที่หน้าจัดคิวเจ้าหน้าที่';

/* เหตุที่ลบไม่ได้ — ข้อความเดียวที่ทั้งปุ่ม (บอกตอนกด) และ API (409) ใช้ */
export const VISIT_DELETE_BLOCKS = Object.freeze({
  survey: `นัดนี้มาจากใบคำร้องประเมินพื้นที่ — ลบไม่ได้ เพราะวันบนใบผูกกับนัดนี้ · ถ้าไม่ไปแล้ว ให้เปลี่ยนสถานะเป็น “ยกเลิก” ${VISIT_EDIT_FORM_PLACE} แล้วลงคิวใหม่จากการ์ดคำร้องบนหน้าเดียวกัน`,
  /* ⚠️ ไม่สัญญาว่า "สร้างกลับมาเอง" — ไม่มีอะไรเติมนัดของรอบเองตามเวลา · เติมเฉพาะตอนมีคนบันทึกรอบ
     (`?generate=1` → `ensureVisits` · วันที่ยังไม่ผ่านและอยู่ใน 90 วันข้างหน้า) */
  round: `นัดนี้มาจากรอบบริการ — ลบไม่ได้ เพราะวันนั้นยังอยู่ในตารางของรอบ: เมื่อมีคนบันทึกรอบบริการอีกครั้ง ระบบจะเติมนัดวันนั้นกลับมา · ถ้าไม่เข้าครั้งนี้ ให้เปลี่ยนสถานะเป็น “ยกเลิก” ${VISIT_EDIT_FORM_PLACE} (นัดที่ยกเลิกแล้วรอบนับว่ามี จะไม่เติมซ้ำ)`,
  // ⚠️ ข้อความเดิมของ route ก่อนมีไฟล์นี้ — คงคำไว้ (คนเคยเห็นแล้ว)
  visited: 'นัดที่เจ้าหน้าที่ไปถึงไซต์แล้วลบไม่ได้ — เป็นประวัติการเข้าไซต์ · ถ้าบันทึกผิดให้แก้ข้อมูลแทน',
  retrieve: `นัดถอนเครื่องลบไม่ได้ — ระบบสร้างนัดนี้ครั้งเดียวตอนฝ่ายขายปิดเรื่อง “ลูกค้าไม่ต่อสัญญา” และจะไม่สร้างให้ใหม่ · ลบทิ้ง = เครื่องค้างเป็น “ใช้งาน” ที่ไซต์โดยไม่มีใครไปถอน · ถ้าไม่ต้องถอนแล้ว ให้เปลี่ยนสถานะเป็น “ยกเลิก” ${VISIT_EDIT_FORM_PLACE} (ยังเหลือในประวัติให้ตามเรื่องได้)`,
});

/* คำสั้นของด่านที่แอดมินข้าม — ต่อท้ายบรรทัด audit ("แอดมินข้ามด่าน…") ให้อ่านออกว่าข้ามเรื่องอะไร */
export const VISIT_DELETE_BLOCK_LABELS = Object.freeze({
  survey: 'นัดของใบคำร้อง',
  round: 'นัดของรอบบริการ',
  visited: 'ประวัติการเข้าไซต์',
  retrieve: 'นัดถอนเครื่องจากเรื่องไม่ต่อสัญญา',
});

/* เจ้าหน้าที่หน้างาน (Operation · `ownWorkOnly`) ลบนัดไม่ได้แม้เป็นงานของตัวเอง — การเอางานออกจากตาราง
   เป็นเรื่องของ "แผน" แบบเดียวกับวัน/เวลา/คนไป (`PLANNING_FIELD_ERROR`) ⇒ ของผู้จัดคิว (`canEditService`) */
export const VISIT_DELETE_CREW_ERROR = 'เจ้าหน้าที่หน้างานลบนัดไม่ได้ — การเอางานออกจากตารางต้องให้ผู้จัดคิวเป็นคนทำ';

/* ⭐ **ร่องรอยว่าเคยมีคนไปถึงไซต์ — อ่านจากช่องบนใบ ไม่ใช่จากสถานะอย่างเดียว**
   🐞 (รีวิว 24/09) ด่านเดิมดูแค่สถานะปัจจุบัน (`canDeleteVisit`) ⇒ ใบที่ปิดเป็น "ทำไม่ได้" (ไปถึงแล้วเข้าไม่ได้)
      แล้วมีคนเปลี่ยนสถานะกลับเป็น ยกเลิก/เลื่อนแล้ว/นัดไว้/ร่าง ในฟอร์มแก้นัด (`unable` อยู่ใน
      `VISIT_STATUSES_MANUAL` · PATCH ไม่กันการถอยจากสถานะปิด) = ปุ่มลบกดได้ + API ยอม ⇒ เธรด "ทำไม่ได้"
      ถูกกวาด และผลรายเครื่อง/ของที่ใช้หายตาม CASCADE — ทางอ้อมแบบเดียวกับที่ visitRetrieval.js เขียนไว้
      ("เปลี่ยนเป็น ยกเลิก ได้ แล้วนัดที่ยกเลิกลบทิ้งได้") ซึ่งเคยปิดให้เฉพาะนัดถอน
   ⭐ ร่องรอย = ช่อง "ผลการเข้าจริง" (`actualDate` · `actualStartTime` · `actualEndTime`) — ปุ่มเริ่มงาน/ปิดงาน
      ประทับ หรือคนกรอกเอง · **ไม่ถูกล้างตอนเปลี่ยนสถานะ** (PATCH รวม `{...before, ...body}` · ฟอร์มส่งค่าเดิมกลับ)
      และสถานะปิดทั้งสาม (รวม "ทำไม่ได้") DB บังคับให้มี `actualDate` (mig 0300) ⇒ ใบที่เคยปิดแล้วถูกเปิดกลับ
      ยังมีร่องรอยเสมอ
   ⚠️ **จงใจไม่นับ `unableReason`** ทั้งที่ดูเหมือนหลักฐาน — ช่องนี้บนฟอร์มโผล่เฉพาะตอนสถานะเป็น "ทำไม่ได้"
      ⇒ ถ้านับ คนที่ล้างวันเข้าจริงเพราะบันทึกผิด (ไม่เคยไปจริง) จะติดเหตุ "ไปถึงไซต์แล้ว" จากช่องที่มองไม่เห็น
      และแก้เองไม่ได้ = ทางตัน · ใบ "ทำไม่ได้" มี `actualDate` คู่กันอยู่แล้ว จึงไม่เสียอะไร
   ⚠️ บันทึกผิดจริง (ไม่เคยไป) = ล้างช่องผลการเข้าจริงในฟอร์มแก้นัดก่อน แล้วค่อยลบ — ตรงกับข้อความ `visited`
   ⚠️ ร่องรอยที่อยู่ **นอกแถวนัด** (ผลรายเครื่อง `service_visit_assets` · ของที่ใช้ `service_visit_items`)
      จอไม่รู้ ⇒ route นับแล้วส่งเข้า `visitDeleteBlock(visit, { fieldRecords })` · จอเห็นเหตุเดียวกันจาก 409 */
export const hasSiteVisitTrace = (visit) => !!(
  visit?.actualDate || visit?.actualStartTime || visit?.actualEndTime
);

/**
 * ด่านของการลบ — `null` = ลบได้ · `{ code, message }` = ลบไม่ได้ (code ∈ VISIT_DELETE_BLOCKS)
 * ลำดับ: ต้นเรื่อง (ใบ → รอบ) ก่อน แล้วค่อยประวัติ แล้วค่อยนัดถอน — เหตุที่ขึ้นคือเหตุที่แก้ยากที่สุดก่อน
 * @param fieldRecords  จำนวนแถวลูกที่เป็นผลของการไปจริง (ผลรายเครื่อง + ของที่ใช้) — **route เป็นคนนับ**
 *                      (จอไม่รู้ จึงไม่ส่ง = 0 · API ยังตีกลับด้วยเหตุเดียวกันถ้ามี)
 */
export function visitDeleteBlock(visit, { fieldRecords = 0 } = {}) {
  if (!visit) return { code: 'visited', message: VISIT_DELETE_BLOCKS.visited };
  let code = null;
  if (visit.requestId) code = 'survey';
  else if (visit.planId) code = 'round';
  else if (!canDeleteVisit(visit) || hasSiteVisitTrace(visit) || Number(fieldRecords) > 0) code = 'visited';
  else if (isRenewalRetrieveVisit(visit)) code = 'retrieve';
  return code ? { code, message: VISIT_DELETE_BLOCKS[code] } : null;
}

/** เหตุที่ลบไม่ได้เป็นสตริง ('' = ลบได้) — ทรงเดียวกับ `blocker` ของ GatedAction */
export const visitDeleteBlocker = (visit) => visitDeleteBlock(visit)?.message || '';

/**
 * ปุ่ม "ลบนัด" บนการ์ดรายการงาน/โมดัลแก้นัด — `null` = ไม่มีปุ่ม · `{ blocker }` = โชว์ (blocker ว่าง = กดได้)
 *
 * ⭐ โชว์เฉพาะ **งานนอกรอบที่ยังไม่ปิด** — ปุ่มนี้มีไว้สำหรับงานนอกรอบ (มติ 24/09)
 *   · นัดของรอบ/ใบคำร้อง = ไม่ใช่เรื่องของปุ่มนี้ (ไม่โชว์ · API ยังตอบเหตุถ้ามีคนยิงตรง)
 *   · ปิดงานแล้ว = ประวัติ ไม่มีอะไรให้ลบ (ไม่โชว์ — ปุ่มลบบนการ์ดที่ปิดแล้วชวนให้ลบประวัติ)
 *   · กำลังทำ / มีร่องรอยการเข้าไซต์ (ใบที่ปิดแล้วถูกเปิดกลับ) / นัดถอนจากเรื่องไม่ต่อสัญญา
 *     = **โชว์แล้วบอกเหตุตอนกด** (กติกา GatedAction)
 * ⚠️ สิทธิ์ (`canEditService`) ผู้เรียกเช็คเอง — ตัวนี้ตอบแค่เงื่อนไขของใบ
 * ⚠️ **หน้าไซต์ใช้กติกาโชว์ตัวนี้ด้วย** (ไม่ใช่แอดมิน = ไม่มีถังขยะบนนัดของรอบ/ใบคำร้อง) — สองจอต้องพูดเหมือนกัน
 */
export function visitDeleteButton(visit) {
  if (!isOutOfRoundVisit(visit) || isClosedVisit(visit)) return null;
  return { blocker: visitDeleteBlocker(visit) };
}

/**
 * กล่องยืนยันก่อนลบ (ใช้กับ `confirmAction`) — บอกว่าใบไหน (รหัส · ไซต์ · วัน) · หายจากตรงไหน · ย้อนไม่ได้
 * @param when  วัน(+เวลา) ที่คนเห็นบนการ์ด เช่น "พ. 24 ก.ย. 09:00–12:00" (ผู้เรียกประกอบ — ไฟล์นี้ไม่จัดรูปวัน)
 * @param day   วันของนัดแบบที่คนเห็นบนการ์ด เช่น "พ. 24 ก.ย." — ใช้ในบรรทัด "หายจากงานของใคร วันไหน"
 */
export function visitDeletePrompt(visit = {}, { siteName = '', when = '', day = '' } = {}) {
  const code = visit.code || visit.id || '';
  const subject = [code, siteName || visit.siteId, when || visit.scheduledDate].filter(Boolean).join(' · ');
  /* ⭐ **ขึ้นตารางแล้ว = อยู่ในงานของคนที่ไปทุกคน** ไม่ใช่แค่ผู้รับผิดชอบ — คนที่ติ๊กเป็น "ผู้ไปด้วย"
        (`assistantIds`) ก็เห็นนัดในงานวันนี้ของตัวเอง (`loadVisits` กรอง assigneeId **หรือ** assistantIds)
     🐞 (รีวิว 24/09) เดิมบอกแค่ "งานวันนี้ของ {ผู้รับผิดชอบ}" ⇒ ผู้ไปด้วยหายไปจากประโยค และ "วันนี้"
        อ่านผิดสำหรับนัดวันพุธของสัปดาห์หน้า ⇒ บอกวันของนัดแทน
     ⚠️ ร่างไม่อยู่ในงานของใคร (ร่างไม่โผล่ในงานวันนี้) ⇒ ไม่มีบรรทัดนี้ */
  const helpers = Array.isArray(visit.assistantIds) ? visit.assistantIds.filter(Boolean).length : 0;
  const crew = [
    visit.assigneeName || '',
    helpers ? `${visit.assigneeName ? 'ผู้ไปด้วยอีก' : 'ผู้ไปด้วย'} ${helpers} คน` : '',
  ].filter(Boolean).join(' กับ');
  const onBoard = isLiveVisit(visit) && crew;
  const dayLabel = day || visit.scheduledDate || '';
  return {
    ...approvalPrompt({
      title: 'ลบนัด',
      verb: 'ลบนัด',
      subject,
      irreversible: true,
      checklist: ['ลบแล้วไม่เหลือร่องรอยบนจอ — ถ้าแค่ไม่ไปแล้วแต่อยากเก็บไว้ว่าเคยนัด ให้เปลี่ยนสถานะเป็น “ยกเลิก” ในฟอร์มแก้นัดแทน'],
      effects: [
        onBoard
          ? `นัดหายจากตารางนัดเข้าบริการ รายการงาน และงาน${dayLabel ? `วัน ${dayLabel} ` : ''}ของ ${crew}`
          : 'นัดหายจากตารางนัดเข้าบริการและรายการงาน',
        'ความเคลื่อนไหวของนัดนี้ถูกลบไปด้วย — ไม่มีถังขยะ กู้คืนเองไม่ได้',
        'ไม่มีการแจ้งเตือนใคร — ถ้าบอกเจ้าหน้าที่หรือลูกค้าไปแล้ว ต้องแจ้งเอง',
      ],
      confirmLabel: 'ลบนัด',
    }),
    // ⚠️ โมดัลของการลบต้องหน้าตาเป็นการลบ (ไอคอนถังขยะ + ปุ่มแดง) — ท่าเดียวกับ productSpecDocView
    tone: 'danger',
  };
}
