// ขนาดกระดาษของเอกสารทั้งระบบ — **A4 เท่านั้น** (มติผู้ใช้ 2026-08-21)
//
// แยกออกมาจาก documentShell เพื่อให้เทสต์อ่านได้โดยไม่ต้องโหลดเปลือกทั้งก้อน
// (เปลือกดึงฟอนต์ base64 มาด้วย) และเพื่อให้มี "ที่เดียว" ที่ตอบว่ากระดาษคืออะไร
//
// ⚠️ เพิ่มขนาดใหม่ที่นี่ = เปลี่ยนกระดาษของทั้งระบบ — เอกสารทุกชนิดกินตารางนี้
export const PAPER_SIZES = Object.freeze({
  portrait: { width: '210mm', height: '297mm', page: 'A4 portrait', scale: 1 },
  landscape: { width: '297mm', height: '210mm', page: 'A4 landscape', scale: 297 / 210 },
});
