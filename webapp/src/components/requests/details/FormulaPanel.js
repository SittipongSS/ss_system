"use client";
// ── การ์ด panel รายหัวข้อ · พัฒนาสูตร (ม-94) ─────────────────────────────
//
// ⭐ การ์ดสรุปตัวเลขของใบ — **ย้าย**มาจากแถบตัวเลขท้ายตาราง (FormulaDevDetail)
// ไม่ก๊อป: สองขั้นที่ค้างโดยไม่มีใครเห็นได้ง่ายที่สุด ("รอลูกค้าตอบ" กับ
// "รอใส่ราคา") ต้องขึ้นเป็นตัวเลข ไม่ใช่ให้คนไล่นับจากตารางเอง
//
// ⚠️ ตัวเลขนับที่ `lib/requests/panelSummary.js` ก้อนเดียวกับทุกหัวข้อ · ปุ่มรายแถว
// อยู่ในตารางสรุปทั้งใบ ปุ่มระดับใบอยู่การ์ด control กลาง — การ์ดนี้ไม่มีปุ่ม
import RequestSummaryPanel from "./RequestSummaryPanel";

/* ⭐ **ทรงเดียวกับทุกหัวข้อแล้ว** (มติผู้ใช้ 2026-08-25) — ตัวเลขนำ "ได้สูตรแล้ว N/M"
   + แกนสามแถว · หัวข้อนี้ไม่มีของเฉพาะตัวลงก้อนล่าง (ราคาอยู่ในแถวของตารางแล้ว)
   ⚠️ หกตัวเลขเดิมถูกถอด **ไม่ใช่ย่อ** — "ยังไม่ได้ส่ง / รอลูกค้าตอบ / รอใส่ราคา"
   คือการนับ `stage` สามค่าแยกกัน ซึ่งแกนสามแถวตอบด้วยคำถามที่ตรงกว่า ("ค้างอยู่ที่
   ใคร") · "ลูกค้าขอให้แก้" กลายเป็นแถวใหม่ในตารางอยู่แล้ว จึงถูกนับสองรอบมาตลอด */
export default function FormulaPanel({ request }) {
  return <RequestSummaryPanel request={request} lineShape="product_dev" />;
}
