-- 0167 - ตรึงที่อยู่ลูกค้าลงใบยื่นชำระภาษี (ใบแจ้งชำระค่าภาษีสรรพสามิต)
--
-- บั๊กที่ปิด: หน้าใบยื่นหา "ลูกค้าของเอกสาร" ด้วยการ find จากลิสต์ /api/customers
-- ซึ่งกรองด้วยทีมที่ดูแล + เฉพาะที่อนุมัติ + ซ่อนลูกค้าพัก → คนที่มองลูกค้ารายนั้น
-- ไม่เห็น (ฝ่ายขายทีมอื่น) กดพิมพ์แล้ว **ที่อยู่บนเอกสารที่ส่งลูกค้ากลายเป็น "-"**
-- ส่วนฝ่ายกฎหมาย (legal:view = scope all) เห็นครบ → เอกสารใบเดียวกันพิมพ์ออกมา
-- ไม่เหมือนกันขึ้นกับว่าใครกด
--
-- กติกาเดิมของระบบ (บทเรียน PR #710): อะไรที่เติมค่าตอน read-time ต้องตรึงลงฉบับจริง
-- ด้วย · เลขภาษีตรึงอยู่แล้วที่ orders."customerTaxId" ตั้งแต่แรก ขาดแค่ที่อยู่
--
-- ไม่ backfill: prod ยังไม่มีใบยื่นแม้ใบเดียว (ยืนยัน 2026-07-26) และใบเก่าที่ไม่มีค่า
-- ยังอ่านจากทะเบียนลูกค้าสดเป็นค่าสำรองได้เหมือนเดิม
-- Additive + idempotent — รันซ้ำได้

ALTER TABLE public.orders
  ADD COLUMN IF NOT EXISTS "customerAddress" text;

COMMENT ON COLUMN public.orders."customerAddress"
  IS 'ที่อยู่ลูกค้า ณ วันสร้างใบยื่น — ตรึงไว้เพื่อให้เอกสารพิมพ์เหมือนกันทุกคนที่กด';

NOTIFY pgrst, 'reload schema';
