-- ============================================================
--  Migration 0313: service_sites."customerAddressId" — ที่อยู่ต้นทางของไซต์
--
--  ⭐ ที่มา (มติ 2026-08-28 · ทำจริงรอบนี้): ตอนสร้างไซต์ ผู้ใช้เลือกที่อยู่จาก
--     ทะเบียนลูกค้าแล้วระบบ **ก๊อป 4 ช่อง** (address · mapUrl · contactName ·
--     contactPhone) ลงแถวไซต์ — แต่ไม่เคยเก็บว่า "ก๊อปมาจากแถวไหน" ⇒ เปิดฟอร์ม
--     ครั้งถัดไปไม่มีอะไรบอกที่มา และเทียบกับทะเบียนไม่ได้ว่ายังตรงกันอยู่ไหม
--
--  ⚠️ **ไม่มี FK โดยเจตนา** — ที่อยู่ไม่ใช่ตาราง แต่เป็นแถวใน jsonb
--     `customers.addresses[]` (mig 0202/0217) · ค่าที่เก็บคือ `addresses[].id`
--
--  ⚠️ **ไม่ sync ย้อนกลับ และไม่ sync ไปข้างหน้าอัตโนมัติ** — ที่อยู่ทางภาษีกับ
--     ที่อยู่หน้างานเป็นคนละความจริง (ห้างแก้ที่อยู่จดทะเบียน ไม่ได้แปลว่าเครื่องย้าย)
--     คอลัมน์นี้บอก **ที่มา** อย่างเดียว เพื่อให้ฟอร์มเทียบค่าแล้วเสนอปุ่ม "ดึงใหม่"
--     ให้คนกดเอง · ค่าเป็น NULL ได้เสมอ = ไซต์ที่พิมพ์ที่อยู่เอง (ล็อบบี้ห้าง โรงแรม
--     พื้นที่เช่า) หรือไซต์ยุคก่อนคอลัมน์นี้
--
--  ⚠️ ย้ายไซต์ข้ามลูกค้า = ค่านี้ต้องถูกล้าง (ชี้เข้า addresses ของลูกค้าคนเดิม)
--     ด่านอยู่ที่ API — PATCH /api/service/sites/[id]
--
--  additive ล้วน รันก่อน deploy ได้
-- ============================================================

ALTER TABLE public.service_sites
  ADD COLUMN IF NOT EXISTS "customerAddressId" text;

COMMENT ON COLUMN public.service_sites."customerAddressId" IS
  'แถวที่อยู่ใน customers.addresses[] ที่ใช้ตั้งต้นไซต์นี้ (ไม่มี FK — ที่อยู่เป็น jsonb) · บอกที่มาอย่างเดียว ไม่ sync สองทาง · ย้ายลูกค้าต้องล้างค่า';

NOTIFY pgrst, 'reload schema';
