-- ============================================================
--  Migration 0236: ถอนตาราง chat_webhooks — ปิดฉากท่อ Google Chat
--
--  ที่มา (มติผู้ใช้ 2026-08-12): "เอา webhook ออก ใช้กระดิ่งกับป้ายตัวเลขก็พอ"
--  ⇒ ถอดโค้ดฝั่งแอปออกทั้งชุดในคอมมิตเดียวกับ migration นี้: `lib/chat.js`,
--    `/api/chat-webhooks`, หน้า `/settings/chat-webhooks` และจุดยิงทั้ง 33 จุด
--
--  ── สภาพก่อนถอน ────────────────────────────────────────────────────────
--  ทุกห้องถูกปิดไปแล้วตั้งแต่ 2026-07-30 (8 แถว `enabled = false` ทั้งหมด) และ
--  ตารางนี้มาก่อน env เสมอ (`lib/chat.js` เดิม: มี row = ยึด row · ไม่มี = fallback
--  `CHAT_WEBHOOK_*`) ⇒ **ไม่มีการ์ดใบไหนถูกส่งจริงมาสองสัปดาห์แล้ว** การถอนจึงไม่
--  ตัดสัญญาณที่ยังทำงานอยู่แม้แต่เส้นเดียว
--
--  ⚠️ **URL ของ webhook หายถาวรพร้อมตารางนี้** (3 ห้องที่ยังเก็บ url ไว้:
--    approvals · sales · pm) — ตั้งใจตามมติ · จะกลับมาใช้ Chat ต้องสร้าง incoming
--    webhook ใหม่ใน Google Chat แล้ววางระบบใหม่ทั้งท่อ ไม่ใช่แค่คืนตาราง
--
--  ⚠️ ตัวแปร `CHAT_WEBHOOK_*` บน Vercel **ไม่ได้ถูกลบโดย migration นี้** (คนละที่)
--    ตอนนี้ไม่มีโค้ดอ่านแล้ว ปล่อยไว้ไม่มีผล · จะลบต้องทำที่แดชบอร์ด Vercel เอง
--
--  ⚠️ ของที่เงียบลงเพราะการถอนครั้งนี้ (มติ: ยอมให้เงียบ ไม่ต่อเข้ากระดิ่ง) —
--    บันทึกไว้ใน docs/notifications-inbox.md §7 ให้ตามอ่านได้ว่าอะไรหายไปบ้าง
--
--  additive-safe: `IF EXISTS` ⇒ รันซ้ำได้ · ไม่มี FK ชี้เข้ามา
-- ============================================================
BEGIN;

DROP TABLE IF EXISTS public.chat_webhooks;

COMMIT;

NOTIFY pgrst, 'reload schema';

-- ── ตรวจหลังรัน ─────────────────────────────────────────────────────────
--   SELECT to_regclass('public.chat_webhooks');   -- ต้องเป็น NULL
--
-- Rollback: ไม่มี — ตารางเก็บแต่ค่าตั้งค่าที่ปิดใช้งานแล้ว สร้างใหม่ได้จาก mig 0099
--           แต่ URL ต้องออกใหม่จาก Google Chat อยู่ดี
