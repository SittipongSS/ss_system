-- ============================================================
--  Migration 0281: แถวมูลค่าคาดการณ์ของดีล — กลุ่ม 03/04 ไม่มีปริมาตร/หน่วยบรรจุ
--  (มติผู้ใช้ 2026-08-21 — เอากติกาเดียวกับทะเบียนสินค้า mig 0277 มาใช้กับฝั่งดีล)
--
--  ที่มา: ผู้ใช้ทักว่า "หน่วยและเงื่อนไขหน่วยของดีล ยังไม่เหมือนกับฐานข้อมูล" — จริงตามนั้น
--  `sales_deal_value_items` ถือ `categoryCode` รายแถวอยู่แล้ว แต่ไม่เคยเอาไปตัดสินอะไรเลย
--  ⇒ แถวของกลุ่ม 03 (ค่าออกแบบ) · 04 (รายได้อื่นๆ) ยังมีปริมาตรได้ ทั้งที่สินค้ากลุ่มเดียวกัน
--  ล้างไปแล้วตั้งแต่ 0277
--
--  📊 ของจริงที่จะโดน (นับ 2026-08-21): 23 แถว — กลุ่ม 03 จำนวน 20 · กลุ่ม 04 จำนวน 3
--     กลุ่ม 03 ทั้ง 20 แถวเป็น 'package' ล้วน = ค่าหลอกแบบเดียวกับที่เจอฝั่งสินค้าเป๊ะ
--     (กรอกไปเพื่อให้ผ่าน constraint "กรอกขนาดแล้วต้องมีหน่วย" ของ 0265)
--
--  ⚠️ ไม่แตะ `unit` (หน่วยขาย) — ทุกแถวอยู่ในลิสต์กลางอยู่แล้ว ตรวจแล้ว 0 แถวนอกลิสต์
--  ⚠️ ไม่แตะยอดเงิน — ปริมาตรไม่เคยเข้าสูตรคิดเงินของดีล (amount = qty × unitPrice)
--     `sales_deals."projectValue"` จึงไม่ขยับแม้แต่บาทเดียว
--
--  🛑 ต้องรันก่อน deploy โค้ด · idempotent · รันซ้ำได้
--  ⚠️ รันมือบน Supabase SQL Editor
-- ============================================================

BEGIN;

UPDATE public.sales_deal_value_items
   SET volume = NULL, "volumeUnit" = NULL
 WHERE left("categoryCode", 2) IN ('03', '04')
   AND (volume IS NOT NULL OR "volumeUnit" IS NOT NULL);

COMMIT;

NOTIFY pgrst, 'reload schema';
