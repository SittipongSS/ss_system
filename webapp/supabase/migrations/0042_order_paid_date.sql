-- ============================================================
--  Migration 0042: orders.taxPaidDate — วันที่ชำระภาษีจริง
--  เดิมมีแต่ filedAt (เวลาบันทึกยื่นในระบบ) / taxDueDate (กำหนดชำระ) แต่ไม่มี
--  "วันที่ชำระจริง" ที่กรมสรรพสามิต → รายงานรอบยื่น/ค้างชำระอ้างวันจริงไม่ได้.
--  เพิ่มคอลัมน์เดียว nullable. additive + idempotent. ⚠ รันมือบน Supabase.
-- ============================================================

alter table public.orders
  add column if not exists "taxPaidDate" date;
