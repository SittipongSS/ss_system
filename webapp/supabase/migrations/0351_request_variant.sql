-- ============================================================
--  Migration 0351: รูปแบบงานของคำร้อง — พัฒนาสูตร standard | NPD (มติผู้ใช้ 2026-09-09)
--
--  คำสั่งตั้งต้น: *"การพัฒนาสูตร อยากให้มีสวิตช์เปิดปิด standard กับ NPD
--  ถ้า NPD ต้องเป็นฟอร์ม PDR"*
--
--  ⭐ **หนึ่งหัวข้อ สองรูปแบบ — ไม่ใช่หัวข้อที่สอง**
--  `kind` เป็น **ตัวตน** ของใบ: มันฝังอยู่ใน `docNo` (RQ-FD-YYMMxxxx) ซึ่ง
--  `guard_dept_request` (0173) ห้ามแก้ตลอดกาล และผูก `stepKey` เข้ากับแม่แบบ
--  ไทม์ไลน์ของดีล ⇒ ถ้าแยกเป็น `formula_dev_npd` การ "สลับสวิตช์" จะแปลว่า
--  **ลบใบเปิดใหม่** ซึ่งไม่ใช่สิ่งที่ผู้ใช้ขอ · คอลัมน์นี้จึงเก็บ *รูปทรงของงาน*
--  แยกจาก *ตัวตนของใบ*
--    standard = ขอตัวอย่างจากกลิ่นที่มีอยู่ · มีตาราง หมวด × กลิ่น (ของเดิมทั้งหมด)
--    npd      = พัฒนาสินค้าใหม่ · ใช้แบบฟอร์ม PDR (FM-RD-01) · ไม่มีตารางรายการ
--
--  ⚠️ **ไม่มี CHECK โดยเจตนา** — แพตเทิร์นเดียวกับคอลัมน์ `kind` ของตารางนี้เอง
--  (0173: *"ชุดชนิดเป็นเรื่องของงาน ไม่ใช่ของ schema"*) · ชุดรูปแบบประกาศอยู่ใน
--  ทะเบียนหัวข้อฝั่งโค้ด (`lib/requests/kinds/rd/formulaDev.js`) และตรวจด้วย
--  `requestVariantError` ทั้งฝั่งจอและฝั่ง API ⇒ วันที่ฝ่ายอื่นอยากได้รูปแบบของตัวเอง
--  คือแก้ไฟล์เดียว ไม่ใช่ออก migration แก้ CHECK บน prod
--
--  ⚠️ **DEFAULT 'standard' ไม่ใช่ NULL** — ใบเก่าทุกใบคือ standard ตามนิยาม
--  (ก่อนวันนี้พัฒนาสูตรมีรูปทรงเดียว) · ปล่อยเป็น NULL แล้วโค้ดต้องเดาแทนทุกจุด
--  ซึ่งเป็นที่มาของบั๊ก "ตอบด้วยรูปแบบตั้งต้นเงียบ ๆ" ที่ทั้งงานนี้พยายามกัน
--  ⚠️ คอลัมน์นี้อยู่กับ **ทุกหัวข้อ** ไม่ใช่เฉพาะพัฒนาสูตร — หัวข้อที่ไม่มีรูปแบบ
--  ให้เลือกจะไม่มีใครอ่านค่านี้เลย (ตัวอ่านถามทะเบียนก่อนเสมอ)
--
--  additive ล้วน · ไม่แตะข้อมูลเดิม · รันซ้ำได้
--  🛑 **ต้องรันก่อน deploy โค้ด** — POST /api/sa/requests ใส่คีย์ `variant` ลง insert
--     และ PostgREST ปฏิเสธ **ทั้งก้อน** เมื่อ body มีคอลัมน์ที่ตารางยังไม่มี
--     (บทเรียนเดียวกับ mig 0204 `productTypeId` และ 0225 `quotationId` ที่เคยทำให้
--     เปิดคำร้องไม่ได้เลยทุกหัวข้อ)
--  ⚠️ รันมือบน Supabase SQL Editor (DDL รันผ่าน PostgREST ไม่ได้)
-- ============================================================

BEGIN;

ALTER TABLE public.dept_requests
  ADD COLUMN IF NOT EXISTS "variant" text NOT NULL DEFAULT 'standard';

COMMENT ON COLUMN public.dept_requests."variant" IS
  'รูปแบบงานของใบ — ชุดค่าประกาศในทะเบียนหัวข้อฝั่งโค้ด (พัฒนาสูตร: standard | npd) · ไม่มี CHECK โดยเจตนา';

COMMIT;
