-- ============================================================
--  Migration 0345: เหตุผลตอนเคาะแพ็คเกจต่างจากสูตร (ใบประเมินพื้นที่ เฟส 3)
--  แผน docs/site-survey-request-plan.md · ม็อก docs/site-survey-mockup.html (จอ 07)
--
--  ⭐ ที่มา: หัวหน้า TS เคาะจำนวนแพ็คเกจเองได้ — สูตร (ปริมาตร ÷ 2,400 ปัดขึ้น) เป็น
--    ข้อเสนอ ไม่ใช่คำสั่ง · **สูตรมีขั้นบันได** 2,400 ลบ.ม. ได้ 1 แพ็คเกจ แต่ 2,401
--    กระโดดเป็น 2 ทันที ⇒ ต้องมีช่องให้ทับ ไม่ใช่คำนวณแล้วล็อก (แผน §5.3)
--
--  🔴 **แต่ทับแล้วต้องบอกเหตุผล** — กติกาเดียวกับการตัดพื้นที่ออก (แผน §มติ 6):
--    *ของที่ต่างไปจากสิ่งที่ SA จะเสนอราคา คือของที่ลูกค้าจะถาม และ SA ไม่ได้ไปหน้างาน*
--    ของจริงในม็อก: โถงจอดรถ 7,200 ลบ.ม. สูตรได้ 3 แพ็คเกจ แต่หัวหน้าใส่ 1 เพราะ
--    "กึ่งกลางแจ้ง เปิดสามด้าน ลมโกรกตลอด — ใส่เท่าที่สูตรบอกก็ไม่อยู่"
--    ⇒ เหตุผลแบบนี้ไม่มีทางเดาจากตัวเลข และเป็นสิ่งเดียวที่อธิบายใบให้ SA ได้
--
--  🪤 **ทำไมไม่ใช้ `note` ที่มีอยู่แล้ว** — `note` คือ **บันทึกหน้างานของช่าง**
--    (เพดานฝ้า · ตำแหน่งปลั๊ก · ข้อจำกัดของอาคาร) ที่เขียนตอนอยู่หน้างาน
--    ให้หัวหน้าเขียนทับ = ลบข้อสังเกตของคนที่ไปเห็นของจริง ทิ้งเงียบ ๆ
--    ⇒ คนละคนเขียน คนละจังหวะ คนละคำถาม ⇒ คนละช่อง
--
--  ⚠ รันมือบน Supabase SQL Editor · **ต้องรันก่อน deploy**
--    (จอส่งผลอ่าน/เขียนคอลัมน์นี้ — ไม่มีคอลัมน์ = บันทึกแพ็คเกจไม่ผ่านทั้งจอ)
--  ⚠ รันซ้ำได้
-- ============================================================

BEGIN;

ALTER TABLE public.service_survey_zones
  ADD COLUMN IF NOT EXISTS "packageNote" text;

ALTER TABLE public.service_survey_zones DROP CONSTRAINT IF EXISTS service_survey_zones_package_note_len;
ALTER TABLE public.service_survey_zones
  ADD CONSTRAINT service_survey_zones_package_note_len
  CHECK ("packageNote" IS NULL OR length("packageNote") <= 500);

COMMENT ON COLUMN public.service_survey_zones."packageNote" IS
  'เหตุผลตอนเคาะแพ็คเกจต่างจากสูตร (mig 0345) — หัวหน้า TS เขียนที่จอส่งผล '
  'คนละช่องกับ note ซึ่งเป็นบันทึกหน้างานของช่าง';

/* ⚠️ **ด่าน "ต่างจากสูตรต้องมีเหตุผล" อยู่ในโค้ด ไม่ใช่ CHECK** โดยตั้งใจ —
   CHECK ระดับแถวคำนวณสูตรเองไม่ได้ (ต้องบวกปริมาตรจาก `parts` jsonb แล้วปัดขึ้น)
   เขียนเป็น CHECK ได้ก็จริงแต่จะกลายเป็นสูตรชุดที่สองที่ต้องคอยซิงก์กับ
   `suggestedPackages()` ใน survey.js ⇒ ที่เดียวคือ `lib/service/survey.js` */

COMMIT;

NOTIFY pgrst, 'reload schema';
