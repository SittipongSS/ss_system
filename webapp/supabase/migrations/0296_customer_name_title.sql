-- ============================================================
--  Migration 0296: แยก "คำนำหน้าชื่อ" ออกจากชื่อลูกค้าบุคคลธรรมดา
--
--  ⭐ ที่มา (มติผู้ใช้ 2026-08-27): ตอนเทียบทะเบียนกับชีทระบบบัญชีเก่า พบว่าชื่อ
--  ลูกค้าบุคคลเขียนกันคนละแบบทั้งทะเบียน — 'คุณ' 42 ราย · 'นาย' 7 · 'น.ส.' 5 ·
--  'นางสาว' 4 · 'ดร.' 1 และอีก ~30 รายไม่มีคำนำหน้าเลย · เรียงชื่อก็เพี้ยนเพราะ
--  คำนำหน้าติดมาข้างหน้า และใบกำกับภาษีเต็มรูปต้องใช้คำนำหน้าตามกฎหมาย
--  ('คุณ' ใช้ไม่ได้)
--
--  ── รูปเก็บ: แพตเทิร์นกระจกเดียวกับ addresses[] → address (mig 0202) ──
--    nameTitle  = คำนำหน้า ('นาย' | 'นาง' | 'นางสาว' | คำอื่นที่พิมพ์เอง)
--    namePerson = ชื่อ-นามสกุล เปล่า ๆ ไม่มีคำนำหน้า
--    name       = **กระจก** ที่ระบบประกอบให้ตอนบันทึก ('นาย' + ' ' + ชื่อ)
--
--  ⭐ ทำไม `name` ยังอยู่และยังเป็นข้อความเต็ม: มีสายที่อ่านช่องนี้อยู่จริงสิบกว่าจุด
--  ทั้งตาราง/ค้นหา/snapshot เอกสาร และ **5 ตารางที่ก๊อปชื่อไว้**
--  (lib/master/customerNameMirrors) — เปลี่ยนให้ทุกจุดประกอบเองคือรีแฟกเตอร์ใหญ่
--  ที่พลาดง่าย และเอกสารที่ออกไปแล้ว (โหมด frozen) จะกลายเป็นชื่อไม่มีคำนำหน้าทันที
--
--  ⚠️ นิติบุคคลไม่มีสองช่องนี้ — `name` ของบริษัทคือชื่อที่พิมพ์ตรง ๆ เหมือนเดิม
--  ⚠️ additive ล้วน รันซ้ำได้ · แถวเดิมที่ยังไม่ backfill อ่าน `name` ได้ตามปกติ
--     (ตัวประกอบถอยไปใช้ `name` เมื่อ namePerson ว่าง)
--  ⚠️ ตัว backfill ไม่ได้อยู่ในนี้ — 'คุณ' 42 รายต้องให้คนตัดสินทีละรายว่าเป็น
--     นาย/นาง/นางสาว (เดาแทนไม่ได้) · ดู scripts/backfill-customer-name-title.mjs
-- ============================================================

alter table public.customers add column if not exists "nameTitle"  text;
alter table public.customers add column if not exists "namePerson" text;
