-- ============================================================
--  Migration 0355: โซนจำได้ว่า "ใบประเมินใบไหนสร้างมันขึ้นมา" (§5E ③)
--
--  🐞 **ตัวเก็บกวาดตอนยกเลิกใบประเมินเดาเจ้าของจากเวลา** (`zoneCleanupDecision`):
--    "โซนเกิดหลังใบ + ไม่มีใครอ้างถึง = ของใบนี้ → ลบ" · แต่ `service_zones` ไม่มีคอลัมน์ชี้กลับ
--    ⇒ เคสจริง: SA เปิดใบขอพื้นที่ใหม่ "Lobby" → ก่อน SA กดส่ง TS คีย์โซน "Lobby" เองที่หน้าไซต์
--    (หรือโมดัลเพิ่มไซต์ย้อนหลัง โหมดเติมต่อ) → SA กดส่ง `materializeSurveyZones` ผูกแถวเข้าโซน
--    ของ TS ตามชื่อ → SA ยกเลิกใบ → ตัวกวาดเห็น "เกิดหลังใบ ไม่มีใครใช้" แล้ว **ลบโซนที่ TS คีย์ไว้**
--    · เส้นลบพื้นที่ที่ช่างเพิ่มหน้างาน (DELETE survey zone) ใช้ตัวตัดสินเดียวกัน = รูเดียวกัน
--    · #1702 ปิดได้แค่ครึ่ง (โซนที่มีจุดติดตั้ง) — โซนที่ยังไม่มีจุดยังโดนลบได้
--
--  ⭐ **ตัวชี้เจ้าของที่เชื่อได้ = เขียนตอนสร้าง ไม่ใช่เดาทีหลัง** — `materializeSurveyZones`
--    (ทางเดียวที่ใบประเมินสร้างโซน: ทั้ง SA กดส่ง และช่างเพิ่มพื้นที่หน้างาน) เติม id ของใบลงช่องนี้
--    ⇒ ตัวกวาดลบได้เฉพาะโซนที่ `createdBySurveyRequestId = ใบที่กำลังยกเลิก`
--    · โซนที่คนเพิ่มในทะเบียนเอง / นำเข้า / ใบประเมินแค่ "ผูก" ตามชื่อ = ช่องนี้ว่าง ⇒ ไม่มีวันถูกกวาด
--  ⚠️ ทางที่ตกไป: เทียบ `createdById` กับผู้ส่งใบ — พื้นที่ที่ช่างเพิ่มหน้างานถูกสร้างโดย *ช่าง*
--    ไม่ใช่ผู้ส่ง ⇒ เส้นลบพื้นที่ของช่างจะกวาดไม่ได้เลย · และบางทางไม่มีผู้สร้าง (null)
--
--  🔑 **FK ON DELETE SET NULL** — ลบคำร้องทั้งใบ (แอดมิน) โซนต้องอยู่ต่อ แค่ลืมว่าใครสร้าง
--    (โซนเป็นทะเบียนของลูกค้า ไม่ตายตามใบ — กติกาเดียวกับ projectId ของไซต์ mig 0299)
--
--  ✅ **ไม่ backfill โดยตั้งใจ** — ตาราง service_zones บน production ว่าง ณ วันเขียน (11/09/2026)
--    และต่อให้มีแถว การเติมย้อนหลังต้องเดาจากเวลาอีก = กลับไปใช้กฎที่ไฟล์นี้มาแก้
--    ⇒ โซนที่เกิดก่อนมิกนี้ = ช่องว่าง = ตัวกวาดไม่แตะ (fail-closed: ค้างเป็นโซนเปล่าได้ แต่ไม่หาย)
--
--  ⚠️ ต้องรัน **ก่อน deploy** — deploy ก่อนรัน: ตัวออกรหัสทิ้งคีย์ที่ไม่มีคอลัมน์เงียบ ๆ ⇒ โซนจากใบ
--    ไม่ได้ตัวชี้ ⇒ ตัวกวาดไม่ลบอะไรเลย (ปลอดภัยแต่ทิ้งโซนเปล่า) · CI ด่าน check:columns แดงจนกว่าจะรัน
--  ✅ รันซ้ำได้ (IF NOT EXISTS)
-- ============================================================

BEGIN;

ALTER TABLE public.service_zones
  ADD COLUMN IF NOT EXISTS "createdBySurveyRequestId" text
    REFERENCES public.dept_requests(id) ON DELETE SET NULL;

-- FK ON DELETE SET NULL สแกนหาแถวที่ชี้ใบที่ถูกลบ — ดัชนีเฉพาะแถวที่มีค่า (ส่วนใหญ่ว่าง)
CREATE INDEX IF NOT EXISTS service_zones_created_by_survey_request_idx
  ON public.service_zones ("createdBySurveyRequestId")
  WHERE "createdBySurveyRequestId" IS NOT NULL;

COMMENT ON COLUMN public.service_zones."createdBySurveyRequestId" IS
  'ใบประเมินพื้นที่ (dept_requests) ที่สร้างโซนนี้ — เขียนโดย materializeSurveyZones เท่านั้น · ว่าง = คนเพิ่มเอง/นำเข้า/ผูกตามชื่อ '
  '· ตัวเก็บกวาดตอนยกเลิกใบลบได้เฉพาะโซนที่ชี้ใบนั้น (§5E ③ · mig 0355)';

COMMIT;
