-- ============================================================
--  Migration 0203: ใบเสนอราคาจำได้ว่า "เลือกที่อยู่ไหน" ของลูกค้า
--
--  ต่อจาก 0202 (ลูกค้ามีได้หลายที่อยู่): ใบเสนอราคาเก็บ **ข้อความ** ที่อยู่เป็น
--  snapshot อยู่แล้ว (billingAddress / shippingAddress / branchCode) ซึ่งพอสำหรับ
--  ตัวเอกสาร แต่ไม่พอสำหรับฉบับ Rev. —
--
--  🐞 กับดักที่ต้องปิดพร้อมกัน: revise ดึงที่อยู่ "สดจาก master" ด้วยการอ่าน
--  customers.address / shippingAddress ตรง ๆ (มติผู้ใช้: ที่อยู่เปลี่ยนแล้วฉบับ
--  ใหม่ต้องได้ค่าใหม่) แต่สองคอลัมน์นั้นตอนนี้คือ **ที่อยู่หลัก** เท่านั้น ⇒ ใบที่
--  ออกให้สาขา/คลังอื่น พอกดออก Rev. จะเด้งกลับไปที่อยู่สำนักงานใหญ่เงียบ ๆ
--
--  เก็บ id ของที่อยู่ที่เลือกไว้บนใบ ⇒ Rev. ดึงข้อความสดของ **ที่อยู่ตัวเดิม** ได้
--  (ที่อยู่นั้นถูกลบไปแล้วค่อยถอยไปที่อยู่หลัก)
--
--  ใบเก่า = null ⇒ พฤติกรรมเดิมทุกประการ (ถอยไปที่อยู่หลัก) ไม่ต้อง backfill
--  เพราะเดาไม่ได้ว่าใบเก่า "ตั้งใจ" เลือกที่อยู่ไหน — ตอนนั้นเลือกไม่ได้ด้วยซ้ำ
--
--  ⚠ รันมือบน Supabase SQL Editor · รันซ้ำได้
-- ============================================================

alter table public.quotations
  add column if not exists "billingAddressId"  text,
  add column if not exists "shippingAddressId" text;
