-- ── 0261 · ชื่อ AE Supervisor บนกระดาษ PDR (ม-124) ──────────────────────
--
-- 🐞 **ผลข้างเคียงของ ม-121 ที่หลุดออกไปกับ #1289** — ตารางลายเซ็นของ FM-RD-01
-- แถว `Account Executive Supervisor` ผูกกับ `approvedByName` ซึ่งเป็นคอลัมน์ของ
-- ประตูหัวหน้าสายงานขาย (mig 0216) · พอถอดขั้นนั้นออกทั้งขั้น ไม่มีใครเขียนคอลัมน์
-- นั้นอีกแล้ว ⇒ `cell()` ใน pdrDocument.js เจอค่าว่างแล้วพิมพ์ **`N/A`** (ไม่ใช่
-- เส้นว่างอย่างที่ #1289 เขียนไว้) ⇒ ทุกใบที่พิมพ์หลังจากนั้นได้ N/A ค้างในตาราง
-- ลายเซ็น และ **ไม่มีช่องไหนบนฟอร์มกรอกมันได้เลย** ต่างจากอีก 5 แถวที่กรอกได้
--
-- ⭐ **มติผู้ใช้ 2026-08-17: "เอาช่องกรอกเหมือนอีก 5 แถว"** ⇒ แถวนี้เลิกเป็น
-- ข้อเท็จจริงของระบบ กลายเป็น **ชื่อบนกระดาษ** เหมือนที่ 0221 ทำกับอีก 5 แถว
--
-- ⚠️ **ไม่ backfill จาก `approvedByName`** — ใบเดียวที่มีค่า (SB-26080002) เคยผ่าน
-- ประตูที่ไม่มีอยู่แล้ว · ย้ายค่าข้ามความหมาย ("คนที่กดยืนยันในระบบ" → "คนที่เซ็น
-- บนกระดาษ") คือการสร้างข้อเท็จจริงที่ไม่เคยเกิด · ใบนั้นกรอกเองได้ถ้าต้องการ
--
-- ⚠️ คอลัมน์ `approvedAt/approvedById/approvedByName` **ยังอยู่ตามเดิม** (ม-121)
-- แค่ไม่มีใครอ่านหรือเขียนแล้ว — เก็บไว้ให้ประวัติของใบเก่าไม่หาย
--
-- ⚠ รันมือบน Supabase SQL Editor · รันหลัง 0260

ALTER TABLE public.dept_requests
  ADD COLUMN IF NOT EXISTS "pdrSignAeSupervisor" text
    CHECK ("pdrSignAeSupervisor" IS NULL OR length("pdrSignAeSupervisor") <= 200);

COMMENT ON COLUMN public.dept_requests."pdrSignAeSupervisor" IS
  'ชื่อผู้เซ็นบนกระดาษ ไม่ใช่ role ในระบบ (ม-124) — แทน approvedByName ที่ตายไปกับประตูหัวหน้า';

NOTIFY pgrst, 'reload schema';

-- ── ตรวจหลังรัน ────────────────────────────────────────────────────────────
-- SELECT count(*) FROM dept_requests WHERE "pdrSignAeSupervisor" IS NOT NULL;  -- ควรได้ 0
