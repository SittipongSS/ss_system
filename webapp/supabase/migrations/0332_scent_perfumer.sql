-- ============================================================
--  Migration 0332: ทะเบียนกลิ่นมีช่อง "ผู้ปรุงกลิ่น (Perfumer)"
--  (มติผู้ใช้ 2026-09-02)
--
--  ⭐ **คนละคนกับ `ownerId`/`ownerName`** — ของเดิมคือ "RD เจ้าของกลิ่น" ซึ่งระบบ
--     เซ็ตให้เองเป็น **คนที่กดรับกลิ่นเข้าทะเบียน** · วัดจากของจริง 115 กลิ่น: ค่าที่อยู่
--     ในช่องนั้นคือ Chonthicha 60 · ProjectCo.Jeab 45 · Admin 9 · Natnicha 1 —
--     ล้วนเป็นผู้ประสานงาน/แอดมิน ไม่ใช่คนปรุง ⇒ เอามาใช้แทนกันไม่ได้
--
--  ⭐ **คนเดียวกับผู้เซ็น "Perfumer" บนกระดาษ PDR** (ถ้อยคำผู้ใช้: *"perfumer คือ
--     คนปรุงกลิ่น ตอนพัฒนากลิ่นจะมีผู้เซ็น perfumer นั่นแหละอันเดียวกัน"*) ⇒ กลิ่นที่
--     เกิดจากคำร้องเติมชื่อจาก `pdrSignPerfumer` ของใบให้เอง · ที่ต้องมีช่องนี้ใน
--     ทะเบียนด้วยเพราะ **ต้องกรอกกลิ่นเก่าย้อนหลัง** ซึ่งไม่มีคำร้องให้ดึงชื่อ
--
--  ⚠️ **ชื่อในช่องนี้แช่แข็ง ไม่ซิงก์ตามบัญชี** — มันคือข้อเท็จจริงว่า *ใครปรุงกลิ่นนี้*
--     ในตอนนั้น · คนลาออกหรือเปลี่ยนชื่อทีหลัง ประวัติต้องไม่เปลี่ยนตาม (กติกาเดียวกับ
--     ชื่อบนเอกสาร — ดู `customer-name-mirrors` ฝั่ง frozen)
--     `perfumerId` เก็บไว้เพื่อกรอง/นับงานรายคนในอนาคต — ไม่ใช่ตัวที่ใช้แสดงผล
--
--  ⚠️ **ไม่มี FK ไปตารางผู้ใช้** — ผู้ใช้อยู่ใน Supabase Auth (`auth.users`) ไม่ใช่
--     ตารางในสคีมาเรา · ทั้งระบบเก็บคู่ id+name แบบนี้อยู่แล้ว (`ownerId`/`ownerName` ·
--     `acceptedById`/`acceptedByName`) ⇒ ทำตามแพตเทิร์นเดิม ไม่สร้างของใหม่
--
--  ไม่มี backfill โดยตั้งใจ — ไม่มีแหล่งข้อมูลไหนบอกได้ว่ากลิ่นเก่าใครปรุง
--  ผู้ใช้จะไล่กรอกเองจากหน้าทะเบียน (เหตุผลที่ขอช่องนี้ตั้งแต่แรก)
-- ============================================================

BEGIN;

ALTER TABLE public.scents
  ADD COLUMN IF NOT EXISTS "perfumerId"   text,
  ADD COLUMN IF NOT EXISTS "perfumerName" text
    CHECK ("perfumerName" IS NULL OR length(btrim("perfumerName")) BETWEEN 1 AND 200);

COMMENT ON COLUMN public.scents."perfumerName" IS
  'ชื่อผู้ปรุงกลิ่น (Perfumer) — ชื่อแช่แข็ง ไม่ซิงก์ตามบัญชี · คนเดียวกับผู้เซ็น Perfumer บน PDR';
COMMENT ON COLUMN public.scents."perfumerId" IS
  'id ผู้ใช้ของผู้ปรุง (ถ้าเลือกจากรายชื่อ) — ไว้กรอง/นับงานรายคน ไม่ใช่ตัวที่ใช้แสดงผล';

COMMIT;
