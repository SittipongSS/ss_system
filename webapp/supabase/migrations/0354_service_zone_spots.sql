-- ============================================================
--  Migration 0354: จุดติดตั้งของโซน — ทะเบียนจุดบนโซนเอง (มติผู้ใช้ 2026-09-11)
--
--  ⚠️ **เคยตั้งเลข 0353 แล้วเปลี่ยนเป็น 0354** — 0353 ชนกับ `0353_so_pending_approval_amount`
--  (แบรนช์ claude/so-pending-actual) ซึ่งรันบน production ไปแล้ว · ไฟล์นี้ยังไม่เคยรัน ⇒ ไฟล์นี้เป็น
--  ฝ่ายที่เปลี่ยนเลข · เนื้อ SQL เหมือนเดิมทุกบรรทัดและรันซ้ำได้ (IF NOT EXISTS / IF EXISTS)
--
--  ⭐ **ที่มา: โมดัล "เพิ่มไซต์ย้อนหลัง"** — คีย์ไซต์ที่มีอยู่ก่อนมีระบบทีละแห่ง
--  ขอบเขตที่ผู้ใช้เคาะ: *"ทะเบียนไซต์ เพิ่มแต่ ไซต์ โซน จุด นะ ยังไม่ต้องเชื่อมเครื่อง
--  กับ SO สัญญา การจ่าย และ อื่นๆ"* · ม็อก `mockups/legacy-site`
--
--  🔴 **จุดติดตั้งไม่มีที่อยู่บนโซนมาก่อน** — มีแค่ `service_survey_zones.spots` (0314)
--  ซึ่งเป็นผลของใบประเมินแต่ละใบ = ภาพ ณ วันประเมิน ไม่ใช่ทะเบียน
--  ⇒ ไซต์เก่าที่ไม่เคยประเมิน (ของเดิม ~380 จุดติดตั้ง) ไม่มีที่ให้จุดอยู่เลย
--
--  ⭐ **รูปเดียวกับ spots ของใบประเมินเป๊ะ** `[{ id, label, note }]` — ไม่มี `selected`
--  (จุดบนโซน = จุดที่มีอยู่จริง ไม่ใช่ "จุดที่ติดตั้งได้" ที่รอหัวหน้าเลือก)
--  ⇒ วันหนึ่งที่ก๊อป "จุดที่เลือกติดตั้ง" จากใบประเมินลงโซน (มติข้อ D · งานรอบหน้า)
--    ทำได้โดยไม่ต้องแปลงรูป
--  ⚠️ **ไม่มีรหัส · ไม่มี FK** — คำตามมติ 29/08 ("จุดติดตั้ง ไม่มีรหัส — อยู่ใน spots jsonb")
--    วันที่เครื่องต้องบอกว่าตั้งอยู่จุดไหน ให้ชี้ด้วย `id` ในรายการ แบบเดียวกับที่รูปในใบ
--    ประเมินชี้จุดด้วย `metadata.spotId` วันนี้
--
--  ⚠️ **ตรวจรูปของสมาชิกที่โค้ด ไม่ใช่ที่ฐาน** (`lib/service/zones.js` normalizeZoneSpots:
--  ชื่อ ≤100 · หมายเหตุ ≤500 · ไม่เกิน 200 จุดต่อโซน) — แบบเดียวกับ spots ของใบประเมิน ·
--  ฐานกันแค่ "ต้องเป็น array" ให้ของที่หลุดเข้ามาผิดรูปพังดัง ไม่ใช่กลายเป็น object เงียบ ๆ
--
--  ✅ ไม่ต้อง backfill — ทุกโซนเริ่มที่ '[]' (ตาราง service_zones บน prod ว่างอยู่แล้ว)
--  ⚠️ ต้องรัน **ก่อน deploy** — ยังไม่รัน = บันทึกโซน/จุดจากโมดัลถูกตีกลับ 503 (ด่าน
--    `zoneSpotsColumnError` · ตัวออกรหัสทิ้งคอลัมน์ที่ไม่มีเงียบ ๆ จึงต้องมีด่าน) และ CI ด่าน
--    check:columns แดงจนกว่าจะรัน
-- ============================================================

BEGIN;

ALTER TABLE public.service_zones
  ADD COLUMN IF NOT EXISTS spots jsonb NOT NULL DEFAULT '[]'::jsonb;

ALTER TABLE public.service_zones DROP CONSTRAINT IF EXISTS service_zones_spots_array;
ALTER TABLE public.service_zones
  ADD CONSTRAINT service_zones_spots_array CHECK (jsonb_typeof(spots) = 'array');

COMMENT ON COLUMN public.service_zones.spots IS
  'จุดติดตั้งของโซน [{ id, label, note }] — ตำแหน่งวางเครื่องข้างในโซน · ไม่มีรหัส (มติ 2026-08-29) '
  '· รูปเดียวกับ service_survey_zones.spots ไม่มี selected · ตรวจรูปที่ lib/service/zones.js (mig 0354)';

COMMIT;
