-- ============================================================
--  Migration 0317: products."docNote" / "docNoteEn"
--  หมายเหตุประจำสินค้าที่ติดไปกับรายการบนใบเสนอราคา/ใบสั่งขาย
--
--  ⭐ ที่มา (มติผู้ใช้ 2026-08-30): หมายเหตุรายบรรทัดมีอยู่แล้วบนใบเสนอราคา
--     (`quotation_lines.metadata.note`) แต่ต้องพิมพ์ใหม่ทุกใบ ทั้งที่ข้อความส่วนใหญ่
--     เป็นของ "สินค้าตัวนั้น" ไม่ใช่ของใบ (เงื่อนไขบรรจุ · ข้อกำหนดของกลิ่น)
--     ⇒ ตั้งไว้ที่ทะเบียนสินค้าครั้งเดียว แล้วให้ระบบเติมลงบรรทัดตอนเลือกสินค้า
--
--  ⚠️ **สองภาษาเพราะเอกสารเลือกภาษาได้** (mig 0238/0295) — ถ้าเก็บช่องเดียว
--     ใบภาษาอังกฤษจะพิมพ์หมายเหตุไทยกลางเอกสารอังกฤษ (ปัญหาเดียวกับชื่อสินค้า
--     ที่แก้ไปแล้วด้วย metadata.descriptionTh/En)
--
--  ⚠️ **ก๊อปตอนสร้างบรรทัด ไม่ผูกสด** — แก้หมายเหตุที่ทะเบียนสินค้าไม่ย้อนไปแก้ใบ
--     ที่ออกไปแล้ว (หลักการเดียวกับราคา/ชื่อที่ตรึงลงบรรทัด) · คนออกใบแก้ทับ
--     รายบรรทัดได้เสมอ
--
--  ⚠️ **ยกเว้นจากด่านอนุมัติสินค้า** (มติผู้ใช้ 2026-08-30) — แก้เฉพาะสองช่องนี้
--     ไม่ทำให้สินค้าที่อนุมัติแล้วเด้งกลับเป็น 'pending' (ซึ่งจะทำให้หลุดจาก picker
--     ทุกหน้าทันที) · ด่านอยู่ที่ PRODUCT_DOC_NOTE_FIELDS ใน lib/master/approval.js
--
--  🔴 **ต้องรันก่อน deploy โค้ดรอบนี้** — `GET /api/products` เลือกสองคอลัมน์นี้ใน
--     PRODUCT_PICKER_COLUMNS ⇒ ถ้าคอลัมน์ยังไม่มี PostgREST ตอบ error ทั้งคำขอ
--     แล้ว **picker สินค้าว่างทั้งระบบ** (ใบเสนอราคา · คำร้อง · ทะเบียนสรรพสามิต)
--     ตัว ALTER เองเป็น additive ล้วน ไม่แตะข้อมูลเดิม รันตอนไหนก็ได้ก่อน deploy
-- ============================================================

ALTER TABLE public.products
  ADD COLUMN IF NOT EXISTS "docNote" text,
  ADD COLUMN IF NOT EXISTS "docNoteEn" text;

COMMENT ON COLUMN public.products."docNote" IS
  'หมายเหตุ (ไทย) ที่เติมลงรายการในใบเสนอราคา/ใบสั่งขายตอนเลือกสินค้า — ก๊อปตอนสร้างบรรทัด ไม่ผูกสด · ยกเว้นจากด่านอนุมัติสินค้า';
COMMENT ON COLUMN public.products."docNoteEn" IS
  'หมายเหตุ (อังกฤษ) คู่กับ docNote — ใบภาษาอังกฤษพิมพ์ค่านี้แทน ตราบใดที่คนออกใบยังไม่แก้ข้อความบนบรรทัด';

NOTIFY pgrst, 'reload schema';
