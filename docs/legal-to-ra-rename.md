# ฝ่ายกฎหมาย (LG) → ฝ่ายกฎระเบียบและขึ้นทะเบียนผลิตภัณฑ์ (RA)

สถานะ: **รอตรวจ** · เขียน 2026-08-28 · แบรนช์ `claude/legal-to-ra`

มติผู้ใช้ 2026-08-28: เปลี่ยนชื่อฝ่ายทั้งชั้น — ทั้ง role, รหัสฝ่าย, ชื่อความสามารถ และป้ายบนจอ

| ชั้น | เดิม | ใหม่ |
|---|---|---|
| role | `legal` | `ra` |
| รหัสฝ่าย | `LG` | `RA` |
| ความสามารถ | `legal:view` · `legal:approve` | `ra:view` · `ra:approve` |
| ป้ายชื่อ role | ฝ่ายกฎหมาย | เจ้าหน้าที่ฝ่ายกฎระเบียบและขึ้นทะเบียนผลิตภัณฑ์ (RA) |
| ป้ายชื่อฝ่าย | ฝ่ายกฎหมาย | ฝ่ายกฎระเบียบและขึ้นทะเบียนผลิตภัณฑ์ |
| รหัสฝ่ายของขั้นตอนแม่แบบ | `LG` | `RA` |

---

## 1. ปัญหาที่ต้องแก้ก่อน: ไม่มี migration ให้ role

`app_metadata.role` อยู่ใน **Supabase Auth** ไม่ใช่ตารางในฐานข้อมูล ⇒ ไม่มี SQL ให้รัน
พร้อม deploy · ถ้าโค้ดขึ้น production ก่อนบัญชีถูกย้าย คนที่ role ยังเป็น `legal`
จะถือ role ที่ไม่มีในทะเบียน = **ไม่มี capability สักตัว = เข้าหน้าไหนไม่ได้เลย**
และถ้าย้ายบัญชีก่อนโค้ดขึ้น ก็พังทางกลับกันด้วยเหตุผลเดียวกัน

### ทางออก: แปลงตอนอ่าน (ท่าเดียวกับที่รหัสฝ่ายใช้อยู่แล้ว)

```js
const LEGACY_ROLE = { legal: 'ra' };
export function normalizeRole(role) { return role ? (LEGACY_ROLE[role] || role) : role; }
```

`normalizeDepartment` ทำแบบนี้กับ `SALES`/`LEGAL` มาตั้งแต่ก่อนหน้านี้แล้ว — เพิ่ม
`LG: 'RA'` เข้าไปในทะเบียนเดิม ⇒ **รหัสฝ่ายไม่ต้องย้ายข้อมูลเลย**

⇒ ลำดับ deploy กับลำดับย้ายบัญชี **ไม่สำคัญอีกต่อไป** ไม่มีช่วงที่ใครเข้าไม่ได้

⚠️ ต้องเรียก `normalizeRole` ที่ **ทุกจุดที่ดึง role ออกจาก app_metadata** (12 จุด) —
`lib/roleReadPoints.test.mjs` อ่านซอร์สจริงมาไล่ตรวจ จุดใหม่ที่ลืมจะแดงก่อน merge

🗑 ถอด `LEGACY_ROLE` ออกได้เมื่อไม่มีบัญชีไหนเหลือ role `legal` (เช็คด้วย Admin API)

---

## 2. รหัสฝ่ายของ "ขั้นตอนแม่แบบ" ก็ต้องย้ายด้วย (mig 0308)

⚠️ นี่เป็น **คนละทะเบียน** กับรหัสฝ่ายของผู้ใช้ แต่ใช้ชุดค่าเดียวกันโดยตั้งใจ
(`permissions.js`: *"matching the PM step-role codes SA/RD/PC/PD/QC/LG/WH"*)
และ `pmTaskEditTier` เทียบตรง ๆ:

```js
normalizeDepartment(user.department) === task?.role
```

⇒ ปล่อยให้ขั้นตอนค้างเป็น `LG` ขณะที่ฝั่งผู้ใช้เป็น `RA` แล้ว = สองฝั่งไม่มีวันแมตช์กันอีก

ค่านี้ถูกบังคับ **สามชั้น** (บทเรียนจาก mig 0192 ที่เจอปัญหาเดียวกันตอนเพิ่ม `TS`):

1. `CHECK` ของ `workflow_template_steps.role` (0121:51 → 0192:37)
2. `CHECK` ของ `project_tasks.role` (0009:15)
3. validation ใน RPC `save_workflow_template_draft` (0121:256 → 0192:42)

แก้ไม่ครบชั้นใดชั้นหนึ่ง = บันทึกแม่แบบไม่ผ่านโดยไม่มีข้อความบอกสาเหตุ

🪤 **อัปเดตแถวก่อน แล้วค่อยเปลี่ยน CHECK** — สลับลำดับแล้ว `ADD CONSTRAINT` ล้มทั้งใบ

ของจริงบนฐาน 2026-08-28: `project_tasks` 26 แถว · `workflow_template_steps` 3 แถว

⚠️ เทสต์ `workflowTemplates.test.mjs` ชี้ที่ **ใบล่าสุดที่นิยาม CHECK/RPC ชุดนี้** เสมอ
(0121 → 0192 → 0308) — ใบใหม่ที่แก้รายชื่อ role ต้องย้าย path ตามด้วย ไม่งั้นเทสต์
เทียบกับใบเก่าแล้วผ่านทั้งที่ของจริงบนฐานเป็นอีกชุด

---

## 3. คำที่ใช้บนจอ

- **ป้ายชื่อ role/ฝ่าย** (dropdown จัดการผู้ใช้ · โปรไฟล์ · ตารางผู้ใช้) — ชื่อเต็ม
- **ในประโยค** (hint ของขั้นตอน · ข้อความ error · คำอธิบาย) — `ฝ่าย RA`

ชื่อเต็ม 40 ตัวอักษรยัดลงใน `"รอฝ่ายกฎหมายตรวจ"` แล้วอ่านไม่ออก ⇒ ชื่อเต็มอยู่ที่
`ROLE_LABELS`/`DEPARTMENT_LABELS` ที่เดียว ส่วนในประโยคใช้ตัวย่อที่ตรงกับรหัสฝ่าย
ที่โชว์อยู่บนตารางอยู่แล้ว

---

## 4. ขั้นตอน rollout

1. merge PR นี้
2. **รัน `mig 0308`** บน Supabase (ย้ายรหัสฝ่ายของขั้นตอนแม่แบบ + CHECK + RPC)
3. deploy
4. ย้าย `app_metadata.role` ของ 2 บัญชี (`legal` → `ra`) ผ่าน Admin API
   — ทำเมื่อไรก็ได้ ไม่มีผลต่อการเข้าใช้งานเพราะ `normalizeRole` รับไว้แล้ว
5. เมื่อไม่เหลือบัญชี `legal` — ถอด `LEGACY_ROLE` ออกจาก `permissions.js`

บัญชีที่ต้องย้าย (ณ 2026-08-28): `sittipong0510@gmail.com` · `rd@scentandsense.co.th`
