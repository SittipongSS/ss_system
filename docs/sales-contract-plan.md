# ระบบสัญญา (บริหารงานขาย)

> สถานะ: **รอตรวจ** · ตรวจกับโค้ดเมื่อ **2026-09-06** (รอบก่อน 2026-08-20)
> · worktree `ss_system-contract` · พรีวิว `contract-dev` พอร์ต 3014
>
> 📇 **หาแม่แบบ → [§5.0 ทะเบียนแม่แบบ](#50-ทะเบียนแม่แบบ--มีอะไรบ้าง-เสร็จแค่ไหน)**
> — ตารางเดียวที่บอกว่าชนิดไหนมีแม่แบบแล้ว รุ่นอะไร ต้นฉบับใบไหน และเพิ่มฉบับใหม่ยังไง
>
> 🔄 **เพิ่ม §5.6 ยกเลิกสัญญาที่ลงนามแล้ว (มติเจ้าของ 2026-09-24 · ไม่มี migration)** เมื่อ 2026-09-24
> — ผู้อนุมัติ (admin · CD · CM · AE Sup) ยกเลิกใบ signed ได้ทุกชนิด · งานบริการหยุดตั้งแต่วันที่ยกเลิก
>
> 🔄 **เพิ่ม §5.5 เอกสารแทนสัญญาของใบสั่งขายย้อนหลัง (mig 0374 · มติ 2026-09-22)** เมื่อ 2026-09-23
> — ชนิด `signed_quotation` ใหม่ · สัญญาที่ไม่ได้เกิดจาก `POST /contracts` · **0374 ต้องรันก่อน merge โค้ดรอบนั้น**
>
> ✅ **รัน migration แล้วบน Supabase (2026-08-20)** — ตอนนั้นไฟล์ชื่อ `0277_sales_contracts.sql`
> ตอน rebase เลข 0277 ชนกับ #1348/#1350 จึงย้ายมาเป็น **0278** · เลขในชื่อไฟล์เป็นลำดับใน git
> เท่านั้น ไม่มีตารางทะเบียน migration ในฐาน ⇒ ไม่ต้องรันซ้ำ

## 1. โจทย์

ออกสัญญาจากในระบบได้ ๓ ชนิด แล้วติดตามได้ว่าใบไหนอยู่ขั้นไหน

| ชนิด | คีย์ | คู่กับงานแบบไหน |
| --- | --- | --- |
| สัญญาจ้างออกแบบกลิ่นและการพัฒนาสินค้า | `scent_design` | ดีลประเภท `SCENT` |
| สัญญาจ้างผลิต | `manufacturing` | ดีลประเภท `NPD` / `RE-ORDER` **สาย `PRODUCT`** |
| สัญญาบริการ | `service` | ดีลประเภท `NPD` / `RE-ORDER` **สาย `SERVICE`** |

มติผู้ใช้ 2026-08-20 — สี่ข้อที่ตัดสินไปแล้ว ห้ามเดาใหม่:

1. **ด่านออกสัญญา = ใบเสนอราคาผ่านการอนุมัติภายใน** (`quotations.approvalStatus = 'approved'`)
   ไม่ต้องรอลูกค้าตอบรับ (ไม่ต้องรอ `status = 'accepted'` หรือดีลขึ้น Won)
2. **คู่ชนิดสัญญากับงาน** ตามตารางด้านบน — สายธุรกิจอ่านจาก **ดีล** ก่อน (mig 0275)
   แล้วค่อยตกไปที่โครงการสำหรับดีลเก่าที่ยังไม่ระบุ
3. **การลงนามทำนอกระบบ** — พิมพ์ออกไปเซ็น แล้วอัปโหลดไฟล์ที่เซ็นแล้วกลับเข้าใบ
   ไม่มีการเซ็นดิจิทัลของลูกค้าในระบบ
4. **ระบบเจนเนื้อสัญญาเอง** จากแม่แบบ + ช่องกรอก แล้วออกเอกสารตรึงฉบับ
   ไม่ใช่ทะเบียนที่แนบไฟล์ Word ที่ทำนอกระบบ

## 2. ของอยู่ตรงไหน (กฎสามชั้น)

สัญญาเป็น **เอกสารของฝ่ายขาย** — ฝ่ายขายเป็นคนเริ่ม ⇒ บ้านอยู่ใน "บริหารงานขาย"
(`/sa/contracts`) ตาม `docs/module-ownership-rule.md` ชั้นที่ 2 เหมือนใบเสนอราคา/ใบสั่งขาย
ไม่สร้างโมดูลใหม่ และไม่เอาไปไว้ใต้ `/database`

## 3. โครงข้อมูล (migration 0278)

ตาราง `sales_contracts` — หนึ่งแถวคือหนึ่งฉบับ

| คอลัมน์ | ความหมาย |
| --- | --- |
| `id` `contractNo` | รหัสภายใน · เลขที่สัญญา **`CT-BB-YYMMXXXX-R`** (ออกตอนกด "ออกสัญญา" เท่านั้น) |
| `kind` | `scent_design` · `manufacturing` · `service` |
| `dealId` `quotationId` `customerId` `team` `ownerId` | ดีลต้นทาง · ใบเสนอราคาที่ปลดล็อก · ลูกค้า · ทีม/เจ้าของ (สำเนาจากดีล ใช้เป็นด่านรายแถว) |
| `status` | `draft` → `awaiting_signature` → `signed` · แยกสาย `revised` · `cancelled` |
| `baseNumber` `revisionNo` `revisedFromId` | สายฉบับแก้ไข (mig 0280) — เลขฐานเดียวกันทั้งสาย |
| `fields` (jsonb) | ค่าที่กรอกลงแม่แบบ (คู่สัญญา ราคา จำนวนครั้งแก้ไข ระยะเวลา ฯลฯ) |
| `templateKey` `templateVersion` | แม่แบบและรุ่นที่ใช้ |
| `issuedAt` `issuedBy` `issuedHtml` | เวลาออกเลข + **เนื้อเอกสารที่ตรึงไว้** |
| `signedAt` `signedDate` `signedFileId` `effectiveDate` `expiryDate` | หลักฐานการลงนามและวันที่ที่ติดตามต่อ |
| `cancelledAt` `cancelReason` | การยกเลิก (ต้องมีเหตุผล ≥ 10 ตัวอักษร) |

**ฉบับแก้ไข (Rev.) — กติกาเดียวกับใบเสนอราคา** (มติผู้ใช้ 2026-08-21)
ร่างลบได้ตลอดจนกว่าจะกดออกสัญญา · พอออกเลขแล้วแก้เนื้อไม่ได้ ต้อง **ออกฉบับแก้ไข** ซึ่งเป็น
แถวใหม่ที่ถือเลขฐานเดิม เลขฉบับถัดไป (`CT-BB-YYMMXXXX-1`) ส่วนใบเดิมกลายเป็น `revised`
อ่านอย่างเดียว (ฉบับตรึงยังพิมพ์ซ้ำได้) · ทะเบียนโชว์เฉพาะฉบับล่าสุดของแต่ละสาย

⚠️ **ใบที่ลงนามแล้วออก Rev. ไม่ได้** — ข้อ 3.2 ของตัวสัญญาเขียนไว้เองว่าการแก้ไขเพิ่มเติม
ต้องทำเป็นลายลักษณ์อักษรและลงนามทั้งสองฝ่าย ⇒ ของแบบนั้นคือ "บันทึกเพิ่มเติมสัญญา"
ซึ่งเป็นเอกสารคนละใบ ไม่ใช่การออกฉบับแก้ไขทับของเดิม

**ทำไมสถานะมีแค่ ๔ ค่า** — โจทย์คือ "ง่ายและติดตามได้" คำถามที่ฝ่ายขายถามจริงมีสามข้อ:
ร่างเสร็จหรือยัง · ส่งไปให้เซ็นหรือยัง · เซ็นกลับมาหรือยัง ⇒ หนึ่งคำถามหนึ่งสถานะ
ขั้นย่อยอย่าง "ส่งไปรษณีย์แล้ว" เก็บเป็นวันที่/หมายเหตุบนใบ ไม่ใช่สถานะเพิ่ม

**FK เป็น RESTRICT ทั้ง `dealId` และ `quotationId`** — สัญญาเป็นเอกสารผูกพันตามกฎหมาย
ห้ามหายตามดีล/ใบเสนอราคา ⇒ `forceDelete` บล็อกการลบพร้อมบอกเลขที่สัญญาที่ขวางอยู่
(ถ้าไม่บล็อก การลบจริงจะตายกลางทางด้วย error ดิบจาก Postgres)

**เลขที่สัญญาไม่ได้อยู่ในทะเบียน "มาตรฐานเอกสาร"** โดยเจตนา — ทะเบียนนั้นบังคับให้มี
รหัสแบบฟอร์มควบคุม (FM-xx-nn) ซึ่งสัญญายังไม่มี · รูปแบบอยู่ที่ `CONTRACT_NUMBER_PATTERN`
ใน `lib/sales/contracts.js` และตัวนับใช้ `entity_number_counters` scope `CT`

## 4. ด่านออกสัญญา (logic ล้วน ทดสอบได้)

`lib/sales/contracts.js` — `contractKindsForDeal(deal, project)` และ
`contractEligibility({ kind, deal, project, quotations })` คืนเหตุผลเป็นข้อความไทยเมื่อออกไม่ได้:

- ดีลยังไม่มีใบเสนอราคาที่อนุมัติ (หรือใบที่อนุมัติถูกยกเลิก/ตีกลับไปแล้ว) → ออกไม่ได้
- ดีลยังไม่ระบุสายธุรกิจ → `NPD`/`RE-ORDER` ออกไม่ได้ (ห้ามเดาสายให้เอง)
- ชนิดที่ขอไม่เข้าคู่กับดีล → ปฏิเสธพร้อมบอกชนิดที่ออกได้

ด่านตัวเดียวกันถูกเรียกทั้งจาก `/api/sales-planning/contracts/options` (จอใช้ตัดสินว่าจะ
โชว์อะไร) และจาก `POST /contracts` + `POST /contracts/[id]/issue` (ด่านจริง) ·
ตอนกด "ออกสัญญา" ระบบ **ตรวจสถานะอนุมัติของใบเสนอราคาซ้ำ ณ วินาทีนั้น** เพราะใบอาจถูก
ถอนอนุมัติระหว่างที่ร่างค้างอยู่

## 5. เอกสารที่พิมพ์

ประกอบจาก `lib/documents/documentShell.js` ตัวเดิม (ห้ามก๊อป CSS ไปอีกชุด) ·
แม่แบบเป็นข้อมูล: `lib/sales/contractTemplateScentDesign.js` เก็บหัวข้อ/ข้อ/ย่อหน้า
พร้อมช่องแทนค่า `{{clientName}}` `{{revisionFee}}` ฯลฯ · เปลี่ยนข้อความ = bump `version` ทุกครั้ง ต้นฉบับ = `สัญญาจ้างออกแบบกลิ่นและการพัฒนาสินค้า.pdf` ฉบับ 13 ส.ค. 2569 (`version: '20260813'`)
— รุ่นก่อนหน้าคือ Ver.20260708 ต่างกันจุดเดียวที่ข้อ 2.9 (เติมคำว่า "ใบรับแจ้ง")

⚠️ **สัญญาตัดหน้าด้วยการ "วัดจริง" ไม่ใช่ประมาณเป็นมิลลิเมตร** — PDR/ใบแจ้งชำระคำนวณ
ต้นทุนแต่ละบล็อกเป็น มม. แล้วตัดแผ่นล่วงหน้า ซึ่งใช้กับสัญญาไม่ได้: ความสูงของย่อหน้าไทย
เดาไม่ได้ และ `.sheet` เป็น `overflow: hidden` ⇒ เดาพลาดเมื่อไร **ข้อสัญญาหายเงียบจากกระดาษ**
⇒ เอกสารเขียนออกมาเป็น "สายเนื้อหา" ที่อ่านครบได้เองก่อน แล้วสคริปต์ที่ฝังในไฟล์ย้ายบล็อก
ลงแผ่น A4 ทีละใบโดยวัด `scrollHeight > clientHeight` จริงจากเบราว์เซอร์ · หัวเอกสารขึ้น
เฉพาะแผ่นแรก · ทุกแผ่นมีท้ายกระดาษ (ชื่อบริษัท · เลขที่สัญญา · หน้า n / m) · บล็อกที่สูง
เกินหนึ่งแผ่นได้แผ่นยืดแทนการถูกครอบตัด · สคริปต์พัง = ยังเห็นเนื้อครบ เสียแค่การแบ่งหน้า

🪤 **กับดักที่เสียเวลาไปหนึ่งรอบ:** `.sheetContent` ของเปลือกเป็น flex column ซึ่ง *บีบ*
บล็อกลูกให้พอดีแผ่นแทนที่จะล้น ⇒ เงื่อนไข "ล้นไหม" ไม่มีวันเป็นจริง แล้วทุกอย่างไปกอง
อยู่แผ่นเดียวโดยตัวหนังสือทับกัน · เอกสารนี้จึงบังคับ `display: block` ให้ `.sheetContent`

**หน้าตากระดาษ (มติผู้ใช้ 2026-08-20):** ระยะขอบ 20/20/15 มม. (กว้างกว่าเอกสารชนิดอื่น
เพราะต้องเซ็นและเย็บเก็บ) · ย่อหน้าความนำ/ปิดท้ายเข้าย่อหน้าแรก 12 มม. ตามต้นฉบับ ·
**หัวเอกสารโชว์ภาษาเดียว** — ชื่อบริษัทและชื่อเอกสารภาษาอังกฤษถูกตัดออก ตัวสัญญาเป็นไทย
ทั้งฉบับ (ทำฉบับภาษาอังกฤษเมื่อไร ให้สลับ *ค่า* ที่ส่งเข้าหัวเอกสาร ไม่ใช่พิมพ์คู่กัน)

**การตรึงฉบับ:** ตอนกด "ออกสัญญา" ระบบออกเลขก่อน (RPC) แล้วเรนเดอร์ HTML เก็บลง
`issuedHtml` ทันที — หลังจากนั้นการเปิดเอกสารจะเสิร์ฟฉบับที่ตรึงเสมอ ไม่ใช่ข้อมูลสด
เรนเดอร์ล้มกลางทาง = ใบมีเลขแต่ยังไม่มีเนื้อ ซึ่งเส้นทางเปิดเอกสารจะเรนเดอร์ซ้ำให้เอง
(idempotent) ไม่ใช่ใบเสีย

## 5.0 ทะเบียนแม่แบบ — มีอะไรบ้าง เสร็จแค่ไหน

ทะเบียนอยู่ที่ `lib/sales/contractTemplates.js` (ไฟล์เดียว **ไม่ใช่โฟลเดอร์+index** —
`scripts/test-loader.mjs` ไม่ resolve `dir/index.js` แบบ Next ⇒ โมดูลที่เป็นโฟลเดอร์
import ไม่ได้ในเทสต์) · แม่แบบแต่ละฉบับอยู่ไฟล์ของตัวเองชื่อ `contractTemplate<ชนิด>.js`

| เอกสาร | คีย์ | ไฟล์แม่แบบ | `version` | ต้นฉบับที่ยึด | สถานะ |
| --- | --- | --- | --- | --- | --- |
| สัญญาจ้างออกแบบกลิ่นและการพัฒนาสินค้า | `scent_design` | `contractTemplateScentDesign.js` | `20260922` | `สัญญาจ้างออกแบบกลิ่นและการพัฒนาสินค้า.pdf` (13 ส.ค. 2569) + ข้อ 2.8 ถ้อยคำใหม่ที่ผู้ใช้ส่งมา 22 ก.ย. 2569 (ดูหัวไฟล์แม่แบบ) | ✅ ใช้งานจริง |
| สัญญาบริการ | `service` | `contractTemplateService.js` | `20260906` | `(ต้นแบบ) สัญญาบริการ_Ver.20260701.docx` (ผู้ใช้ส่ง 3 ก.ย. 2569) | ✅ ใช้งานจริง |
| สัญญาจ้างผลิต | `manufacturing` | — (ทะเบียนตั้งเป็น `null`) | — | **ยังไม่มีต้นฉบับ** | 🔴 ออกได้เฉพาะทาง "เอกสารภายนอกใช้แทนสัญญา" |
| บันทึกเพิ่มเติมสัญญาจ้างออกแบบกลิ่นน้ำหอม | `scent_design_addendum` | `contractTemplateAddendum.js` | `20260821c` | `บันทึกเพิ่มเติมสัญญาจ้างออกแบบกลิ่นน้ำห.pdf` (21 ส.ค. 2569) | ✅ ใช้งานจริง · **อยู่นอกทะเบียน** (import ตรง 3 จุด) |

**จุดที่ตั้งใจต่างจากต้นฉบับ — สัญญาบริการ** (นอกเหนือจาก 4 จุดที่ต้นฉบับขัดกันเอง ซึ่งบันทึกอยู่หัวไฟล์)
· **ตำแหน่งผู้ลงนามฝั่งผู้ว่าจ้าง = "ผู้มีอำนาจ/ผู้รับมอบอำนาจ"** (มติผู้ใช้ 2026-09-06 · `version 20260906`)
  ต้นฉบับเป็นฉบับ *กรอกจริง* ของลูกค้ารายหนึ่ง คำว่า "กรรมการผู้จัดการ" ที่เห็นจึงเป็นตำแหน่งของคนคนนั้น
  ไม่ใช่คำของแม่แบบ — ย่อหน้าปิดท้ายของต้นฉบับเองก็เขียน "โดย กรรมการผู้มีอำนาจหรือ ผู้รับมอบอำนาจ"
  ⚠️ ฝั่งผู้รับจ้างยังเป็น "กรรมการผู้จัดการ" ตายตัว เพราะเป็นตำแหน่งจริงของผู้ลงนามเรา
  (บันทึกเพิ่มเติมสัญญาออกแบบกลิ่นใช้คำเดียวกัน · ตัวสัญญาออกแบบกลิ่น **ไม่มีค่าตั้งต้น**
   ตามต้นฉบับ 13 ส.ค. ที่ตัดบรรทัดนี้ออก — เว้นว่าง = ไม่พิมพ์บรรทัดนั้น)

⚠️ **ชื่อเอกสารบนกระดาษต้องตรงกับ `titleTh` ของแม่แบบ** — ตัวเรนเดอร์อ่าน
`CONTRACT_KIND_DOC_TITLES` (`lib/sales/contracts.js`) ก่อนเสมอ `template.titleTh` เป็นแค่ทางถอย
⇒ เขียนคนละคำเมื่อไร กระดาษพิมพ์คำที่ไม่มีใครตั้งใจ · เคยเป็นแบบนั้นจริงกับสัญญาบริการ
(กระดาษ "สัญญาให้บริการ" · แม่แบบ "สัญญาบริการ") — **มติผู้ใช้ 2026-09-06: ยึด "สัญญาบริการ"**
มีเทสต์ล็อกสองที่ให้พูดคำเดียวกันแล้ว

⚠️ **ห้ามแต่งข้อความสัญญาขึ้นเองไม่ว่ากรณีใด** — ชนิดที่ไม่มีต้นฉบับต้องบอกตรง ๆ ว่ายังไม่มี
(`MISSING_TEMPLATE_NOTE`) เอกสารผูกพันตามกฎหมาย: ข้อความที่ไม่มีใครอนุมัติคือความเสียหาย ไม่ใช่ช่องว่าง
· ด่านฝั่ง server มีสามจุดใช้ข้อความเดียวกัน: สร้างสัญญา (400) · ออกสัญญา (409) · เปิดเอกสาร (409)
· `buildContractHTML` โยน error ทันทีถ้าไม่มีแม่แบบ ไม่ปล่อยให้เรนเดอร์เอกสารครึ่ง ๆ

### แม่แบบหนึ่งฉบับประกอบด้วยอะไร

| คีย์ | คือ | หมายเหตุ |
| --- | --- | --- |
| `key` · `version` | รหัสแม่แบบ + รุ่น | ร่างตรึง `templateKey` ตั้งแต่ **ตอนสร้าง** · `templateVersion` ตรึงตอน **กดออกสัญญา** (คนละจังหวะ) |
| `titleTh` · `titleEn` | ชื่อเอกสาร | ⚠️ **ทั้งคู่ไม่ได้ขึ้นกระดาษ** — ชื่อบนเอกสารมาจาก `CONTRACT_KIND_DOC_TITLES` (`lib/sales/contracts.js`) · `titleEn` ถูกส่งเป็น `null` เพราะหัวใบภาษาเดียว |
| `fields[]` | ช่องกรอกของใบ | `{key,label,type,required,default,source,hint}` · **จอแก้** (หน้ารายละเอียด) อ่านจากที่นี่ ไม่ฮาร์ดโค้ด · จอสร้างไม่มีช่องของแม่แบบเลย — ค่าตั้งต้นเติมฝั่ง server ตอน POST (`contractFieldDefaults`) |
| `intro[]` | ย่อหน้าความนำ | ย่อหน้าแรกเข้า 12 มม. |
| `definitions` | หมวดคำจำกัดความ | `null` ได้ (สัญญาบริการไม่มี ⇒ ตัวเรนเดอร์คืนสตริงว่าง) |
| `sections[]` / `clauses[]` | ตัวข้อสัญญา | สัญญาหลักใช้ `sections` (หมวด → ข้อ) · บันทึกเพิ่มเติมใช้ `clauses` ตรง ๆ |
| `closing[]` · `signatures[]` | ย่อหน้าปิดท้าย + ช่องเซ็น | ช่องเซ็นเป็นเส้นว่างเสมอ (เซ็นด้วยปากกา) |

**บล็อกพิเศษที่ข้อหนึ่งข้อใส่ได้** — `title` (หัวข้อย่อยตัวหนา) · `table: {source:'quotationLines'}`
(ตารางบริการ 7 คอลัมน์ — **หนึ่งแถวต่อหนึ่งใบเสนอราคา** ค่ามาจากหัวใบ + token · มีเฉพาะช่อง "รายละเอียด" ที่ต่อจาก `description` ของบรรทัดในใบ) · `table: {columns:[...]}` (ตารางสูตรของบันทึกเพิ่มเติม
ดึงจากคำร้องพัฒนากลิ่นที่ปิดเรื่อง) · `lines: {source:'quotationInstallments'}` (งวดชำระจาก
`quotations.paymentPlan.installments`) · `after` (ย่อหน้าต่อท้ายรายการ)

### ค่าไหนระบบเติมให้ ค่าไหนต้องกรอกเอง

- เติมจาก **ทะเบียนลูกค้า** (`source: 'customer'`) จริง ๆ 2 คีย์: `clientName` (อ่านผ่าน
  `customerNameIn(customer,'th')` เพราะสัญญาเป็นเอกสารไทย) · `clientRegNo`
- `clientAddress` ประกาศ `source:'customer'` ก็จริง แต่ **ผู้เรียกทับให้ก่อนเสมอ** — POST
  `/contracts` ใช้ลำดับ *ที่อยู่ออกบิลของใบเสนอราคา → ที่อยู่ออกบิลหลักของลูกค้า →
  คอลัมน์ `customers.address` (กระจกยุคเก่า)* ⇒ ที่อยู่บนสัญญาตรงกับใบเสนอราคาของมันเอง
- เติมจาก **ใบเสนอราคา**: ตารางบริการและงวดชำระเติมตอนเรนเดอร์
  🪤 แต่ **ช่องกรอกที่ตั้ง `source:'quotation'` ไม่ถูกเติมเลย** — `contractFieldDefaults`
  เดินเข้าสาขานี้จริง (สัญญาบริการตั้งไว้ที่ `totalWithVat`) แต่ข้างในรู้จักคีย์เดียวคือ
  `contractValue` ซึ่งไม่มีแม่แบบไหนประกาศ ⇒ ยอดรวมยังต้อง **พิมพ์เอง** (ดู §7 ข้อ 5)
- ค่าที่คนกรอกไว้แล้วชนะเสมอ — `contractFieldDefaults` เติมเฉพาะช่องที่ยังว่าง
- ช่องที่ระบบคำนวณให้ไม่ได้จึงต้องกรอกมือ: `machineCount` (จำนวนเครื่อง — เครื่องเกิดตอน TS
  จัดสรรลงโซนทีหลัง) · `finalPaymentDate` (วันของงวดอยู่ในช่อง `note` ที่เป็นข้อความอิสระ)

### เพิ่ม/แก้แม่แบบ — เช็กลิสต์

1. **ต้องมีไฟล์ต้นฉบับจากผู้ใช้ก่อน** (PDF/DOCX) — ไม่มีต้นฉบับ = ไม่เริ่ม
2. คัดข้อความ **ตัวต่อตัว** ลงไฟล์แม่แบบ · ต้นฉบับขัดกันเอง = ถามผู้ใช้ ห้ามตัดสินเอง
   (สัญญาบริการมี 4 จุดแบบนี้ — ผู้ใช้ชี้ขาดครบแล้ว บันทึกอยู่หัวไฟล์)
3. ตรวจซ้ำแบบเทียบตัวอักษรสองทาง — เขียนสคริปต์เฉพาะกิจ (แตกข้อความจาก PDF/DOCX แล้ว diff
   กับ HTML ที่เรนเดอร์) ⚠️ **ไม่มีสคริปต์นี้เก็บไว้ในรีโป** ต้องเขียนใหม่ทุกรอบ
4. จุดที่ **ตั้งใจต่างจากต้นฉบับ** ต้องเขียนไว้ทั้งในหัวไฟล์และในเอกสารนี้ (§5.1 · §5.2)
5. เพิ่มคีย์ในทะเบียน `TEMPLATES` แล้วเทสต์: ช่องกรอกครบ · เรนเดอร์ไม่มี `{{token}}` ค้าง
6. **เปลี่ยนเนื้อ = bump `version` เสมอ** — ใบเก่าตรึง HTML ของตัวเองไว้แล้ว แต่ต้องรู้ว่า
   ใบไหนออกด้วยรุ่นไหน
7. 🪤 **ห้ามมี backtick ในคอมเมนต์ของไฟล์ CSS/แม่แบบที่อยู่ใน template literal** — พังตอน parse
   (เจอมาแล้วหลายรอบ ล่าสุด 2026-09-03)

## 5.1 จุดที่ตั้งใจต่างจากต้นฉบับ — อย่า "แก้ให้ตรง"

ตรวจเทียบตัวอักษรทั้งฉบับกับต้นฉบับล่าสุด (`สัญญาจ้างออกแบบกลิ่นและการพัฒนาสินค้า.pdf`
ฉบับ 13 ส.ค. 2569) เมื่อ 2026-08-21 — ข้อสัญญา 23/23 ข้อและทุกส่วนนอกข้อตรงกันหมด
เหลือสามจุดที่ต่าง และ **มติผู้ใช้คือคงแบบปัจจุบัน**:

| จุด | ต้นฉบับ | ระบบ |
| --- | --- | --- |
| ข้อ 1.1 | "ไม่มีการตอบกลับภายใน **ภายใน** 7 วัน" (พิมพ์ซ้ำ) | พิมพ์ "ภายใน" ครั้งเดียว |
| ช่องเซ็น | `ลงชื่อ……ผู้ว่าจ้าง` แล้วขึ้นบรรทัดใหม่เป็นวงเล็บชื่อ | คำว่าผู้ว่าจ้าง/ผู้รับจ้างอยู่บรรทัดของตัวเองใต้วงเล็บชื่อ |
| ที่อยู่ผู้รับจ้าง | หน้าแรกไม่มี "ถนนเพชรเกษม" · ย่อหน้าปิดท้ายมี | ใช้ค่าจากทะเบียนบริษัทค่าเดียวทั้งใบ |
| ตำแหน่งผู้ลงนามฝั่งผู้ว่าจ้าง | ฉบับ 13 ส.ค. **ตัดบรรทัดนี้ออก** | ค่าตั้งต้น "ผู้มีอำนาจ/ผู้รับมอบอำนาจ" (มติ 2026-09-06 · `20260813b`) — ลบทิ้งรายใบได้ = กลับไปเหมือนต้นฉบับ |
| ข้อ 2.8 | ข้อความที่ผู้ใช้ส่งมา 22 ก.ย. เขียน "ผู้รับจ้างจะ**เป็นผู้ผลิต**ให้กับทางผู้ว่าจ้างแต่เพียงผู้เดียว" จบแค่นั้น · ชื่อสองบริษัทพิมพ์ตรง | "จะ**ผลิต**ให้กับ...แต่เพียงผู้เดียว **และผู้รับจ้างไม่สามารถนำไปผลิตให้กับผู้ว่าจ้างรายอื่นได้**" (มติ 2026-09-22 เลือกความหมาย "ผลิตสูตรนี้ให้ลูกค้ารายนี้รายเดียว" · ประโยคท้ายยกจากฉบับเดิม) · ชื่อเป็นช่อง `{{contractorName}}`/`{{clientName}}` |

⚠️ ใครมาตรวจรอบหน้าแล้วเห็นห้าจุดนี้ **ห้ามแก้ให้ตรงต้นฉบับ** โดยไม่ถามเจ้าของเรื่องก่อน —
ทุกจุดผ่านการตัดสินไปแล้ว (สองข้อแรกเป็นการเก็บกวาดที่ตั้งใจ · ข้อสามคือต้นฉบับไม่ตรงกันเอง ·
ข้อสี่คือ **มติ 2026-09-06 กลับด้านจากมติ 2026-08-21** เพราะสามฉบับที่ลูกค้ารายเดียวกันเซ็น
— สัญญาออกแบบกลิ่น · บันทึกเพิ่มเติม · สัญญาบริการ — ต้องใช้คำเดียวกัน ·
ข้อห้าคือ **มติ 2026-09-22** เลือกความหมายของ "แต่เพียงผู้เดียว" แล้วเขียนให้อ่านได้ทางเดียว)

**ส่วนที่ระบบเพิ่มจากต้นฉบับโดยตั้งใจ:** หัวเอกสาร (โลโก้ · ข้อมูลบริษัท · เลขที่/วันที่สัญญา ·
อ้างอิงใบเสนอราคา) และท้ายกระดาษพร้อมเลขหน้าทุกแผ่น

## 5.2 บันทึกเพิ่มเติมสัญญา (mig 0282)

มติผู้ใช้ 2026-08-21 — ต้นฉบับ `บันทึกเพิ่มเติมสัญญาจ้างออกแบบกลิ่นน้ำห.pdf`

- **ออกจากสัญญาหลักที่ลงนามแล้วเท่านั้น** · สัญญาที่ยังไม่เซ็นให้ใช้ Rev. แทน
- **ต้องอ้างคำร้องพัฒนากลิ่นที่ปิดเรื่องแล้ว** (`kind=scent_dev` · `status=closed`) เพราะ
  ตารางสูตรในบันทึก (ชื่อสูตร · รหัสสูตร · วันที่สูตร) ดึงจากแถวคำร้องที่ผลิตสูตรออกมาแล้ว
  (`producedFormulaId` → ทะเบียนสูตร) แล้ว **ตรึงลงใบตั้งแต่ตอนสร้าง**
- เลขที่ = เลขสัญญาแม่ + `-A{ครั้งที่}` (เช่น `CT-26080001-0-A1`) — ไม่มีตัวนับของตัวเอง
  ครั้งที่ถูกจองด้วย `UNIQUE (contractId, addendumNo)` ⇒ กดพร้อมกันก็ไม่ได้เลขซ้ำ
- ตารางแยก `sales_contract_addenda` ไม่ใช่ `kind` ใหม่ของสัญญา — `kind` ของสัญญาหมายถึง
  *ชนิดสัญญา* ยัดรวมกันเมื่อไร ทุกด่านที่ถาม "สัญญาชนิดไหน" ต้องมาแยกกรณีเพิ่ม
- ⚠️ **ต่างจากต้นฉบับหนึ่งจุดโดยตั้งใจ** (มติผู้ใช้ 2026-08-21): ต้นฉบับแยกวันที่เป็นบรรทัด
  ใต้หัวเรื่อง แล้วประโยคแรกขึ้นต้นว่า "บันทึกฉบับนี้ทำขึ้นที่ …" ⇒ ระบบย้ายวันที่เข้ามาใน
  ประโยคเป็น **"บันทึกฉบับนี้ทำขึ้นเมื่อ … ที่ …"** ให้อ่านจบในประโยคเดียว
- ⚠️ **จุดที่สองที่ตั้งใจต่าง** (มติผู้ใช้ 2026-08-21): ความนำของต้นฉบับใส่เลขทะเบียนนิติบุคคล
  ให้ฝั่งผู้ว่าจ้างฝ่ายเดียว ⇒ ระบบเติมของฝั่งผู้รับจ้างด้วย ให้ครบทั้งสองฝ่ายเท่าสัญญาหลัก
  (แม่แบบ `version: 20260821b`)
- ข้อ 2 อ้างวันที่สัญญาแม่แบบ **ไม่มีคำว่า "เดือน"** (`contractDatePlainTh`) ส่วนความนำใช้รูปเต็ม
  `contractDateTh` — ต้นฉบับเขียนคนละรูปสองที่จริง ไม่ใช่ความพลาด ห้ามรวบให้เหมือนกัน
- **ไม่มีให้เลือกคำร้อง** (มติผู้ใช้ 2026-08-22: *"คำร้องมันมาจาก SO อยู่แล้ว แล้วสัญญาหลัก
  ก็มาจาก SO งั้นก็ไม่ต้องเลือกคำร้องเลย"*) — ระบบหาให้เอง
  เส้นที่ใช้ผูกคือ **`dealId`**: QT · SO · คำร้อง · สัญญา ถือ `dealId` ของตัวเองทุกใบ
  ⇒ `dept_requests` ที่ `dealId` = `sales_contracts."dealId"` · kind `scent_dev` · ปิดเรื่องแล้ว
  ⚠️ เคยไล่สาย `quotationId` → `sales_orders` → `"salesOrderId"` ซึ่งแคบกว่าและขาดง่าย
  (ใบสั่งขายที่ไม่ได้ออกจากใบเสนอราคาใบที่ผูกกับสัญญา = ลิสต์ว่างทั้งที่ของมีในดีลเดียวกัน)
  · กรองด้วยลูกค้าอีกชั้นเสมอ (`customerId` ก่อน แล้วตกมาที่ชื่อสำหรับใบเก่า)
  · หลายใบ = หยิบใบที่ปิดเรื่อง**เก่าสุดก่อน** ให้ครั้งที่ 1, 2, 3 ไล่ตามลำดับจริง
  · จอไม่กรองเอง — `lib/sales/addendumRequests.js` เป็นคนตัดที่เดียว ทั้งตอนโชว์และตอนกดสร้าง
- **หนึ่งคำร้อง = หนึ่งบันทึก** (มติผู้ใช้ 2026-08-22) — ใบที่ถูกใช้แล้วหายจากดรอปดาวน์
  กันสามชั้น: กรองในลิสต์ · ถามฐานซ้ำตอนกดสร้าง · partial unique index ของ mig 0282
  ⚠️ นับเฉพาะบันทึกที่ **ยังไม่ถูกยกเลิก** — ยกเลิกแล้วคำร้องต้องกลับมาใช้ได้อีก
- เอกสารใช้ **ชิ้นส่วนเดียวกับสัญญา** (CSS · สคริปต์ตัดหน้าที่วัดจริง · ตัวเติม token)
  ⇒ หน้าตาเป็นเอกสารชุดเดียวกัน และแก้ที่เดียวมีผลทั้งคู่

## 5.3 ใบเสนอราคาถูกปิด → สัญญาต้องรู้ตัว (มติผู้ใช้ 2026-08-22)

**ร่างปิดตาม · ใบที่ออกเลขแล้วแค่เตือน** — สัญญาที่ออกเลข/ลงนามแล้วเป็นเอกสารผูกพัน
ระบบยกเลิกแทนคนไม่ได้ · ร่างที่ยังไม่ออกเลขไม่มีสถานะทางกฎหมาย จึงยกเลิกตามได้

นับว่า "ปิด" เมื่อ (`lib/sales/contractQuotationState.js` — กติกาที่เดียว จอ/API/ตัวไล่ปิดใช้ร่วมกัน):
- `status = 'revised'` ออกฉบับแก้ไข ⚠️ **เช็คก่อนสถานะอนุมัติเสมอ** ใบที่ออก Rev. ยังค้าง
  `approvalStatus = 'approved'` ⇒ สลับลำดับแล้วได้เหตุผลผิดว่า "อนุมัติถูกถอน"
- `status = 'cancelled'` ยกเลิก · `status = 'rejected'` ลูกค้าไม่รับ
- `approvalStatus <> 'approved'` อนุมัติถูกถอน/รีเซ็ต (ด่านออกสัญญาคือ approved)
- **"อนุมัติที่ใบอื่น"** = ใบอื่นของดีลเดียวกันอนุมัติทีหลัง → **เตือนอย่างเดียว ไม่ปิดร่างตาม**
  (ดีลหนึ่งมีใบอนุมัติหลายใบพร้อมกันได้จริง: ออกแบบกลิ่นใบหนึ่ง ผลิตอีกใบหนึ่ง)

ไล่ปิดสองจังหวะ ฟังก์ชันเดียวกัน (`contractQuotationSync.js` · ซ้ำได้ไม่เสียหาย):
1. ตอนออก Rev. — คนเห็นผลทันที
2. ตอนเปิดทะเบียน/หน้าใบ (lazy) — สถานะใบเสนอราคาเปลี่ยนได้จากทางที่โค้ดฝั่งสัญญาไม่ได้ถือมีดด้วย
   (RPC ของ accept/won · แก้มือบนฐาน) ⇒ ไล่เฉพาะทาง 1 จะมีร่างค้างที่ไม่มีวันปิด
⚠️ คำสั่งยกเลิกย้ำ `.eq('status','draft').is('contractNo', null)` ที่ตัว UPDATE ด้วย —
ระหว่างอ่านมาอาจมีคนกดออกเลขไปแล้ว ใบที่ออกเลขแล้วห้ามถูกตัวไล่ปิดแตะเด็ดขาด

แจ้งเตือนเข้าแอปหาเจ้าของ+คนสร้าง (`kind: 'contract_quotation_closed'`)
· `dedupeKey = QTCLOSE-<quotationId>-<contractId>` กันแจ้งซ้ำทุกครั้งที่มีคนเปิดทะเบียน
· `sales_contract` ลงทะเบียนใน `notificationTargets` แล้ว (ลิงก์ไปที่ใบ)
· **เข้ากล่องกระดิ่งแล้ว** ตั้งแต่ 2026-09-06 — ดู §5.4

## 5.4 สัญญาเข้ากระดิ่ง (2026-09-06)

ก่อนหน้านี้แจ้งเตือนของสัญญาถูกเขียนลงตารางจริง แต่ไปโผล่เฉพาะหน้าเต็ม `/notifications`
ซึ่งไม่มีใครเปิด ⇒ ข้อความ "ร่างสัญญาถูกยกเลิกตามใบเสนอราคา" ไม่เคยถึงตาเจ้าของใบ

**สี่ kind อยู่ใน `CONTRACT_BELL_KINDS` (`lib/notifications.js`)** — สองตัวล่างเพิ่ม 2026-09-25 (§5.7)

| kind | ยิงเมื่อ | ใครได้ | กุญแจกันซ้ำ |
| --- | --- | --- | --- |
| `contract_quotation_closed` | ใบเสนอราคาที่สัญญาอ้างถึงถูกปิด (§5.3) | เจ้าของใบ + คนสร้าง | `QTCLOSE-<quotationId>-<contractId>` |
| `contract_signature_overdue` | ใบค้างขั้น "รอลงนาม" เกิน `SIGNATURE_LATE_DAYS` วัน | เจ้าของใบ (ไม่มีก็คนสร้าง) | `CTLATE-<วันไทย>-<userId>` |
| `contract_approval_pending` | มีใบรอผู้อนุมัติ — เอกสารแทนสัญญาที่**แนบไฟล์แล้ว** + ใบรอรับรองการลงนาม (cron ประจำวัน) · ชุดใบ = เลน "รอฉันรับรอง" ของทะเบียนเป๊ะ (`isContractWaitingOnMe` — **ไม่นับ**เอกสารแทนสัญญาของใบสั่งขายย้อนหลัง ซึ่งอนุมัติพร้อมใบ) | AE Supervisor ที่ยังใช้งาน (`SALES_BELL_ROLES` · ไม่มีสักคนค่อยถอยไป admin) — CD/CM อนุมัติได้แต่**ไม่รับกระดิ่ง** (มติ 24/09 ข้อ 6) | `CTAPPROVE-<วันไทย>-<userId>` |
| `contract_approved` | ผู้อนุมัติ (admin · CD · CM · AE Sup) กดอนุมัติเอกสารแทนสัญญา / รับรองการลงนาม (ยิงทันทีใน route · ใบย้อนหลังไม่ผ่านทางนี้) | เจ้าของใบ + คนสร้าง **ยกเว้นคนกดเอง** | `CTAPPROVED-<contractId>` |

🪤 **เข้าทาง `kinds` ห้ามใส่ `'sales_contract'` ลง `entityTypes`** — นอกจากเหตุผลเดิม
(ลากทุกความเคลื่อนไหวเข้ากล่องจนคำร้องตกขอบ 30 แถว) ยังมีด่านแข็งอีกชั้น: เทสต์
`notifications.test.mjs` บังคับว่าทุก `entityType` ในกล่องต้องมีเธรดใน `UPDATE_ENTITIES`
และ **สัญญาไม่มีเธรด** ⇒ ใส่แล้วเทสต์แดงทันที

**ตัวทวงค้างลงนาม + ตัวทวงรออนุมัติ อยู่ที่ cron ไม่ใช่ตอนเปิดหน้า** (ต่างจาก §5.3 และทะเบียนต่อสัญญา
ที่กวาดตอนมีคนเปิดทะเบียน) — ทะเบียนสัญญากรองตามขอบเขตของคนเปิด (`loadScoped`)
⇒ กวาดตอนนั้นจะทวงได้เฉพาะคนที่บังเอิญเปิดหน้า ซึ่งคือคนที่ไม่ต้องทวงอยู่แล้ว
· กติกาอยู่ที่ `lib/sales/contractNotify.js` (ฟังก์ชันบริสุทธิ์) · ตัวยิงอยู่ที่
`/api/cron/daily-digest` (08:30 ไทย จ–ศ) ซึ่งรันด้วยสิทธิ์ admin จึงเห็นทุกใบทุกทีม

⚠️ **หนึ่งคนหนึ่งเด้งต่อวัน** (กติกาผู้รับ mig 0185) — ใบค้าง 6 ใบของคนเดียวกันได้ข้อความ
เดียวที่บอกว่า 6 ใบ · แถวผูกกับ **ใบที่ค้างนานสุด** เพื่อให้ลบใบนั้นแล้วแจ้งเตือนถูกกวาดตาม
⚠️ DELETE ของสัญญาเรียก `purgeNotificationsMany` เอง — สัญญาไม่มีเธรด จึงไม่ผ่าน
`purgeUpdates` ที่กวาดแจ้งเตือนให้เองเหมือน entity อื่น

## 5.5 เอกสารแทนสัญญาของใบสั่งขายย้อนหลัง (mig 0374 · มติ 2026-09-22)

สายนี้เป็น **ที่มา `external` ชนิดใหม่ของเอกสารภายนอก** ที่ไม่ได้เกิดจาก `POST /contracts` และไม่เดินขั้นรับรองที่หน้าสัญญา
เต็มเส้นอยู่ที่ [historical-sales-orders.md](historical-sales-orders.md)

**ชนิดเอกสารภายนอกเพิ่มตัวที่สี่: `signed_quotation` = "ใบเสนอราคาที่ลูกค้าเซ็น"**
(CHECK `sales_contracts_external_kind` ของ 0322 ถูกแทนใน 0374 · `EXTERNAL_DOC_KINDS` = `customer_po` · `email` ·
`paper_contract` · **`signed_quotation`** · `other`) — งานบริการเก่าจำนวนมากไม่เคยมีสัญญา มีแต่ใบเสนอราคาที่ลูกค้าเซ็นกลับมา

### วงจรชีวิตของ "เอกสารแทนสัญญา" (substitute contract)

| ระยะ | เกิดอะไร |
|---|---|
| เกิด | **RPC คีย์ใบย้อนหลังสร้างให้** (`create_historical_sales_order`) พร้อมใบในทรานแซกชันเดียว — ไม่ได้มาจาก `POST /contracts` · `kind='service'` · `source='external'` · `status='draft'` · `contractDate` = `effectiveDate` = วันเริ่มสัญญา · `expiryDate` = วันสิ้นสุด · `team`/`ownerId` ตามดีลภาชนะ · **`metadata.historicalSalesOrderId` ชี้กลับที่ใบ** (= ตัวชี้ว่าเป็น substitute, `isSubstituteContract`) |
| ระหว่างใบยังไม่อนุมัติ (`draft`/`pending_approval`/`rejected`) | **ล็อก แก้ · ลบ · ยกเลิก · อนุมัติที่หน้าสัญญา** (`historicalContractLockReason` — route ทั้งสี่เส้นและจอถามตัวเดียวกัน) · หน้าสัญญาขึ้น StatusNotice + ลิงก์กลับไปที่ใบ · ราง (`SUBSTITUTE_STEPS`) ใช้หมุดชุดเดียวกับ external ทุกคำ ต่างแค่คำใบ้: ร่าง = "แก้ที่ฟอร์มคีย์ใบสั่งขายย้อนหลัง" · จบ = "อนุมัติพร้อมใบสั่งขายย้อนหลัง" · **เลน "รอฉันรับรอง" ของ AE Sup ไม่นับร่างพวกนี้** (เป็นงานของคิวใบสั่งขาย ไม่ใช่คิวสัญญา — ไม่งั้นงานเดียวโผล่สองที่) |
| ระหว่างใบ `pending_approval` | **ชุดไฟล์ถูกตรึง** (`historicalContractFilesFrozen`) — `POST /api/attachments` และ `guardAttachmentWrite` ตอบ 409 "ดึงกลับก่อนถ้าต้องเปลี่ยนไฟล์" ⇒ ไฟล์ที่ AE Sup เห็นตอนกด คือไฟล์ที่ถูกอนุมัติจริง |
| ตอน AE Sup อนุมัติใบ | RPC อนุมัติเรียก **`approve_external_sales_contract` ในทรานแซกชันเดียวกับการอนุมัติใบ** ⇒ สัญญาเป็น `signed` + **ออกเลข CT** · ไฟล์ที่ผูกเป็นฉบับลงนามคือ **`p_signed_file_id` ที่ผู้อนุมัติเห็นในโมดัล** (RPC ตรวจว่าเป็น attachment ของสัญญาใบนี้ `docType='external_doc'` จริง · เส้น `approve-external` ตรวจเหมือนกัน) · อ่านซ้ำหลังเรียกว่าได้ `signed` + มีเลขจริง ไม่งั้น raise (ตัวเขียนค่ากลางทิ้งคีย์ที่ไม่รู้จักเงียบ ๆ) |
| ตอนใบถูกยกเลิก/ลบ | **trigger `sales_orders_historical_void_contract_upd/_del` ยกเลิกสัญญาให้ในทรานแซกชันเดียว** ทั้งฉบับร่างและฉบับลงนามแล้ว — ไม่มีตัวตามเก็บฝั่ง JS · มี `NOT EXISTS` กันไม่ให้แตะสัญญาที่ใบอื่นซึ่งยังไม่ยกเลิกผูกอยู่ |
| สัญญากำพร้า | ใบหายหรือใบยกเลิกแล้ว ⇒ `historicalContractLockReason` คืน `null` ⇒ **ล็อกไม่มีวันค้างถาวร** แอดมินจัดการได้ตามปกติ |

⚠️ **`YYMM` ของเลข CT = เดือนที่ AE Sup อนุมัติ** ไม่ใช่เดือนของสัญญาจริง (ซึ่งเป็นปีก่อนได้) —
เลขรันมาจากบ่อเดียวของทั้งบริษัท ตัวตัดรอบยังเป็น `'-'` ⇒ เพดาน 9,999 ตลอดอายุระบบตามเดิม (ดูท้ายไฟล์)
⚠️ **เลขของสัญญาที่ถูก void ไม่คืนเข้าบ่อ** — แก้ข้อมูลใบที่อนุมัติแล้วต้องยกเลิกใบแล้วคีย์ใหม่ ⇒ กินเลข CT ใบละครั้ง
🔒 ยาม: `lib/sales/contracts.test.mjs` (ล็อก/ตรึงไฟล์/เลนรออนุมัติ/ชนิดใหม่) · `attachmentWriteGateCoverage.test.mjs` ·
`lib/sales/historicalApprovalMigration.test.mjs` (trigger + ลำดับใน RPC อนุมัติ)

## 5.6 ยกเลิกสัญญาที่ลงนามแล้ว (มติเจ้าของ 2026-09-24)

*"ย้อน/ยกเลิก ให้สิทธิกับผู้ที่สามารถกดอนุมัติ"* — **เพิ่มสิทธิ์** ไม่ถอดของเดิมจากใคร

| เรื่อง | กติกา |
|---|---|
| ใครกดได้ | `canApproveExternalContract` (admin · CD · CM · AE Sup — ด่านเดียวกับรับรองการลงนาม) · AC Sup/AE/AC ไม่เห็นปุ่ม |
| ใบไหน | `signed` ทุกชนิด (บริการ · ออกแบบกลิ่น · จ้างผลิต) ทุกที่มา · ทางยกเลิกเดิมของร่าง/รอลงนาม/รอรับรอง (`canCancelContract`) **ไม่ขยับ** |
| ติดด่าน (ปุ่มจาง + เหตุ) | เอกสารแทนสัญญาที่ใบสั่งขายย้อนหลังของมันยังมีชีวิต → ยกเลิกที่ใบสั่งขาย (`HISTORICAL_CORRECTION_PATH`) · ใบกำพร้ายกเลิกได้ |
| เหตุผล | บังคับ ≥ 10 ตัวอักษร |
| บันทึกเพิ่มเติม | ที่ยังไม่ยกเลิกถูกยกเลิกตาม เหตุ `สัญญาแม่ CT-… ถูกยกเลิก — <เหตุผล>` · ลงนามบันทึกใต้สัญญาแม่ที่ไม่ใช่ signed ถูกกันที่ `/addenda/[id]/sign` · คำสั่งเขียนของ `/sign` (`status='awaiting_signature'`) และ `/issue` (`status='draft'`) กรองสถานะที่ด่านตรวจไว้ ⇒ การยกเลิกตามที่ลงมาระหว่างด่านกับคำสั่งเขียนไม่ถูกเขียนทับ (409 · รีวิว 25/09) |
| งานบริการ | ⭐ **หยุดตั้งแต่วันที่ยกเลิก** (เจ้าของเลือกเอง ทับคำแนะนำ "ผ่านถึงสิ้นวัน") — นัดวันนั้นที่ยังไม่ปิดงานติดด่าน ① ด้วย · นัดก่อนวันยกเลิก + นัดวันนั้นที่ **ปิดงานก่อนเวลากดยกเลิก** ผ่านตามเดิม (`contractCoverageOn` เทียบ `visitClosedAtKey` = วันเสร็จ/วันเข้า + เวลาจบ กับ `contractCancelMoment` · นาฬิกาไทยทั้งคู่) ⇒ ใบส่งงานเก่าไม่กลายเป็น "งดบริการ" ย้อนหลัง · นัดที่มาปิดหลังยกเลิก / ไม่รู้เวลาปิด = ติด (รีวิว 25/09: เดิมถามแค่ "ตอนนี้ปิดแล้วไหม" ใบส่งงานจึงพลิกตามการปิดงาน) · ด่านของการเปลี่ยนแปลง (ร่าง → ขึ้นตาราง · ใบใหม่ · โมดัลนัด) ตรวจด้วย **สถานะก่อนแก้** (`gateVisitBeforeChange`) · ถอนเครื่อง/สำรวจยังไปได้ |
| ทะเบียนต่อสัญญา | วันจบ = วันที่ยกเลิก หรือวันหมดอายุถ้ามาก่อน (`contractEndDate`) |
| ใบสั่งขาย | ไม่ถูกแก้ (ยังชี้สัญญาเดิมเป็นประวัติ · งวด/เงิน/Actual ไม่ขยับ) · audit หนึ่งแถวต่อใบที่ยังผูก (ไม่นับใบยกเลิก/ใบที่ถูก Rev. แทน) · กู้ = ออกสัญญาใหม่ → รับรอง → ผูกที่หน้าใบสั่งขาย |
| พิมพ์ซ้ำ | ฉบับตรึงเสิร์ฟพร้อมลายน้ำ "ยกเลิก" ตอนเปิด (`stampWatermark`) ไม่เขียนกลับ — ใช้กับบันทึกเพิ่มเติมด้วย |
| โมดัล | บอกผล (`signedCancelEffects`): ย้อนไม่ได้ · ใบสั่งขายที่ผูก (เลขที่) · นัดตั้งแต่วันนี้ติดด่าน · บันทึกเพิ่มเติม N ฉบับ · ทางออกฉบับใหม่ |

⚠️ **ไม่มีคอลัมน์ใหม่** — "เคยมีผล" = `status='cancelled' AND "approvedAt" IS NOT NULL` (0323 บังคับให้ทุกใบที่เคย signed
มี `approvedAt` และการยกเลิกไม่ล้าง) · ผู้ยกเลิกเก็บที่ `metadata.cancelledById/cancelledByName/cancelledFromStatus`
⚠️ ตัวโหลดสัญญาของด่าน (`gateContext.js`) กับทะเบียนต่อสัญญา (`renewals/route.js`) ต้อง select `approvedAt`/`cancelledAt`
ชุดเดียวกัน — ลืม = ด่านถือว่าไม่เคยมีผล (gateContext.test ล็อก)
🔒 ยาม: `lib/sales/contractSignedCancel.test.mjs` · `lib/service/visitGate.test.mjs` · `lib/service/renewals.test.mjs` ·
`lib/documents/stampWatermark.test.mjs`

## 5.7 แท็บ "สัญญา" ของใบสั่งขาย (มติผู้ใช้ 2026-09-15 + 2026-09-25)

*15/09: "อยากให้เข้าจาก SO แท็บสัญญาได้"* · *25/09: "เพิ่มสัญญา ในหน้า SO"* → เลือก **"ทุกใบ"**
(งานของ 15/09 ค้างไม่ได้ commit 10 วัน · ย้ายฐานขึ้น main 25/09 แล้วแก้ตามรีวิว 5 มุม)

**หน้าตา (มติ 25/09)**
- แท็บ **สัญญา** ขึ้นทุกใบ — ยกเว้นใบย้อนหลังที่ไม่อยู่เส้นบริการ (ไม่มีอะไรให้แสดง)
  · เหตุ: ของจริง 25/09 สัญญาออกแบบกลิ่น 8 ใบอยู่บนดีลที่มี SO (4 ใบเป็นสายสินค้า) แต่เปิดดูจาก SO ไม่ได้เลย
- ในแท็บ: การ์ด "สัญญาบริการของใบนี้" (ผูกสัญญา · **เฉพาะเส้นบริการ**) + การ์ด "สัญญา" ของดีล
  (`DealContractsCard` ตัวเดียวกับหน้าดีล · ทุกฉบับทุกสถานะ + รางความคืบหน้า + ปุ่ม **ออกสัญญา / เอกสารแทน**)
- ปุ่ม "ออกสัญญาจากใบนี้" บนการ์ดจัดการ **ถอดแล้ว** — ย้ายเข้าแท็บ (แท็บขึ้นทุกใบแล้ว ไม่ต้องมีทางเข้าที่สอง)
  · เงื่อนไขปุ่มเดิมทุกข้อ `canCreateContract` = แก้ใบได้ · ไม่ใช่ใบย้อนหลัง · ไม่อยู่โหมดแก้ · ใบไม่ยกเลิก/ถูกแทน
- ใบย้อนหลังไม่มีการ์ดสัญญาของดีล — ดีลของมันเป็นดีลภาชนะ ⇒ ลิสต์ทั้งดีลพาเอกสารแทนสัญญาของใบพี่น้องมาปน
- รางในตาราง: ช่องต้องมี `ui-cell-wide` ไม่งั้นเพดานเซลล์ 220px ตัดหมุดสุดท้าย (วัด: ราง 341px ในช่อง 274px)
- โหลดลิสต์พัง ⇒ StatusNotice + "ลองใหม่" · ไม่มี "0 ฉบับ / ยังไม่มีสัญญาของดีลนี้" · ปุ่มออกสัญญากดได้เมื่อลิสต์ขึ้นแล้ว
  (ว่างปลอม = ชวนออกร่างซ้ำ)
- ข้อความ "ยังไม่มีสัญญาที่ผูกได้" ชี้ "การ์ด สัญญา ด้านล่าง" เฉพาะเมื่อการ์ดนั้นมีปุ่มให้คนดูจริง (`canCreateBelow`)
  · ไม่งั้นบอกเหตุ: ดูอย่างเดียว / ใบปิดแล้ว / ใบย้อนหลัง (ลิงก์ไปการ์ด "สัญญา" บน**หน้าดีล** — โมดัลของทะเบียน
  เลือกดีลภาชนะไม่ได้ เพราะรายการดีลมาจากใบเสนอราคาที่อนุมัติ) / โหมดแก้

**เส้นทางเต็มของเอกสารแทนสัญญา (สายบริการ)**

1. ใบสั่งขาย → แท็บ **สัญญา** → การ์ด "สัญญา" ของดีล → **ออกสัญญา / เอกสารแทน**
   (โมดัล `ContractCreateModal` ตัวเดียวกับทุกทางเข้า · ดีลกับใบเสนอราคามาจากใบสั่งขาย)
   เลือกที่มา "เอกสารภายนอกใช้แทนสัญญา" + ชนิดเอกสาร → สร้างร่าง → พาไปหน้าสัญญา
2. หน้าสัญญา → การ์ด "ไฟล์ของสัญญา" → แนบไฟล์ชนิด **เอกสารที่ใช้แทนสัญญา** (`external_doc`)
   — ใบย้ายจากเลน "ค้างอยู่กับคุณ" ของเจ้าของใบ ไปเลน "รอฉันรับรอง" ของผู้อนุมัติทันที (ป้ายเมนูทั้งสองฝั่ง)
   และ AE Sup ได้กระดิ่ง `contract_approval_pending` เช้าวันทำการถัดไป
3. ผู้อนุมัติ (admin · CD · CM · AE Sup — `canApproveExternalContract`) → หน้าสัญญา → **อนุมัติเอกสารแทนสัญญา**
   → กรอกวันเริ่มมีผล/สิ้นสุด → ยืนยัน ⇒ RPC `approve_external_sales_contract` ออกเลข CT + `signed` ในทรานแซกชันเดียว
   ⇒ เจ้าของใบ + คนสร้างได้กระดิ่ง `contract_approved` (ยกเว้นคนกดเอง)
4. กลับมาที่ใบสั่งขาย → แท็บสัญญา → การ์ด "สัญญาบริการของใบนี้" → **ผูกสัญญาเข้าใบนี้**
   (มีใบที่ผูกได้ใบเดียว ระบบเลือกไว้ให้ในช่อง แต่คนยังต้องกดผูกเอง)

**ร่างที่แนบเอกสารแล้ว — สามที่ต้องพูดเรื่องเดียวกัน**
- 🐞 ของเดิมรางสั่ง "แนบเอกสาร" ค้างไว้แม้แนบแล้ว (ของจริง: เคพี อาร์ท แนบครบ 3 ก.ย. ยังไม่มีคนอนุมัติ ณ 25/09)
- **ราง** (ทะเบียน · การ์ดบน SO/ดีล — `contractListTrack` ธง `_externalDocReady`) และ **หน้ารายละเอียด**
  (`EXTERNAL_ATTACHED_STEPS` · ธง `docAttached`: การ์ดไฟล์โหลดแล้วเชื่อการ์ด · ยังไม่รู้ใช้ `_externalDocReady` จาก GET
  ของใบ — ชุดแรก `[]` ของการ์ดคือ "ยังไม่รู้" ไม่ใช่ "ไม่มีไฟล์") เดินหมุดเดียวกัน: ร่าง ✓ → "รอ AE Supervisor อนุมัติ"
  · เทสต์ล็อก **สถานะของหมุด** ไม่ใช่แค่คำ
- **เลน/ป้ายเมนู** — `externalDocReadyIds` ถาม: ผู้อนุมัติ = ทุกใบ · คนอื่น = ใบที่ตัวเองเป็นเจ้าของ/คนสร้าง
  ⇒ ร่างที่แนบแล้วหลุดเลน "ค้างอยู่กับคุณ" ของเจ้าของ · ทะเบียนถาม `anyViewer` ครั้งเดียวแล้วกรองด้วย
  `externalDocLaneIds` (กติกาเดียวกัน) ⇒ ป้ายกับรายการนับตรงกัน · ไม่มีใบ external ร่างของตัวเอง = ไม่ยิงฐาน
- เอกสารแทนสัญญาของใบสั่งขายย้อนหลังไม่อยู่ในทั้งสามที่ (งานของคิวใบสั่งขาย) — ผู้เรียกทุกตัวต้อง select `metadata`
  (🐞 รีวิว 25/09: cron ลืม `metadata` ⇒ AE Sup จะได้กระดิ่งของใบที่ `?waiting=1` ไม่แสดง)
- cron อ่านไฟล์แนบแบบ `strict` — อ่านพังเป็น error ของรอบนั้น ไม่ใช่ "ไม่มีใบรออนุมัติ" · มีใบรอแต่ไม่มีผู้รับ = error แยก

**ปุ่มอนุมัติ — ตรวจแล้ว ด่านถูก**
- โผล่เฉพาะผู้อนุมัติ (admin · CD · CM · AE Sup) บนใบ external ที่ยังเป็นร่าง และไม่ถูกล็อกเป็นของใบย้อนหลัง (`showExternalApprove`)
- ปุ่มบนการ์ดใช้ด่าน "เปิดฟอร์ม" (`externalApproveOpenError`: สิทธิ์ · ที่มา · สถานะ · ชนิดเอกสาร ·
  มีไฟล์) · ปุ่มยืนยันในโมดัลใช้ด่านเต็ม (+ วันเริ่ม/สิ้นสุด) — แยกกันตั้งแต่เดดล็อกของ #1529
- route ถามด่านตัวเดียวกัน + ไฟล์ต้องเป็นของใบนี้จริงและเป็นชนิด `external_doc`
- RPC ส่ง `approvedById`/`approvedAt` ครบตาม CHECK `sales_contracts_signed_needs_approval` (mig 0323)
- ⚠️ **ยังไม่เคยมีใบ external ไหนถึง `signed` บน production** (ตรวจ 25/09: signed external = 0 · ร่าง 3 · ยกเลิก 4)
  ⇒ การกดจริงครั้งแรกคือ UAT ของเส้นนี้ ถ้าตอบ 500 ให้ดูข้อความ error ของ RPC ก่อน

## 6. สิ่งที่ทำแล้ว (ไล่ตาม PR)

ระบบสัญญาเริ่ม 2026-08-21 · ทุกใบด้านล่าง merge เข้า main แล้ว

| PR | ทำอะไร | migration |
| --- | --- | --- |
| #1351 | ทะเบียน `/sa/contracts` + หน้ารายละเอียด · สถานะ ร่าง→รอลงนาม→ลงนามแล้ว/ยกเลิก · ออกเลข+ตรึงเนื้อใน RPC เดียว | `0278` |
| #1358 · #1359 | วางแม่แบบสัญญาออกแบบกลิ่นตามต้นฉบับ 13 ส.ค. · ถอด **ค่าตั้งต้น** ตำแหน่งผู้ลงนามฝั่งผู้ว่าจ้าง (ช่องยังอยู่ · เว้นว่าง = ไม่พิมพ์บรรทัดนั้น) | — |
| #1361 | เอกสารล้วน — บันทึกสามจุดที่ตั้งใจต่างจากต้นฉบับ | — |
| #1362 · #1363 · #1366 · #1367 · #1368 | หน้ากระดาษ: ชื่อ/accent/ช่องเซ็นชิดท้าย · ระยะช่องพยาน · ย่อหน้าคำจำกัดความ · เลิก justify แล้วใช้ `inter-character` | — |
| #1369 | **ฉบับแก้ไข (Rev.)** — ออกเลขแล้วแก้เนื้อไม่ได้ ต้องออกใบใหม่เลขฐานเดิม · ใบที่ลงนามแล้วออก Rev. ไม่ได้ | `0280` |
| #1371 | **บันทึกเพิ่มเติมสัญญา** — ตารางแยก · ออกได้เฉพาะสัญญาที่ลงนามแล้ว · อ้างคำร้องพัฒนากลิ่นที่ปิดเรื่อง · หนึ่งคำร้อง = หนึ่งบันทึก | `0282` |
| #1373 | ปุ่มสร้างสัญญาบนหัวทะเบียน + ปุ่มลบร่าง (กติกามีอยู่แล้วแต่ไม่มีจอไหนเสนอปุ่ม) | — |
| #1377 | โมดัลสร้างสัญญาถามเรียง **ลูกค้า → ชนิด → ดีล** · ไม่เลือกสามช่องนี้ล่วงหน้า (ใบเสนอราคายังเลือกใบแรกให้ · ที่มาตั้งต้น `generated`) | — |
| #1381 | สร้างสัญญาได้จากหน้าดีล/โครงการ (prop `dealIds`) | — |
| #1484 | ขั้นลงนามเป็น **โมดัล** (เดิมเป็นการ์ดท้ายหน้าที่กดแล้วจอไม่ขยับ) | — |
| #1529 · #1530 | **เอกสารภายนอกใช้แทนสัญญา** (PO/อีเมล/สัญญากระดาษเก่า) + AE Sup อนุมัติ · แล้วแก้บั๊กที่ตัวกรองแม่แบบปิดฟีเจอร์นี้เอง | `0322` |
| #1531 | ขั้น **"รอหัวหน้ารับรอง"** (`awaiting_approval`) ของสัญญาที่ระบบเจนเอง | `0323` |
| #1533 · #1535 · #1538 · #1542 · #1544 | สายบริการ: ผูกสัญญาเข้า SO · ปลดด่านนัด · ทะเบียนต่อสัญญา+กระดิ่ง · UAT เต็มเส้น · นัดถอนเครื่องเมื่อไม่ต่อ | `0324` · `0327` |
| #1570 · #1573 | ปิดรูใบ external ที่ยังเรนเดอร์เอกสารระบบได้ · เก็บงานค้างสายบริการ 5 ข้อ | — |
| (รอบนี้) | **เอกสารแทนสัญญาของใบสั่งขายย้อนหลัง** — ชนิด `signed_quotation` · สร้างจาก RPC คีย์ใบ · ล็อกระหว่างใบยังไม่อนุมัติ · ออกเลขพร้อมการอนุมัติใบ · ยกเลิกด้วย trigger (§5.5) | `0374` |
| #1596 | **เลขที่สัญญาไม่ตกบรรทัดบนหัวใบ** — คอลัมน์ป้ายกว้างเท่าที่ใช้จริง + ห้ามตัดบรรทัด | — |
| #1608 | **แม่แบบสัญญาบริการ** ตามต้นฉบับ Ver.20260701 (งวดชำระ/ตารางบริการดึงจากใบเสนอราคา) | — |
| #1626 | ผู้ลงนามฝั่งผู้ว่าจ้างเป็น "ผู้มีอำนาจ/ผู้รับมอบอำนาจ" ครบทั้งสามฉบับ | — |
| #1628 | **สัญญาเข้ากระดิ่ง** (สอง kind + ตัวทวงค้างลงนามที่ cron ประจำวัน · §5.4) · `totalWithVat` เติมจากใบเสนอราคา · รวมเกณฑ์ 14 วันไว้ที่เดียว | — |
| (รอบนี้) | **ยกเลิกสัญญาที่ลงนามแล้ว** โดยผู้อนุมัติ · งานบริการหยุดตั้งแต่วันยกเลิก · บันทึกเพิ่มเติมยกเลิกตาม · ลายน้ำตอนเสิร์ฟ (§5.6) | — |

**migration ของสายสัญญา:** `0278` ตาราง+RPC · `0280` Rev. · `0282` บันทึกเพิ่มเติม ·
`0322` เอกสารภายนอก · `0323` ขั้นรับรอง · `0324` ผูก SO ↔ สัญญาบริการ · `0327` ทะเบียนต่อสัญญา
· `0374` ชนิด `signed_quotation` + เอกสารแทนสัญญาของใบย้อนหลัง (§5.5 · **ต้องรันก่อน merge โค้ดรอบนั้น**)
— `0278` `0280` `0282` `0322` `0324` `0327` **ยืนยันบนฐานแล้ว** (ตรวจ 2026-09-06 ผ่าน REST:
คอลัมน์/ตารางที่แต่ละใบสร้างมีจริง)
`0323` **ผู้ใช้ยืนยันว่ารันแล้ว 2026-09-06** (รันซ้ำจากไฟล์ในรีโป)
⚠️ ใบนี้เป็น **CHECK ล้วน ไม่มีคอลัมน์/ตาราง/RPC ให้จับ** ⇒ ตรวจผ่าน PostgREST ไม่ได้
(อ่าน `pg_constraint` ไม่ได้) · หลักฐานตรงตัวจะมาตอนใบแรกเดินผ่านสถานะ `awaiting_approval` จริง

**ของจริงบนฐาน ณ 2026-09-06:** สัญญา 10 ใบ — ร่าง 4 · รอลงนาม 5 · ยกเลิก 1 ·
แยกตามที่มา generated 9 · external 1 · **ยังไม่มีใบไหนไปถึงสถานะลงนามแล้ว**

## 7. ของที่ยังขาด / ขั้นถัดไป

1. 🔴 **แม่แบบสัญญาจ้างผลิต** — ชนิดเดียวที่ยังไม่มีต้นฉบับ (ออกแบบกลิ่น/บริการมีครบแล้ว)
   ระหว่างนี้ออกใบได้ทางเดียวคือ "เอกสารภายนอกใช้แทนสัญญา"
2. **UAT บนใบจริงของสาย generated** ยังไม่ครบวง — ยืนยันจากฐาน: ใบที่ไกลที่สุดหยุดที่
   "รอลงนาม" (5 ใบ) ยังไม่มีใบไหนผ่าน บันทึกลงนาม → AE Sup รับรอง → ลงนามแล้ว
   (สายบริการ UAT เต็มเส้นไปแล้วที่ #1542)
3. ~~กระดิ่งยังไม่มีชนิดของสัญญาเลย~~ **ปิดแล้ว 2026-09-06** — ดู §5.4
   (สอง kind เข้ากล่องกระดิ่ง + ตัวทวงค้างลงนามที่ cron ประจำวัน)
4. **ปุ่มสลับภาษาบนหน้าพรีวิวสัญญาเป็นปุ่มตายทั้งคู่** — ปุ่ม English ปิดไว้พร้อมเหตุผล
   แต่ปุ่ม ไทย ก็ไม่มี `onclick` และไม่มีสคริปต์สลับภาษา (ใบเสนอราคามีครบทั้งคู่)
   · `CONTRACT_KIND_DOC_TITLES_EN` ประกาศไว้แล้วแต่ไม่มีใครเรียก
5. ~~ช่อง `totalWithVat` ของสัญญาบริการไม่ถูกเติมอัตโนมัติ~~ **ปิดแล้ว 2026-09-06** —
   `contractFieldDefaults` เติม `totalWithVat` (ตัวเลขดิบ) และ `totalWithVatText`
   (`amountInWords`) จาก `quotation.totalAmount` · เติมเฉพาะตอนยอดเป็นตัวเลข > 0
   ⚠️ ยอดว่าง/ศูนย์ **ไม่เติม** โดยตั้งใจ — ไม่งั้นได้ "ศูนย์บาทถ้วน" พิมพ์ลงสัญญา
6. ~~เศษที่ควรเก็บ (คำสองทางเข้า · เกณฑ์ 14 วันสามสำเนา)~~ **ปิดแล้ว 2026-09-06** —
   โมดัลสร้างสัญญาใช้ประโยคเดียว (`TEMPLATE_MISSING_NOTE`) ทั้งสองเลน · `SIGNATURE_LATE_DAYS`
   ย้ายไปอยู่ `lib/sales/contracts.js` ติดกับ `daysAwaitingSignature` แล้วทั้งการ์ดสรุป ·
   ป้ายหน้ารายละเอียด · ราง · **ตัวหนังสือบนป้าย** · และตัวทวงกระดิ่ง อ่านจากที่เดียวกัน
7. เลขที่สัญญายังไม่เข้าทะเบียน `DOCUMENT_STANDARD_KEYS` เพราะยังไม่มีรหัสแบบฟอร์มควบคุม
   (FM-xx-nn) จากฝ่ายเอกสาร

## รูปแบบเลขที่สัญญา `CT-BB-YYMMXXXX-R` (มติผู้ใช้ 2026-08-31)

| ช่อง | คือ | ตัวอย่าง |
|---|---|---|
| `BB` | ชนิดสัญญา — `SD` ออกแบบกลิ่น · `MF` จ้างผลิต · `SR` บริการ | `SR` |
| `YYMM` | **เดือนที่ออกใบ** (ไม่ใช่ตัวตัดรอบเลขรัน) | `2608` |
| `XXXX` | เลขรัน 4 หลัก **เดินยาวทั้งบริษัท ไม่ตัดรอบเดือน** | `0001` |
| `R` | เลขฉบับแก้ไข (Rev.) | `-0` · `-1` |
| `A<n>` | บันทึกเพิ่มเติมสัญญา (ต่อท้ายเลขสัญญา) | `-A1` |

ตัวอย่างเต็ม: **`CT-SR-26080001-0`** · บันทึกเพิ่มเติม **`CT-SR-26080001-0-A1`**

> 🕓 **เคยลองทรง `CT-AAAA-BB-XXXXX` ที่มีรหัสลูกค้านำหน้าแล้วถอยกลับ** (31/08 วันเดียวกัน)
> ทรงนี้สั้นกว่าและยังบอกเดือนที่ออกได้ ซึ่งทรงที่มีรหัสลูกค้าทำไม่ได้เพราะไม่มี `YYMM`
> · ถ้าจะกลับไปคุยเรื่องรหัสลูกค้าในเลขอีก ให้รู้ไว้ว่ารหัส AR จริงมี 5 ทรง และมี 4 คู่
> ที่ให้เลข 4 หลักชนกัน (`A-002`↔`AR-002` · `A-003`↔`AR-003` · `A-009`↔`AR-009` ·
> `AR-005`↔`AR-K0005`) ⇒ ต้องล้างทะเบียนลูกค้าก่อน

### 🔴 `YYMM` ในเลข ≠ ตัวตัดรอบของเลขรัน

จุดที่พลาดง่ายที่สุดของเลขชุดนี้ — **สองค่านี้มาคนละทาง**:

- `YYMM` ที่เห็นในเลข มาจาก **prefix** ซึ่งคิดจากวันที่ออกใบ
- ตัวตัดรอบของเลขรันคือ **คีย์ `month` ของ `entity_number_counters`** ซึ่งตั้งเป็น `'-'`

⇒ เลขเดินต่อข้ามเดือน: `CT-SR-26080001` → `CT-SR-26080002` → (เดือนถัดไป) `CT-SR-26090003`
ไม่ใช่ย้อนกลับเป็น `0001`
⚠️ แพตเทิร์น `'-'` ไม่ใช่ท่าที่คิดขึ้นใหม่ — `AR` กับ `FG` บนฐานใช้อยู่แล้ว
⇒ **ไม่ต้องมี migration** เพราะ RPC รับ `p_month`/`p_prefix` เป็นพารามิเตอร์

⚠️ **เพดาน 9,999 ใบตลอดอายุระบบ** (ไม่ใช่ต่อเดือนแล้ว) — เกินแล้ว RPC โยน
`contract_monthly_sequence_exhausted` · ปัจจุบันมีสัญญา 4 ใบ จึงยังห่างมาก
🪤 กิ่ง seed ของ RPC ใช้ `LIKE p_prefix || '%'` ซึ่งผูกกับเดือน ⇒ ถ้าแถวตัวนับ `(CT, '-')`
หายไป มันจะ seed จากเลขของ *เดือนปัจจุบัน* เท่านั้นแล้วนับซ้ำ · กิ่งนั้นทำงานเฉพาะตอน
แถวหาย ซึ่ง trigger ของ mig 0241 กันไว้ไม่ให้ลบอยู่แล้ว

⚠️ ใบที่ออกเลขไปแล้วยังเป็นรูปแบบเก่า (`CT-26080001-0` ไม่มีอักษรย่อ) **ห้ามแปลงย้อนหลัง**
— เลขที่ออกไปแล้วถูกอ้างบนกระดาษของลูกค้า
