# จัดทีมเอง ไม่ต้องรอแอดมิน — ออกแบบหน้าและหลัง

**สถานะ: เสร็จสมบูรณ์** — ออกแบบ 2026-08-28 · ขึ้นระบบแล้ว (mig 0310 · ทะเบียน `teams` +
หน้า `/sa/teams` `/service/teams`) · ทั้ง 3 ข้อของ §7 ถูกเคาะครบตามข้อเสนอ (แยกสองแกน ·
"ผู้ช่วย" = per-user grant `team:manage` · ย้ายทีมแล้วให้ไปตามเก็บเอง)

> 🐞 **แก้ 2026-09-04 — ฟีเจอร์นี้ใช้ไม่ได้จริงเลยตั้งแต่วันแรกจนถึงวันนั้น**
> ผู้ใช้แจ้ง *"ธุรกิจบริการ จัดทีมไม่ได้"*: กด "สร้างทีม" แล้วขึ้น toast คำว่า `forbidden` เปล่า ๆ
> เหตุคือ `proxy.js` มีสองด่านซ้อนกัน และ **`lockedOut` ตัดสินก่อน `apiWriteAllowed` เสมอ** ·
> `/api/teams` ถูกลงไว้ใน `OPEN_READ_APIS` (จึงอ่านได้ หน้าเรนเดอร์ครบ ปุ่มขึ้นตาม `canManage`)
> แต่ไม่เคยอยู่ใน `OPEN_WRITE_APIS` ⇒ POST/PATCH/DELETE ตกลง default-deny ให้ทุกคนที่ไม่ใช่
> แอดมิน ⇒ กฎ `team:manage` ที่เขียนไว้ใน `apiWriteAllowed` **เป็นโค้ดตาย**
> ⇒ วันนี้ `lockedOut` มีช่องผูกกับ cap `team:manage` โดยตรง (ทะเบียนทีม + ย้ายทีมรายคน)
> และ `proxy.test.mjs` เช็ค **สองด่านคู่กันเสมอ** สำหรับทุก role ที่ถือ cap
> · เมนู "จัดทีม" ของธุรกิจบริการก็ถูกแคบจาก `canEditService` มาเป็น `canManageTeams(u,'TS')`
> ให้ตรงกับฝาแฝดที่ `/sa/teams` (เดิม `ts_planner` เห็นเมนูแล้วเข้าไปเจอหน้าที่กดอะไรไม่ได้เลย)

> ⭐ **2026-09-06 — จัดทีมมีเจ้าของที่เดียว** (มติผู้ใช้: *"ตรงหน้าตั้งค่าผู้ใช้งาน ก็เอาทีมออกด้วย"*)
> หน้า `/users` ถอดทั้งคอลัมน์ "ทีม" และช่องเลือกทีม/ทีมหลักบนฟอร์มออก — เหลือดูแลเฉพาะ
> ตำแหน่ง · ฝ่าย · สิทธิ์เสริม · การเข้าระบบ
> **เหตุผลไม่ใช่ความสวย**: สองที่นั้นตรวจคนละทะเบียน — หน้าผู้ใช้ตรวจกับค่าคงที่ `TEAMS`
> ในโค้ด ส่วน `/sa/teams` ตรวจกับตาราง `teams` จริง (ปฏิเสธทีมที่ปิดแล้ว และโชว์จำนวน
> ดีล/เป้าที่ค้างก่อนกด) ⇒ ทางที่หลวมกว่าถูกถอดทิ้ง
> สิ่งที่ตามมาและต้องรู้:
> · `validateIdentity` ยอมให้ตำแหน่งสายทีม **ยังไม่มีทีม** ได้แล้ว — "ยังไม่ได้จัดเข้าทีม"
>   กลายเป็นสถานะปกติของบัญชีที่เพิ่งเปิด (เดิมเป็น 400)
> · `PATCH /api/users/[id]` **ไม่แตะทีมเมื่อ body ไม่ส่งมา** (`resolveTeamUpdate`) — ไม่งั้น
>   ทุกการแก้ชื่อ/เบอร์/สิทธิ์เสริมจะล้างทีมของคนนั้นทิ้งเงียบ ๆ · ยังรับค่าทีมได้อยู่
>   เป็นทางฉุกเฉินของแอดมิน/สคริปต์
> · `PATCH /api/users/[id]/team` ถอนสิทธิ์เอกสารร่วมบน Drive แล้ว — เดิมมีแต่เส้น
>   `/api/users/[id]` ที่ถอน พอเหลือทางนี้ทางเดียว ไม่ถอน = สิทธิ์บน Drive บวมทางเดียว
> · จอเตือนสองจุดคือสิ่งที่กันคนใหม่ตกหล่น: ป้าย "ยังไม่ได้จัดเข้าทีม" รายแถวในตารางผู้ใช้
>   และโมดัลหลังสร้างบัญชีที่พาไป `/sa/teams` ต่อ — ด่าน `users/teamOwnership.test.mjs` คุมไว้

> ⭐ **2026-09-06 — รื้อหน้าจอทั้งเส้น** (มติผู้ใช้หลังดูม็อก `mockups/teams-rework/`)
> · **ทะเบียนเป็นตาราง** ทรงเดียวกับทะเบียนอื่นทั้งเว็บ: แถบตัวเลข 4 ช่อง → ค้นหา (ครอบ
>   **ชื่อสมาชิก** ด้วย เพราะคำถามจริงคือ "สมชายอยู่ทีมไหน") → กรองสถานะ → เรียง →
>   ตาราง/การ์ดสลับตามจอด้วย `useResponsiveView`
> · **หน้าทีมของตัวเอง** `/sa/teams/[code]` · `/service/teams/[code]` — ข้อมูลทีม +
>   รายชื่อสมาชิก ซ้าย · แผง `TEAM CONTROL` ขวา (ทรงเดียวกับหน้าสินค้า/ลูกค้า)
> · **ฟอร์มสร้าง = ฟอร์มแก้** (`TeamFormFields`) — ถามประเภททีมก่อน (ไม่มีค่าตั้งต้น)
>   และ **โชว์รหัสที่จะได้** ก่อนกดสร้าง เพราะรหัสเปลี่ยนทีหลังไม่ได้
> · **ทีมขายยังสร้างไม่ได้จริง** — ไทล์โชว์อยู่พร้อมเหตุผล (ด่านสิทธิ์อ่านค่าคงที่ `TEAMS`
>   ในโค้ด + `check:teams` ทำ CI แดงถ้ามีทีมขายนอกค่าคงที่) · ปลดล็อกเมื่อถอดค่าคงที่แล้ว
> · **ทีมหลักเป็นคำถามจริง** — โผล่เมื่อเลือก ≥2 ทีม · ของเดิมส่ง `teams[0]` ตามลำดับที่วาด
>   ⇒ ดีล/ยอดใหม่ไปขึ้นทีมที่ไม่มีใครเลือก
> · ของที่หายไปพร้อมกัน: ปุ่มแดงที่กดแล้วเงียบ (ใช้ `disabledReason` แทน) · คลาสตาย
>   `form-error`/`ui-badge violet` · โทเคนที่ไม่มีจริง `--surface`/`--surface-2`/`--fw-regular`
>   · ป้ายหัวหน้าทีมสี `--accent` 2.75:1 (เป็น `Tag tone="accent"`) · หัวหน้าทีมที่ค้าง
>   (อ่านชื่อสดจากทะเบียนคน + เตือนเมื่อคนนั้นไม่อยู่ในทีมแล้ว) · ทีมที่ปิดแล้วเปิดกลับได้
> · **ย้ายเจ้าหน้าที่บริการข้ามทีมในการกดครั้งเดียว** — `PUT /api/teams/[code]/members`
>   เลิกตีกลับทั้งชุดเมื่อมีคนของทีมอื่นปนมา แล้วย้ายให้แทน (กติกา "คนหนึ่งอยู่ทีมเดียว
>   ต่อฝ่าย" เหมือนเดิม เปลี่ยนแค่ว่าระบบบังคับให้) · จอบอกก่อนกดว่าใครจะย้ายมาจากทีมไหน
>   ⚠️ **ไม่มี transaction** (PostgREST) ⇒ ลำดับคือ ล้างทีมนี้ → เพิ่มทั้งชุด → **ถอนออกจาก
>   ทีมเดิมเป็นขั้นสุดท้าย** · เลือกอาการ "ค้างอยู่สองทีม" (เห็นได้จากทั้งสองหน้า กดบันทึก
>   ซ้ำแล้วหาย) แทน "ไม่มีทีมเลย" (หายจากทุกคิวโดยไม่มีใครสังเกต) · ล้มขั้นนี้ = ส่ง `stuck`
>   กลับไปให้จอบอกผู้ใช้ ไม่ใช่เงียบ · ตรรกะล้วนอยู่ที่ `planCrewRoster` และมีเทสต์คุม
> · **ยังไม่ทำ**: จัดกลุ่มแถวในทะเบียน (`bucketList`) — ทีมยังน้อยเกินกว่าจะคุ้ม

> ⭐ **2026-09-07 — สร้างทีมขายใหม่ได้แล้ว** (มติผู้ใช้: *"ปลดล็อกทีมขายใหม่ ทำเลย"*)
> **ข้อสมมติที่พังตอนสำรวจ**: เอกสารกับคอมเมนต์หลายที่เขียนว่าปลดล็อกไม่ได้เพราะ "ด่านสิทธิ์
> อ่านทีมแบบ sync ตอน render จึงอ่านฐานไม่ได้ (ADR 0015)" · ของจริงคือ **ADR 0015 ไม่ได้ห้าม
> อ่านฐาน** มันห้าม *ย้ายที่เก็บสมาชิก* ออกจาก `app_metadata` · และด่านที่วิ่งตอน render
> (`inScope` · `hasTeam` · `attributionTeam` · `teamScope`) อ่านแค่ `teams[]` **ของคนที่ล็อกอิน**
> จึงรับรหัสใหม่ได้อยู่แล้วโดยไม่ต้องแก้อะไรเลย
> ค่าคงที่ `TEAMS` ถูกอ่านจริงแค่ **2 จุด** ทั้งคู่เป็นทางเขียนฝั่งเซิร์ฟเวอร์ ⇒ ฉีดรายชื่อทีมสด
> เข้าไปพอ ไม่ต้องรื้อเป็น async ทั้งระบบ
>
> สิ่งที่แก้: `loadSalesTeamCodes()` (ทะเบียนสด กรองด้วย `kind='sales'` ไม่ใช่ฝ่าย) ·
> `resolveTeamAssignment`/`validateIdentity` รับ `validCodes` · ทางเขียน 6 เส้นเทียบทะเบียนสด
> (สร้างบัญชี · แก้บัญชี · ย้ายทีม · ยกลูกค้าให้ทีม · คัดกรองลีด · กรองรายงานภาษี)
>
> 🔴 **`resolveTeamAssignment` คืน `dropped` แล้ว และผู้เรียกต้องตีกลับให้ดัง** — ของเดิม
> `.filter()` ทิ้งรหัสที่ไม่รู้จักเงียบ ๆ ⇒ บัญชีถูกสร้าง/แก้สำเร็จโดยไม่มีทีม ตอบ 201/200
> เหมือนสำเร็จ แล้วเจ้าตัวเปิดระบบมาเจอจอว่างทุกหน้าโดยไม่มี error
>
> **`TEAMS` เปลี่ยนความหมาย**: จาก "ทีมขายทั้งหมด" เป็น **"สามทีมที่ seed มาแต่แรก"** —
> เหลือสองหน้าที่คือค่าถอยของฝั่งจอ และรายการที่ `DELETE /api/teams/[code]` ห้ามลบ
>
> **`check:teams` เปลี่ยนหน้าที่**: เดิมบังคับ "ทะเบียน = ค่าคงที่" สองทาง (สร้างทีมใหม่ = แดง)
> แต่ตรวจจริงด้วย fixture แล้วพบว่า **ลบ KA/ODM/SV ทิ้งก็ยังผ่าน** — ของที่มีค่าจริงไม่ได้ถูกคุ้ม
> เลย · ตอนนี้: ทีมตั้งต้นต้องไม่หาย/ไม่ถูกปิด · ป้ายของทีมที่โค้ดรู้จักต้องตรง · ทีมใหม่ที่ยังไม่มี
> ป้าย = **เตือน ไม่ตก** · ลำดับเทียบเฉพาะสามทีมตั้งต้น
> 🔴 แต่ด่านนี้ **ยังถูกข้ามใน CI จริง** เพราะยังไม่ได้ตั้ง SUPABASE secret (`ci.yml:122`)
>
> **ยังเหลือ**: ทีมขายใหม่จะโชว์เป็น *รหัส* ในจอที่อ่าน `TEAM_LABELS` (ทุกจุดใช้
> `TEAM_LABELS[t] || t` จึงไม่พัง) และไม่ขึ้นในชิปกรองทีมที่สร้างจากค่าคงที่ ⇒ ถ้าจะใช้จริง
> ให้เติมป้ายในโค้ด หรือทำทะเบียนฝั่งจอ (ดูข้อเสนอ `cachedFetchJson('/api/teams')`)
> ⭐ **2026-09-07 (รอบสอง) — ทะเบียนทีมฝั่งจอ + ปิดรูที่การโยกทำหลุด**
> **ตรวจย้อนสามใบที่ปล่อยติดกัน (#1629 · #1633 · #1641) แล้วเจอของพังสามอย่าง:**
> · 🔴 **ปุ่ม "จัดเข้าทีม" ในถัง "ยังไม่อยู่ทีมไหน" หายไปตอนรื้อหน้าทะเบียน** ⇒ ตั้งแต่หน้าผู้ใช้
>   ถอดช่องทีมออก บัญชีขายเกิดมาไม่มีทีมเสมอ และ **ไม่มีทางไหนในระบบเลย**ที่จะให้ทีมแรก
>   กับเขาได้ · โมดัลหลังสร้างบัญชีก็พามาหน้านี้พร้อมบอกว่าจัดต่อได้ที่นี่
>   ⇒ ยกโมดัลเป็น `TeamAssignModal` ตัวเดียว ใช้ทั้ง "จัดเข้าทีมครั้งแรก" และ "ย้ายทีม"
> · 🔴 **ปิดทีมขายที่ยังมีคนอยู่ได้เงียบ ๆ** — ด่านนับสมาชิกจาก `team_members` ซึ่งเป็นตาราง
>   ของทีม *ปฏิบัติงาน* ⇒ ทีมขายนับได้ 0 เสมอ · แก้ให้นับจาก Auth (`loadTeamHolderIds`
>   ตัวเดียวกับที่ DELETE ใช้อยู่แล้ว)
> · 🔴 **คนที่ทีมเดิมถูกปิด แก้บัญชีไม่ได้เลยตลอดกาล** — ฟอร์มแก้ส่ง `role` เสมอแต่ไม่ส่งทีม
>   ⇒ ค่าเดิมถูกยกมาเทียบกับทีมที่ยัง active เท่านั้น แล้ว 400 ทุกครั้ง (เปลี่ยนชื่อ · รีเซ็ตรหัส
>   · เปลี่ยนตำแหน่ง) ⇒ "ไม่ได้ส่งทีมมา" ยอมรับทีมที่ปิดแล้วด้วย · "ส่งมาเอง" ยังบังคับว่าต้องเปิดอยู่
>
> **ทะเบียนฝั่งจอ**: `GET /api/teams?labels=1` คืนเฉพาะ `code/name/kind/isActive/sortOrder`
> และ **คืนก่อนด่านฝ่าย** ⇒ ทุกคนที่ล็อกอินอ่านได้ (ด่านฝ่ายเดิมปกป้อง `members`/`people`
> ไม่ได้ปกป้องชื่อทีม) · ฝั่งจอใช้ `lib/master/salesTeamRegistry.js` — `cachedFetchJson` +
> ค่าสำรองจากค่าคงที่ ทรงเดียวกับ `companyProfile`
> ⚠️ **ห้ามคืน `[]` ระหว่างโหลด** — ชิปตัวกรองที่ว่างหนึ่งเฟรมทำให้ค่าที่ผู้ใช้เลือกไว้ถูกล้าง
>
> แปลงแล้ว: `MyTeamsFilter` · `TeamPickerField` · ตัวเลือกทีมของหน้าลูกค้า + `CustomerForm`
> ✅ **แปลงหน้าวางเป้าแล้ว (2026-09-07 รอบสาม)** — `targets/page` · `targets/plan` · `targets/history`
> อ่านทะเบียนทั้งหมด · **สองลิสต์คนละหน้าที่**: `activeCodes` (แบ่งเป้าปีหน้า) กับ `historyCodes`
> (ยอดจริงย้อนหลัง รวมทีมที่ปิดแล้ว) — สลับกันเมื่อไรจะได้ยอดรวมที่ขาดไปเงียบ ๆ หรือทีมที่ปิดแล้ว
> ได้เป้าปีหน้า ทั้งคู่ไม่มี error ให้เห็น
> 🐞 **ตัวเลขผีที่เจอระหว่างทาง**: ทั้งสามจอวนตามลิสต์คงที่ ⇒ แถวของทีมนอกลิสต์ถูกทิ้งเงียบ
> (ไม่มีแถว แก้ไม่ได้ เกลี่ยออกไม่ได้ **แต่ยังอยู่ในฐานและยังถูกทับบนแท็บผลงาน**) — อาการเดียวกับ
> ที่ `historyEntry` เขียนไว้เองว่าสร้างมาเพื่อกัน แต่กันได้เฉพาะ *คน* ที่หลุดทีม ไม่ได้กัน *ทีม*
> ⇒ ทุกจอกวาดรหัสทีมจากแถวข้อมูลเข้ามาด้วยเสมอ
> 🔴 **กับดักที่ต้องไม่พลาด**: ห้ามให้ลิสต์ทีมหลุดเข้า deps ของ `load` ในหน้าแผน —
> `useEffect(() => load(), [load])` + `useRevalidateOnFocus(load)` ผูกกับ identity ของ `load`
> ⇒ อาเรย์ที่สร้างใหม่ทุกเรนเดอร์จะยิง `/api/sales-planning/history` (กวาด `sales_deals` ทั้งตาราง)
> วนไม่รู้จบ · การเติม `teamHist` จึงขับด้วย **ข้อมูล** ไม่ใช่ลิสต์ และมีเทสต์ล็อก deps ไว้
>
> ✅ **ป้ายฝั่งเซิร์ฟเวอร์เสร็จแล้ว (2026-09-07 รอบสี่)** — `loadTeamNames(supabase)` (teamsRepo)
> คืน Map รหัส→ชื่อ **รวมทีมที่ปิดแล้ว และไม่กรอง `kind`** เพราะทุกจุดที่ใช้เป็นการแปลรหัส
> *ของเก่า* · กรองอะไรออก = แถวนั้นได้รหัสดิบกลับไปเงียบ ๆ
> 🔴 **ห้ามใช้แมปนี้เป็นด่านของทางเขียน** — กว้างกว่า `loadSalesTeamCodes` โดยตั้งใจ
>
> 🔴 **ตัดสินแล้ว: ไม่รู้จัก = รหัสดิบ ห้ามถอยไป `TEAM_LABELS`** — แมปมาจากฐานสด ถ้ามีรหัสนั้น
> ก็คือชื่อจริง · ถอยไปค่าคงที่มีผลเฉพาะตอนอ่านฐานพลาด ซึ่งตอนนั้น **รหัสดิบคือความจริง
> ส่วนชื่อเก่าคือคำโกหกที่ดูเหมือนปกติ**: ทีมที่ถูกเปลี่ยนชื่อในทะเบียนจะพิมพ์ชื่อเก่าลงไฟล์
> Excel ตลอดไปโดยไม่มีอะไรบอกว่าเพี้ยน
>
> 🐞 **เทสต์ที่ตรึงป้ายไว้ 3 ตัวเป็นชนิดที่หลอกตัวเอง** — ทั้งสามไม่ส่งแมปแล้ว assert ค่าที่
> ค่าคงที่ให้พอดี ⇒ ต่อให้ประกาศพารามิเตอร์แล้วไม่เคยอ่าน หรือ workbook ลืมส่งต่อ เทสต์ก็ยัง
> เขียว · แก้ให้ assert ค่าที่ **ต่างจากค่าคงที่** และเพิ่มเคสทีมที่สร้างใหม่ (ค่าคงที่ตอบไม่ได้เลย)
>
> ⚠️ **แจ้งเตือนลีดโหลดแมปใน `after()` ไม่ใช่ก่อนหน้า** — ฟังก์ชันนั้นถูกเรียกหลัง DB เขียนสำเร็จ
> และห้ามเพิ่ม latency · โหลดเฉพาะตอน `action === 'screen'` (ขั้นเดียวที่ข้อความมีชื่อทีม)
> · หัวเรื่องถูกเขียนลงตาราง `notifications` **ถาวร** ⇒ ทีมที่ยังไม่มีชื่อในแมปจะค้างเป็นรหัส
> ตลอดไปแม้เติมชื่อทีหลัง

> ⭐ **2026-09-07 (รอบห้า) — ชื่อทีมเหลือแหล่งเดียวจริง ๆ · ลบ `TEAM_LABELS` ทิ้ง**
> เดิมมีสองแหล่ง: ทะเบียน + สำเนาชื่อสามทีมในโค้ด · และ **สองฝั่งใช้กติกาถอยตรงข้ามกัน** —
> เซิร์ฟเวอร์คืนรหัสดิบ ส่วนจอถอยไปค่าคงที่ ⇒ หลังเปลี่ยนชื่อทีมแล้วโหลดทะเบียนพลาด
> เบราว์เซอร์โชว์ **ชื่อเก่าอย่างมั่นใจ** ส่วน Excel โชว์ **รหัสดิบ** — ไม่มีอันไหนจริง
> ⇒ กติกาเดียวทั้งระบบ: **มีในทะเบียน = ชื่อจริง · ไม่มี = รหัสดิบ**
> · ค่าสำรองเฟรมแรกเหลือ **รหัสล้วน ไม่ใช่ชื่อ** (ชิปตัวกรองที่ว่างหนึ่งเฟรมทำให้ค่าที่ผู้ใช้
>   เลือกไว้ถูกล้าง จึงยังต้องมีรายการ แต่ไม่ต้องมีชื่อ)
> · `AppLayout` เป็นเจ้าของการโหลด (`useSalesTeams()` ตัวเดียว) แล้วโมดูลที่เรียก hook ไม่ได้
>   (`personSearchText` · `leadLifecycle` · ตัวประกอบข้อความ) อ่านสแนปช็อตผ่าน `teamLabelNow()`
>   🔴 ถอด hook ตัวนั้นออกจากเปลือกเมื่อไร ทุกจอที่ใช้ `teamLabelNow` จะค้างที่รหัส
> · กฎ ② ของ `check:teams` (ชื่อในทะเบียน = ชื่อในโค้ด) **ตายไปพร้อมค่าคงที่** — กฎข้อนั้นเอง
>   คือสิ่งที่ทำให้ "เปลี่ยนชื่อทีม" เป็นเหตุการณ์ที่ทำ CI แดง
>
> 🐞 **เปลี่ยนชื่อแล้วแท็บที่เปิดค้างไม่ตาม** — TTL ของแคชฝั่งจอเป็นแค่ *พื้น* ไม่ใช่เพดาน:
> แคชอยู่ระดับโมดูล มีชีวิตเท่าแท็บ ล้างที่เดียวคือตอนล็อกเอาต์ ⇒ ต้อง unmount แล้ว mount ใหม่
> ถึงจะยิงซ้ำ · แก้สองทาง: `invalidateSalesTeams()` ตอนบันทึกทะเบียนสำเร็จ + revalidate ตอน
> กลับมาที่แท็บ
> 🔴 **และ revalidate อย่างเดียวไม่พอ** (จับได้ตอน UAT) — `cachedFetchJson` เห็นว่ายังไม่ครบ TTL
> แล้วคืนของเดิมทันที ⇒ ตัว revalidate กลายเป็นของประดับ · ต้อง **ข้าม TTL** ตอน focus
> · พิสูจน์บนของจริง: เปลี่ยนชื่อ ODM จากอีกทาง แล้วแท็บที่เปิดค้างอยู่เห็นชื่อใหม่หลัง focus
>   โดยไม่ต้องรีโหลด แล้วเปลี่ยนกลับเรียบร้อย


- โจทย์จากผู้ใช้ 2026-08-28: *"อยากย้ายการจัดทีมออกมาเป็นของแต่ละระบบแทน ให้ Supervisor
  หรือหัวหน้า หรือผู้ช่วยตั้งเองได้ไม่ต้องรอแอดมิน และแยกเฉพาะฝ่ายได้ด้วย"*
  หน้าใหม่ต้อง **ดูรายชื่อ + ย้ายคนข้ามทีม · สร้าง/ปิดทีมเอง · ตั้งหัวหน้าทีม**
- ตรวจของจริงก่อนออกแบบ: 6 มุม (โค้ด · ฐานข้อมูล · เอกสารมติ) 2026-08-28

---

## 1. ของจริงวันนี้ — สามข้อที่เปลี่ยนรูปงานทั้งหมด

| # | ข้อเท็จจริง | ที่มา |
|---|---|---|
| 1 | **"ทีม" ไม่ใช่ข้อมูล** เป็นค่าคงที่ 3 ตัวในโค้ด และไม่มีตาราง `teams` ในฐานเลย | `permissions.js:162` · ไม่มี migration ไหนสร้างตารางนี้ |
| 2 | **สังกัดของคนอยู่ใน Supabase Auth `app_metadata`** ไม่ใช่ตารางแอป — เขียนได้เฉพาะ service role ผ่าน `/api/users` ซึ่งกั้นด้วย `users:manage` = **role `admin` เท่านั้น** และ cap นี้ **จงใจ grant ไม่ได้** | `proxy.js:319` · `permissions.js:302-320,471-478` |
| 3 | **`TS` เป็นฝ่าย ไม่ใช่ทีม โดยเจตนา** — ทีมมีได้เฉพาะ role ฝ่ายขาย ถ้าจับช่างเป็นทีมต้องให้ role `ae` แล้วช่างจะได้ cap ขายมาทั้งชุด | มติ 2026-07-31 · `permissions.js:124-129,184` |

รหัสทีมยังถูกก๊อปเป็น **ข้อความเปล่า** ลง 20 คอลัมน์ใน 19 ตาราง + อยู่ในกุญแจของ unique index
3 ตัว + ~~`CHECK (teamKey IN ('ODM','KA','SV'))` ที่ mig 0128~~ (**ถูก mig 0149 ถอดคอลัมน์ทิ้งไปแล้ว** — ดู §6 ข้อ 4) + มีรายการทีมซ้ำอีก 4 ชุด
ในโค้ด (`SALES_TEAMS` · `TEAM_ORDER` · `TEAM_LABELS` ที่ก๊อปมือไว้ในหน้าวางเป้า)
ซึ่ง**เรียงลำดับไม่ตรงกับตัวแม่ด้วยซ้ำ**

⇒ "สร้างทีมเองได้" ไม่ใช่การเพิ่มหน้าจอ แต่คือการ **ยกทีมขึ้นเป็นทะเบียน** ก่อน

---

## 2. แยกสองแกนที่วันนี้ปนกันอยู่ในคำเดียว

> **ทีมขาย ≠ ทีมปฏิบัติงาน** — นี่คือข้อเสนอหลักของแผนนี้

| | **ทีมขาย** (ของเดิม) | **ทีมปฏิบัติงาน** (ของใหม่) |
|---|---|---|
| ตอบคำถาม | ยอดนี้เป็นของทีมไหน · ใครเห็น/แก้แถวนี้ได้ | ใครทำงานกลุ่มไหน · วันนี้ใครคู่กับใคร |
| ผูกกับ | `app_metadata.team/teams` → ขอบเขตสิทธิ์ | สมาชิกในตาราง ไม่แตะสิทธิ์เลย |
| ใครมี | เฉพาะ role ฝ่ายขาย (ODM/KA/SV) | ฝ่ายไหนก็มีได้ (TS · PD · WH …) |
| ปิดทีม | กระทบยอดย้อนหลัง (ต้องเก็บรหัสไว้อ่าน) | ไม่กระทบข้อมูลเก่า |

**ทำไมต้องแยก** — ถ้าจับช่าง TS เข้า `app_metadata.teams` เพื่อให้ "มีทีม" ช่างต้องถือ
role `ae` (กติกา `TEAM_ROLES`) แล้วจะได้สิทธิ์เห็นดีล/ใบเสนอราคา/มูลค่าทั้งทีมขายติดมาด้วย
ซึ่งขัดมติ 2026-07-31 ตรง ๆ · การแยกแกนทำให้ TS มีทีมได้โดยไม่ต้องแตะระบบสิทธิ์เลยสักบรรทัด

---

## 3. หลังบ้าน

### 3.1 ตารางใหม่สองใบ

```sql
-- ทะเบียนทีม (แทนค่าคงที่ TEAMS)
create table public.teams (
  code        text primary key,          -- 'ODM' · 'KA' · 'SV' · 'TS-A' …
  name        text not null,             -- ป้ายที่คนอ่าน
  department  text not null,             -- เจ้าของทีม: SA · TS · PD …
  kind        text not null,             -- 'sales' | 'crew'   ← แกนที่ §2 แยกไว้
  "leadId"    text, "leadName" text,     -- หัวหน้าทีม (ยังไม่มีที่ไหนในระบบวันนี้)
  "isActive"  boolean not null default true,
  "sortOrder" integer not null default 100,
  note        text
);

-- สมาชิกทีมปฏิบัติงาน (kind='crew' เท่านั้น — ทีมขายยังอ่านจาก app_metadata)
create table public.team_members (
  "teamCode" text not null references public.teams(code) on delete restrict,
  "userId"   text not null,
  "userName" text,
  "joinedAt" date,
  primary key ("teamCode", "userId")
);
```

⚠️ **ทีมขายไม่ย้ายที่เก็บ** — `app_metadata.team/teams` ยังเป็น source of truth ของขอบเขต
สิทธิ์เหมือนเดิม เพราะทุกด่านสิทธิ์อ่านมันแบบ sync ตอน render · ย้ายไปตารางแปลว่าทุกการเช็ค
สิทธิ์ต้องยิง DB (ADR 0015 ทั้งฉบับตั้งอยู่บนของเดิม) ⇒ ตาราง `teams` ทำหน้าที่
**ทะเบียนชื่อ/ป้าย/หัวหน้า/ลำดับ** ส่วนการเป็นสมาชิกของสายขายยังอยู่ที่บัญชีผู้ใช้

### 3.2 สิทธิ์ — cap ใหม่ที่แคบ ไม่ใช่แจก `users:manage`

`users:manage` เปิดสร้าง/ลบบัญชี/รีเซ็ตรหัสผ่านมาด้วยทั้งชุด และถูกกันไม่ให้ grant ไว้
ด้วยเหตุผล SECURITY ที่เขียนกำกับ ⇒ **ห้ามแตะ** · ของใหม่คือ

```js
// permissions.js
'team:manage'   // จัดทีมของฝ่ายตัวเอง — ไม่ใช่จัดการบัญชี
```

- ถือโดย: `ae_supervisor` (ทีมขาย) + คนที่ได้ **per-user grant** (`GRANTABLE_CAPS`)
  ⇒ "ผู้ช่วย" ที่ผู้ใช้พูดถึง = คนที่หัวหน้าขอให้แอดมินติ๊ก grant ให้ครั้งเดียว จบ
- **ขอบเขต = ฝ่ายของตัวเอง** — `canManageTeams(user, department)` คืน true เมื่อ
  `departmentOf(user) === department` (admin ข้ามได้)
- สิ่งที่ cap นี้ **ทำไม่ได้**: สร้าง/ลบบัญชี · เปลี่ยน role · เปลี่ยนฝ่าย · รีเซ็ตรหัส ·
  แก้ `extraCaps` — สี่อย่างนี้ยังเป็นของแอดมินเหมือนเดิม

### 3.3 API

| เส้น | ทำอะไร | ด่าน |
|---|---|---|
| `GET /api/teams?department=TS` | ทะเบียนทีม + จำนวนสมาชิก | `team:manage` หรือ `users:view` |
| `POST /api/teams` | สร้างทีม (code ออกจากชื่อ + ฝ่าย) | `team:manage` ของฝ่ายนั้น |
| `PATCH /api/teams/[code]` | เปลี่ยนชื่อ · หัวหน้า · ลำดับ · ปิดใช้งาน | เดียวกัน |
| `PUT /api/teams/[code]/members` | ย้ายคนเข้า/ออก **ทีมปฏิบัติงาน** | เดียวกัน |
| `PATCH /api/users/[id]/team` | ย้าย **ทีมขาย** (เขียน app_metadata) | `team:manage` + ฝ่ายต้องตรง + role ปลายทางต้องเป็น `TEAM_ROLES` |

⚠️ เส้นสุดท้ายเป็นเส้น **ใหม่ที่แคบ** ไม่ใช่เปิด `/api/users/[id]` เดิมให้กว้างขึ้น —
เส้นเดิมเขียน role/department/extraCaps ได้ด้วย ซึ่งไม่ควรอยู่ในมือหัวหน้าทีม

### 3.4 ปิดทีม ≠ ลบทีม

รหัสทีมถูกก๊อปเป็นข้อความลง 19 ตาราง และอยู่ในกุญแจ unique index ⇒ **ลบไม่ได้**
`isActive=false` แปลว่า: ไม่ขึ้นในดรอปดาวน์ให้เลือกใหม่ · ยังอ่านป้ายได้ทุกที่ที่อ้างถึง ·
ยังกรองรายงานย้อนหลังได้ · ปิดไม่ได้ถ้ายังมีสมาชิกหรือมีงานเปิดค้าง (บอกจำนวนตอนกด)

### 3.5 ย้ายทีมแล้วอะไรค้าง — ต้องบอก ไม่ใช่เงียบ

คู่มือปัจจุบันบอกว่าย้ายทีมมี **4 ขั้น** และ 3 ขั้นเป็นงานมือ: แก้บัญชี → แก้ทีมของดีล
ที่ยังเปิดทีละใบ → ย้ายเป้าเดือนอนาคต → กดถอนสิทธิ์เอกสารร่วม
⇒ ปุ่ม "ย้ายทีม" ที่แก้แค่ `app_metadata` จะทิ้งดีล/เป้า/สิทธิ์ไฟล์ค้างที่ทีมเดิมโดยไม่มีอะไรฟ้อง

**ดีไซน์**: กดย้ายแล้วขึ้นโมดัลสรุปผลข้างเคียงก่อนยืนยัน (แพตเทิร์น `approvalPrompt`)

> ย้าย ต้า จาก KA → SV
> · ดีลที่ยังเปิดอยู่ **7 ใบ** ยังนับเป็นของ KA — ไปแก้ทีละใบที่หน้าดีล
> · เป้าเดือน ก.ย.–ธ.ค. **4 เดือน** ยังอยู่ใต้ KA
> · เอกสารร่วมที่แชร์ไว้ **12 ไฟล์** ยังเปิดได้ — กดถอนสิทธิ์ที่หน้าผู้ใช้
> [ ] รับทราบว่าต้องตามเก็บสี่อย่างนี้เอง

⚠️ ดีลเดือนเก่ารายงานใต้ทีมเดิม **ถูกต้องแล้ว** ไม่ต้องย้อนแก้

---

## 4. หน้าบ้าน

### 4.1 อยู่บ้านไหน

ตามกฎ module-ownership (เมนู = งานที่ฝ่ายนั้นทำ · ตัดที่ `visible` ไม่ใช่ที่ `cap`):

| ฝ่าย | เส้นทาง | เมนู |
|---|---|---|
| ขาย | `/sa/teams` | ใต้ระบบบริหารงานขาย |
| บริการ | `/service/teams` | ใต้ธุรกิจบริการ |

**หน้าเดียวกัน component เดียวกัน** ต่างกันแค่ `department` ที่ส่งเข้าไป — ไม่ใช่สองไฟล์
ที่จะเพี้ยนหากันภายในสองเดือน (กฎ AGENTS.md ข้อแรกของ repo) · แอดมินยังเห็นทุกฝ่าย
ผ่าน `/settings/teams` ที่ใช้ component เดิมอีกทีโดยไม่ล็อกฝ่าย

### 4.2 โครงหน้า

```
จัดทีม · ฝ่ายบริการ                                   [+ สร้างทีม]
────────────────────────────────────────────────────────────────
[การ์ดทีม]  ทีม A — เขตกรุงเทพตะวันออก        หัวหน้า: ต้า
            4 คน · BKK-E, BKK-N
            ┌ สมาชิก ────────────────────────────────────┐
            │ ต้า (หัวหน้า)   · เข้าทีมเมื่อ 1 ส.ค. 2026  │
            │ เบส            · …                    [ย้าย]│
            └───────────────────────────────────────────┘
[การ์ดทีม]  ทีม B — ปริมณฑล                    หัวหน้า: —  ⚠ ยังไม่ตั้งหัวหน้า
            2 คน

[แถบล่าง]  ยังไม่อยู่ทีมไหน 3 คน   ← ถังนี้ต้องมีเสมอ
```

**กติกาที่ยึด**
- ทีมเป็น **การ์ด** ไม่ใช่แถวตาราง — สมาชิกอ่านเป็นกลุ่ม ไม่ใช่รายการยาว
- **ถัง "ยังไม่อยู่ทีมไหน" ต้องมีเสมอ** แม้ว่าง — ถังที่หายไปคือคนที่หายไปจากสายตา
  (โรคเดียวกับแดชบอร์ดที่เคยตัดถัง null ทิ้งจนยอดรวมไม่ตรง)
- ย้ายคน = ปุ่ม "ย้าย" บนแถวสมาชิก แล้วเลือกทีมปลายทางเป็น **ไทล์** (ตัวเลือกน้อย)
  ⚠️ **ไม่ใช่ลากวาง** — ลากวางบนมือถือทำไม่ได้ และการย้ายคนควรมีจังหวะยืนยัน
- ตั้งหัวหน้า = เลือกจากสมาชิกในทีมนั้นเท่านั้น (หัวหน้าที่ไม่ได้อยู่ในทีมคือข้อมูลที่ผิด)
- ทีมที่ปิดแล้วยุบเป็นแถวเดียวใต้สุด กดขยายดูได้ ไม่หายไปจากจอ

### 4.3 สิ่งที่หน้านี้ **ไม่มี** โดยตั้งใจ

- ❌ ปุ่มสร้าง/ลบบัญชีผู้ใช้ · เปลี่ยน role · รีเซ็ตรหัสผ่าน — ยังเป็นของแอดมินที่ `/users`
- ❌ ตัวสลับ "ทีมที่กำลังทำงานอยู่" — เคยตัดสินว่าตกไปแล้ว (ADR 0015:92) เพราะลืมสลับ
  แล้วยอดเข้าผิดทีมเงียบ ๆ
- ❌ การให้สิทธิ์ตามการเป็นสมาชิกทีมปฏิบัติงาน — ทีม crew **ไม่แตะสิทธิ์เลย**
  ถ้าวันหนึ่งอยากให้แตะ ต้องเป็นมติใหม่ ไม่ใช่ผลข้างเคียง

---

## 5. ลำดับงาน

| งวด | ได้อะไร | migration |
|---|---|---|
| T-1 | ตาราง `teams` + backfill 3 ทีมเดิม + API อ่าน + `team:manage` | ✅ |
| T-2 | หน้าจัดทีมของฝ่ายขาย (`/sa/teams`) — ดู/ย้าย/ตั้งหัวหน้า | — |
| T-3 | ทีมปฏิบัติงาน (`team_members`) + หน้าของ TS (`/service/teams`) | ✅ |
| T-4 | ผูกทีมช่างเข้ากับการจัดคิว (กรองกริดตามทีม · ภาระรายทีม) | — |
| T-5 | ย้ายจุดที่อ่าน `TEAMS` const มาอ่านทะเบียน (44 ไฟล์ · ทำทีละกอง) | — |

⚠️ T-5 เป็นงวดที่ยาวที่สุดและ **ไม่ควรทำก่อน** T-1…T-4 — ระหว่างนั้น `TEAMS` const ยังอยู่
และทะเบียนต้องมีค่าตรงกันเป๊ะ (มีเทสต์ยามเทียบสองฝั่ง)

---

## 6. กับดักที่ต้องเขียนเทสต์ดัก

1. เทียบทีมด้วย `===` แทนการตัดกันของชุด — เคยพลาดมาแล้วสองรอบ
2. `null` เทียบ `null` แล้วผ่าน ⇒ ใบไร้ทีมกลายเป็นใบสาธารณะ
3. ปิดทีมแล้วป้ายในรายงานย้อนหลังกลายเป็นรหัสดิบ
4. ~~เพิ่มทีมใหม่แล้วลืม `CHECK` ของ `commercial_presets`~~ — **ไม่ใช่กับดักจริง**: `mig 0149` ถอดคอลัมน์ `teamKey` ทิ้งไปแล้วตอนรื้อคลังเงื่อนไขการค้าเป็น "คลังตามชนิด" (ตรวจตอนรัน mig 0311 แล้วล้มที่ 42703 · ข้อมูลเดิมมาจากการอ่าน mig 0128 โดยไม่ได้ไล่ต่อ)
5. `sortOrder` ในทะเบียนไม่ตรงกับ `TEAM_ORDER` ในโค้ด ⇒ รายงานเรียงคนละแบบกับหน้าจัดทีม
6. หัวหน้าทีมถูกย้ายออกจากทีมแล้วยังค้างเป็นหัวหน้า

---

## 7. สามข้อที่ต้องให้ผู้ใช้เคาะก่อนเริ่ม

1. **แยกสองแกนตาม §2 ไหม** — ทีมขาย (ผูกสิทธิ์/ยอด) กับทีมปฏิบัติงาน (จัดคน) เป็นคนละของ
   · ถ้าไม่แยก ต้องยอมรับว่าช่างจะได้ cap ขายติดมา ซึ่งขัดมติ 2026-07-31
2. **"ผู้ช่วย" คือใคร** — ข้อเสนอคือ per-user grant `team:manage` ที่แอดมินติ๊กให้ครั้งเดียว
   (ไม่มีแนวคิด "ผู้ช่วย" เป็น role ในระบบวันนี้)
3. **ย้ายทีมแล้วดีล/เป้าที่ค้าง** — ข้อเสนอคือ *บอกให้ครบแล้วให้คนไปตามเก็บเอง*
   · ทางเลือกอื่นคือย้ายให้อัตโนมัติ ซึ่งจะเขียนทับเจ้าของงานหลายสิบใบในคลิกเดียว
