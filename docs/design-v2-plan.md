# แผนดีไซน์ v2 "โต๊ะช่าง" — รื้อภาษาภาพทั้งระบบ

สถานะ: กำลังดำเนินการ (Phase 0) · ตรวจล่าสุด 2026-08-08

## มติที่ตัดสินแล้ว

- **ทิศทาง: "โต๊ะช่าง"** — ทึบ 100% เงาเหลือเฉพาะแผงลอย เส้น 1px เป็นตัวแบ่งชั้น
  accent ส้ม `#d97b4f` ปุ่ม filled เดียวต่อบริบท (มติผู้ใช้ 2026-08-08 —
  เลือกจาก mock 5 ทิศทาง: artifact `52191a1d-532a-49e2-a18d-f9642c2ee9e6`)
  - **มติแสงประดับ (สุดท้าย 2026-08-08 รอบสาม): มีทั้งสองธีมแต่คนละชุด** —
    เส้นทาง: mock ตัด → ขอคืน (ได้ชุด v1 ทั้งคู่) → ชุด v1 "ไม่เข้ากับมืด" →
    ลองมืดแบนสนิทก็ไม่เอา → จบที่ สว่าง = ชุดส้มอุ่นของฐาน ·
    มืด = ชุด "แสงโคมโต๊ะช่าง" จางกว่าครึ่งหนึ่ง (accent 3–6%) + เงาลึกมุมล่าง
    · blur ตายถาวรทั้งคู่
  - **สองธีม (มติรอบห้า 2026-08-08 — สุดท้าย)**: สว่าง "โต๊ะช่างกลางวัน"
    เทาอมทรายอุ่น · มืด "โต๊ะช่างทึบ" ถ่านอมน้ำเงิน + navy แบรนด์จริง
    (#20365d) บน topbar + แสงโคม accent — **สะพานเชื่อมสองธีมคือ accent
    ส้ม + ภาษาแสงชุดเดียวกัน ไม่ใช่การย้อมพื้นให้อุ่นตาม**
    เส้นทางเต็ม: ถ่านน้ำเงินแบน → คืน gradient → เทาเย็นสว่าง "cool ไป" →
    สว่างอุ่น → ยกคอนทราสต์มืด → มืดอุ่นทั้งชุด (รอบสี่) → **"navy บริษัท
    หาย" จึงกลับถ่านน้ำเงิน+gradient พร้อมคืน navy ที่ topbar** (รอบห้า)
    บทเรียน: ตอนทำถ่านน้ำเงินรอบแรก topbar ถูกกดเป็นเกือบดำ = อัตลักษณ์
    navy หายโดยไม่มีใครสั่ง · ทุกคู่สีวัด contrast บนพื้นจริงทุกรอบ ≥4.6
- **วิธีรื้อ: pilot หน้าตัวแทน pattern → รันเป็น wave** — ไม่ใช่ทีละคอมโพเนนต์
  (70 ตัวดูนอกบริบทตัดสินไม่ได้) และไม่ใช่หน้าเดียวแล้วรันหมด (8 pattern
  หน้าเดียวครอบไม่ถึง)
- **pilot ใช้ชุดบริหารงานขาย** — เคาะบนหน้าที่ทีมหลักใช้จริง ไม่ใช่หน้าที่สะดวก
  (มติผู้ใช้ 2026-08-08)
- **เอกสารพิมพ์ไม่แตะรอบนี้** — `documentShell` + ตระกูล quotation เป็น design
  system ของ "กระดาษ" แยกจากจอโดยเจตนา จอเปลี่ยน ใบพิมพ์ต้องหน้าตาเดิม 100%
- **โครงที่ไม่รื้อ**: โทเคน 7 ชั้น + `audit:ui` + เทสต์ 40 ไฟล์ + ratchet —
  พวกนี้คือเครื่องมือของการรื้อ ไม่ใช่มรดกเก่า สิ่งที่รื้อคือ*ค่า*ในโทเคน
  และภาษาภาพ (glass/premium/gradient)

## กลไก

ชั้นทดลองคู่ขนาน `[data-ds="v2"]` ท้าย `globals.css` — **นิยามทับชื่อโทเคนเดิม
เท่านั้น ห้ามตั้งชื่อใหม่** (ชื่อใหม่ที่ไม่มีคนอ้างตกเทสต์ tokenUsage) ทั้ง 88
route จึงสลับพร้อมกันโดยไม่แตะไฟล์อื่น · สวิตช์อยู่ที่ `/settings/design-preview`
(`localStorage.ds` → theme-init ใน `layout.js` ติด attribute ก่อน paint)
· v2 มีโหมดเดียว (มืด) — ธีมสว่างเป็นการตัดสินใจค้างของ Phase 1

## Phase 0 — โครง `กำลังดำเนินการ`

- [x] worktree `ss_system-design` (branch `claude/design-v2`) — แยกจาก session
  อื่นที่รัน `ss_system` อยู่ · dev server พอร์ต 3001
- [x] block โทเคน v2 ใน `globals.css` + ดับ gradient ประดับ
- [x] สวิตช์บน design-preview + theme-init
- [x] `audit:ui` ผ่าน · เทสต์ 2398 ผ่าน · `build` ผ่าน · lint เหลือ warning
  เดิม 7 จุดที่มีมาก่อน (ไม่เกี่ยวกับงานนี้)
- [x] พิสูจน์การสลับแล้วบนหน้าจริง: design-preview + `/sa/dashboard`
  (สลับทั้งหน้าโดยไม่แตะไฟล์หน้า)

## Phase 1 — Pilot 5 หน้า (ชุดขาย) `รอดำเนินการ`

วนทีละหน้า: แก้ primitive กลางให้อ่าน v2 → เปิดดูจริง desktop + mobile →
ผู้ใช้ดู → แก้ → ผ่านแล้วล็อก ค่อยไปหน้าถัดไป

| # | หน้า | pattern | พิสูจน์ |
|---|---|---|---|
| 1 | `/sa/dashboard` | dashboard | KPI + กราฟ + 5 แท็บ — หน้าหลักของทีมขาย |
| 2 | `/sales-planning/targets` | matrix | กริดเป้า AE×12 เดือน — ความเสี่ยง "แห้งเป็น Excel" ของโต๊ะช่างเห็นที่นี่ก่อน |
| 3 | `/sales-planning/quotations/[id]` | form | editor ใบเสนอราคา — ฟอร์มโหดสุดของระบบ (จอเปลี่ยน แต่ส่วน preview เอกสารใน iframe ต้องหน้าตาเดิม) |
| 4 | `/sales-planning/deals` | list | pipeline + MetricStrip + filter — ตัวแทน pattern ใหญ่สุด (33 หน้า) |
| 5 | `/requests/[id]` | detail | WorkflowRail + เธรด — หน้าที่สองฝ่ายใช้ร่วม (มติ ม-31) |

5 หน้านี้บังคับ primitive หลักทั้ง ~20 ตัวออกมายืนในบริบทจริง — ผ่านครบ =
คอมโพเนนต์กลางเสร็จโดยปริยาย · design-preview ต้องอัปเดตตามทุกครั้ง
(กติกาเดิม: หน้าต้นแบบต้องพูดความจริง) · ~~ตัดสินใจค้าง: ธีมสว่าง~~ →
เคาะแล้ว 2026-08-08: มีธีมสว่างตั้งแต่ Phase 0 (ดูมติแก้ด้านบน)

ความคืบหน้า pattern กลาง (2026-08-08):

- [x] **pattern โมดัล = "สามชั้นแผ่นเดียว"** (มติผู้ใช้ — เลือกจาก mock 3 โครง
  + กติกาคอนโทรล, artifact `83d209ac`; เทียบกับหน้าเปิดคำร้อง RD แล้วเข้าทาง
  เดียวกัน): `Modal` มีโซน header(+subtitle)/toolbar/body/footer — หัวกับ
  แถบปุ่มนิ่ง เนื้อเลื่อน**ชั้นเดียว** · primitive ใหม่ 3 ตัว **OptionTiles ·
  StageSteps · ChoiceChips** ตามกติกาคอนโทรล: ชุดตายตัวเล็ก = เห็นครบแล้วจิ้ม ·
  ลำดับขั้น = แถบขั้นโชว์ผล (FC%) ใต้ทุกขั้น · รายการสั้น = ชิป · รายการยาว/
  ชื่อยาว (ลูกค้า/โครงการ/หมวด/AE) = SearchableSelect เดิม · นำร่องที่ฟอร์มดีล
  (ประเภท=แผ่นสี badge · สถานะ+FC ยุบช่องเดียว · แบรนด์=ชิปติดหลังลูกค้า ·
  ชิปลัดวันที่คาดปิด) — โมดัลอีก ~60 ใบใช้ชั้นเข้ากันได้ไปก่อน ไล่ตอน wave
  พร้อมย้ายญาติร่วมภาษา (ปุ่มฝ่าย requestForm · ชิป PDR) เข้า primitive ชุดนี้
- [x] **selector หมวดสินค้า = แผงสองชั้น TwoPanePicker** (มติผู้ใช้ 2026-08-08 —
  เลือก "แบบ B" จาก mock 3 ทาง และสั่งใช้เป็นดีไซน์กลาง): ProductCategorySelect
  กลายเป็น adapter บน TwoPanePicker (ของกลางเดิมจากมติ 2026-08-06) — rail
  หมวดหลัก 4 + นับต่อกลุ่ม · ลิสต์ย่อย + ค้นสองฝั่ง · ป้ายสรรพสามิต/แจ้ง อย.
  รายแถว · สัญญา onChange เดิมครบ ผู้เรียก 3 จุดไม่แตะ
- [x] **ช่องบังคับฟอร์มดีล** (มติเดียวกัน): ทุกช่องบังคับ ยกเว้น ลูกค้า · แบรนด์ ·
  โครงการ · หมวดสินค้า · รายละเอียด — ด่านที่ submit ของ DealCreateModal
- [x] **สวิตช์ "ดีลเก่าจากระบบเดิม"** (มติผู้ใช้ 2026-08-08 — "เปิดหมดได้เลย" =
  ถาวรทุกคน): สวิตช์ที่แถวสถานะของฟอร์มสร้าง เปิดแล้วแถบขั้นงอก Won — server
  ยอมสร้างที่ Won เมื่อ `metadata.legacy` (wonValue/confirmedAt คง null: Actual
  มาจากใบสั่งขายที่ผูกภายหลัง ตามมติ Won เดิม ไม่ปั้นตัวเลข) · primitive ใหม่
  `.ui-switch` (ธง/โหมดพิเศษ = สวิตช์ ตามกติกาคอนโทรล)
- [x] **สถานะไม่มี default** (มติเดียวกัน "สถานะต้องบังคับเลือก"): initialDealForm
  เลิก default "lead" — ต้องจิ้มเอง (จากลีดยังได้ qualified จาก flow) · ด่าน submit
- [x] **รายชื่อผู้รับผิดชอบกรองเหลือ ae/senior_ae** (มติเดียวกัน): `DEAL_HOLDER_ROLES`
  คุมทั้งดรอปดาวน์และ validateDealOwner — admin/AC หายจากลิสต์ (แก้ดีลได้แต่ถือไม่ได้)

## Phase 2 — รันทั้งระบบเป็น wave `รอดำเนินการ`

ลำดับตาม priority องค์กร (2026-07-26): sales ที่เหลือ (16) → sahamit (11) →
database (10) → tax (6) → mgmt+pm (8) → settings/users/audit/account (15) →
rd/service/production/support/กลาง (ที่เหลือ)

ต่อ wave: แปลงหน้า + **ลบชั้นเก่าทันที** (`glass-panel`/`premium-*`/inline style
— ratchet รูดลง งานวัดด้วยบรรทัดที่ลบ) + screenshot ก่อน/หลังส่งให้ผู้ใช้ดูรวดเดียว
ท้าย wave ไม่ต้องนั่งดูทุกหน้า

## Phase 3 — ของที่ pattern ปกติไม่ครอบ `รอดำเนินการ`

ทุกตัวใช้กติกา "เสนอให้เลือกก่อนลง" (ดูกติกาถาวรข้อ 2):

- **ปฏิทิน 4 หน้า** — mgmt/calendar · sa/calendar · service/schedule (กริดช่าง×วัน
  มีมติ "จงใจไม่ทำรายชั่วโมง" ในไฟล์) · settings/holidays → ทำ pattern
  ปฏิทินกลางครั้งเดียวใช้ 4 ที่
- **matrix ที่เหลือ** — sahamit/forecast · reconcile · targets/history ·
  production/board (กติกา "เตือนเกินกำลังแต่ไม่บล็อก" ต้องคงไว้)
- **Gantt** `pm/ProjectDocumentView` (860 บรรทัด) — drag + คณิตพิกเซล
  (ROW_H/DAY_MS) ห้ามเปลี่ยน spacing โดยไม่แก้ค่าคงที่คู่กัน
- **เอกสารพิมพ์** — ไม่แตะ (มติด้านบน) · `quotation-preview` แตะได้เฉพาะ
  chrome นอก iframe

## Phase 4 — ปิดงาน `รอดำเนินการ`

สลับ v2 เป็นค่าเริ่มต้น → ลบค่าธีมเก่า (:root/[data-theme] ชุดเดิม) →
คลาสเก่าตายจริง → รูด ratchet รอบสุดท้าย → เขียน `UI_DESIGN_SYSTEM.md` ใหม่
→ ลบสวิตช์และ `data-ds`

## กติกาถาวร (มติผู้ใช้ 2026-08-08 — อยู่ต่อหลังจบงานรื้อ)

1. **หน้าใหม่หยิบจาก design-preview เท่านั้น ห้ามคิดค่า/คลาสเอง** — `audit:ui` +
   ratchet บังคับอยู่แล้ว · ช่องโหว่ที่ต้องอุดใน Phase 0-1: เพิ่มโมดูล
   rd/service/production/support เข้า `scripts/uiLegacyBudget.mjs` + ไฟล์งบ
   (ตอนนี้ตกตะแกรง `shared` — ชั้นเก่างอกในโมดูลใหม่ได้โดยไม่มีใครเห็น)
2. **pattern ที่ไม่มีของกลาง = ห้ามเขียนในหน้า** — ทำตัวเลือก 2–3 แบบลง
   design-preview (หรือ artifact) → ผู้ใช้เลือก → ลงเป็น primitive + โทเคน →
   หน้าค่อยเรียกใช้
3. ทุก PR: `audit:ui` ผ่าน + ratchet ไม่ขึ้น — เพดานตกสองทางเหมือนเดิม
   (เกิน = เพิ่มของเก่า · ต่ำกว่า = ลืมรูดเพดาน)

## อ้างอิง

- mock 5 ทิศทาง + ตารางเทียบ: artifact `52191a1d-532a-49e2-a18d-f9642c2ee9e6`
- ผลสแกน pattern 88 route + call site count: session 2026-08-08
- `UI_DESIGN_SYSTEM.md` — สัญญาชั้นกลางปัจจุบัน (จะถูกเขียนใหม่ใน Phase 4)
