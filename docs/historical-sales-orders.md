# ใบสั่งขายย้อนหลัง (SO ย้อนหลัง) — คีย์งานบริการที่ขายไปแล้วนอกระบบ

> สถานะ: **กำลังดำเนินการ** · ตรวจกับโค้ดเมื่อ 2026-09-16 · P1 (โครงสร้าง + ตัวเขียน + จุดตัดเรื่องเงิน + คิว TS) **ขึ้น prod แล้ว 16/09/2026 (PR #1723 · mig 0360 รันแล้ว)** · P2a (โมดัลคีย์ 4 ขั้น + ทะเบียน SO) โค้ดครบ รอตรวจ — §8A · P2b ข1 ("TS ไม่พบจุดนี้หน้างาน" มติข้อ 23) โค้ดครบ รอตรวจ — §8B · **mig 0362 ยังไม่รัน** · ข2 (ถอดบรรทัด + กระดิ่ง) ยังไม่เริ่ม · เฟส 3 (คีย์ของจริง) รอ P2b ข1 ขึ้น

อ่านคู่กับ:
[so-pending-approval-amount.md](so-pending-approval-amount.md) (Actual = SO อนุมัติ · ตัว cache คู่ sync/enforce) ·
[legacy-deal-switch.md](legacy-deal-switch.md) (สวิตช์ "ดีลเก่า" — **คนละเรื่อง** กับสายนี้) ·
[service-intake-phase4.md](service-intake-phase4.md) (คิวงานเข้าใหม่ของ TS) ·
[sales-contract-plan.md](sales-contract-plan.md) (เอกสารแทนสัญญา) ·
ม็อก/บรีฟ `~/ss-team/mockups/legacy-so/` (อยู่นอกรีโป)

## 0. ย่อหนึ่งย่อหน้า

ชีตทะเบียนสัญญาบริการของทีม (379 จุดติดตั้ง · 149 ลูกค้า) ไม่มีทางเข้าระบบ เพราะสะพานเส้นเดียวระหว่างขายกับบริการ
(`service_zone_terms`) บังคับบรรทัดใบสั่งขาย ⇒ งานเก่าไม่ขึ้นคิวนัด ไม่มีรอบเติม ไม่มีต่อสัญญา
ทางที่เลือก: **แถวจริงใน `sales_orders` + ธง `origin = 'historical'`** เก็บยอดจริงไว้ในใบ แล้ว **กรองที่ตัวคำนวณ**
ทุกจุดที่คิดเงิน · ใบเกิดเป็น "อนุมัติแล้ว" ไม่มีใบเสนอราคา ไม่มีโครงการ ผูกกับ **ดีลภาชนะ** 1 ใบต่อ (ลูกค้า × AE)
· TS ผูกบรรทัดเข้าโซนเองในคิวงานเข้าใหม่

## 1. มติ (บรีฟ §3 · เคาะ 14–16/09/2026 "เอาตามที่แนะนำ")

| ข้อ | มติ | ลงโค้ดที่ |
|---|---|---|
| 1 | เงินงานเก่า **ไม่เข้า** Actual / FC / เป้า / ผลงานขาย (ออกบิลผ่าน Express ไปแล้ว) | §4 ทั้งหมด |
| 2 | เก็บยอดจริงไว้ในใบ แล้ว **กรองที่ตัวคำนวณ** (ไม่ใส่ 0 ในคอลัมน์เงิน) | `pipelineRowsOnly` · `salesOrderAmountKind` → `'excluded'` |
| 3 | ปลดเงื่อนไขใบเสนอราคาเฉพาะสายนี้ · เลขเดิมเก็บเป็นข้อความอ้างอิง | `quotationId DROP NOT NULL` + CHECK `sales_orders_origin_shape` (pipeline ยังบังคับ) |
| 4 | ผูกดีล · ดีลภาชนะ 1 ใบต่อลูกค้า × AE · ยอดดีล = 0 | CHECK `sales_deals_historical_shape` + unique `sales_deals_historical_container_uk` |
| 5 | ระบบออกเลขใหม่ตามรูปแบบปกติ (`SO-{YY}{MM}{RUNNING:4}-0`) · **วันที่ใบ = วันเริ่มสัญญาจริง** · เลข Express/IV/Q# เก็บเป็นอ้างอิง ค้นหาได้ | RPC ใช้บล็อกออกเลขชุดเดียวกับ 0343 (เทสต์เทียบ) · `historicalQuoteRef/ExpressRef/InvoiceRef` |
| 6 | เกิดเป็น **อนุมัติแล้ว** ทันที (ไม่เข้ากองรออนุมัติ) · `approvedAt` = เวลาคีย์ | RPC `create_historical_sales_order` |
| 7 | **ไม่ผูกโครงการ** | CHECK `"projectId" IS NULL` · guard create-project / link-project |
| 8 | 1 ใบ = ลูกค้า × ช่วงสัญญา (≈220) · **1 บรรทัด = 1 จุดติดตั้ง** (379) | `sales_order_lines."installationPoint"` (1–200) |
| 9 | งานที่จบแล้ว 196 แถว = เฟสสอง | planner ปฏิเสธ `running:false` |
| 10 | แถว "ยกเลิก" ไม่เอาเข้า | — (ตัดสินตอนคีย์) |
| 11 | แถวมูลค่า 0 เอาเข้าถ้ายังเดิน พร้อมหมายเหตุ | ยอด 0 ต้องมี `notes` + ยกเว้นด่านเงินอัตโนมัติ |
| 12 | สัญญางานที่ยังเดิน = **เอกสารแทนสัญญา** (external) | `POST /contracts` รับดีลภาชนะเฉพาะ external + service |
| 13 | งวดชำระ = คีย์เท่าที่รู้ + **สวิตช์ยกเว้นด่านเงินรายใบ** มีร่องรอยผู้ยกเว้น | `paymentGateExempt*` 4 คอลัมน์ · `visitGate` ข้อ② |
| 14 | รอบบริการต่อบรรทัดกรอกเท่าที่รู้ | `serviceRounds` เว้นว่างได้ |
| 15 | คีย์ได้: **AE Supervisor + แอดมิน** (เทียบ role ตรง ๆ ไม่ใช่ isSuperuser) | `canKeyHistoricalSalesOrder` + literal ใน RPC |
| 16 | คีย์ทีละใบ (ไม่อัปโหลดทั้งชีต) | ไม่มีตัวนำเข้า |
| 17 | **ผูกบรรทัด ↔ โซน = TS ในคิว "งานเข้าใหม่"** · ฝ่ายขายพิมพ์ชื่อจุดติดตั้งเป็นข้อความ ไม่มีตัวเลือกโซนในโมดัล | §8 |
| 18 | ค่า origin = **`'historical'`** (ไม่ใช่ `'legacy'` — `metadata.legacy` เป็นของสวิตช์ดีลเก่า) | `lib/sales/historicalOrders.js` บ้านเดียวของ literal |
| 19 | ป้ายดีลภาชนะ **ห้ามเรียก "ดีลเก่า"** | `HISTORICAL_DEAL_TITLE` = "งานบริการย้อนหลัง · ลูกค้า" |
| 20 | SO ย้อนหลังผูกดีลสวิตช์เดิมไม่ได้ — ดีลปลายทางต้อง historical + won + SERVICE + ไม่มีโครงการ | RPC ตรวจ `historical_so_deal_invalid` หลังทุกทางที่หยิบดีล |
| 21 | รายงานเป้า (`report/route.js`) ต้องกรอง origin ในเฟส 1 · ตัวกรองของ sync/enforce อยู่ใน **0360** (บรีฟเขียน 0357 — เลขย้ายแล้ว) | §4 |
| 22 | **ชื่อบนจอของดีลภาชนะ = "ดีลงานบริการย้อนหลัง"** (ห้าม "ดีลเก่า" ตามข้อ 19) · ชื่อดีลในโค้ดคงเดิม (`HISTORICAL_DEAL_TITLE`) · ลูกค้าที่มี AE สองคนได้ดีลชื่อซ้ำ ⇒ **โชว์ชื่อ AE ข้างชื่อดีลทุกที่** | โมดัลขั้น ④ · บรรทัดรองใต้ลูกค้าในทะเบียน SO = "AE {ชื่อ}" (§8A) |
| 23 | **จุดที่ TS หาไม่เจอ = ทาง ข** — TS กด "ไม่พบจุดนี้หน้างาน" พร้อมเหตุผล (ไทล์ 4 ตัว ไม่มีค่าตั้งต้น · บังคับหมายเหตุเมื่อ "อื่น ๆ") จุดนั้นหลุดจากคิว/ป้ายทันที จุดอื่นผูกต่อได้ · ฝ่ายขายเห็นชิป "TS ไม่พบจุด" ในทะเบียน SO แล้วตัดสินที่หน้าใบ (แก้ชื่อส่งกลับ / ปิดจุด เก็บยอด) · กระดิ่งถึง AE Sup ผู้คีย์ | ข1 = §8B (**mig 0362** · เลขย้ายจาก 0361 ที่ถูกจองไปก่อน) · 🔴 ข2 (ถอดบรรทัด + **กระดิ่ง**) ยังไม่ลงโค้ด |

### คำตอบคำถามเปิดของแผน P1 (15/09/2026 · ทับค่าตั้งต้นของแผน)

1. **AE บังคับตอนคีย์** — ต้องเลือก AE ปัจจุบัน (ae/senior_ae ที่ใช้งานอยู่ · กติกาเดียวกับ `validateDealOwner`) ·
   ไม่มีดีลภาชนะเจ้าของว่าง (CHECK + RPC `historical_so_owner_required`) · ทีมตามเจ้าของดีล ·
   แถวชีตที่ AE ว่าง (94 แถว) ผู้คีย์เลือก AE ให้ · unique เป็น `(customerId, ownerId)` ตรง ๆ
2. **งวดที่เก็บนอกระบบแล้วไม่คีย์** — คีย์เฉพาะยอดที่ยังต้องเก็บ · ใบที่มีประวัติเก็บเงินแล้วใช้สวิตช์ยกเว้นด่านเงิน ·
   พรีวิวเตือนงวดที่เลยกำหนด ("จะขึ้นเลยกำหนดในทะเบียนบัญชีทันที")
3. **TS หาจุดติดตั้งไม่เจอ** — P1 ไม่มีทางออก · บรรทัดค้างในคิวและนับบนป้ายตลอด · undo เดียวคือแอดมินลบทั้งใบก่อน TS ผูก ·
   **เคาะแล้ว 16/09: ทาง ข ตามมติข้อ 23** (TS กด "ไม่พบจุดนี้หน้างาน" พร้อมเหตุผล) — ลงโค้ดแล้วใน P2b ข1 (§8B · **mig 0362**)
4. **ย้ายพนักงาน (`users/[id]/transfer`) ไม่ย้ายดีลภาชนะอัตโนมัติ** — พรีวิว/ผลลัพธ์บอกจำนวนดีลภาชนะที่คนออกยังถืออยู่ ·
   ย้ายทีละใบด้วย PATCH ดีล (§6.6) · ปลายทางมีดีลภาชนะของลูกค้ารายนั้นอยู่แล้ว = 409 (P1 ไม่รวมดีล)

## 2. โครงสร้างข้อมูล (mig 0360)

- `sales_deals.origin` · `sales_orders.origin` — `'pipeline' | 'historical'` DEFAULT pipeline · **เปลี่ยนไม่ได้** (trigger `guard_record_origin_immutable`)
- `sales_orders`: `historicalQuoteRef` · `historicalExpressRef` · `historicalInvoiceRef` (≤200) · `historicalIntakeHash` (sha256 ของคำขอ) ·
  `paymentGateExemptAt/ById/ByName/Reason` (เหตุผล 10–500 · CHECK ห้ามใบ pipeline มี)
- `sales_order_lines."installationPoint"` (1–200)
- `sync_sales_order_actual` / `enforce_sales_order_actual_on_deal` = 0353 + `origin = 'pipeline'` สี่จุด (เทสต์ตัดตัวกรองแล้วเทียบ 0353)
- RPC (service_role เท่านั้น): `create_historical_sales_order` (intake key + fingerprint · replay เฉพาะคำขอเดียวกัน · ล็อกลูกค้า × AE ·
  สร้างดีลภาชนะถ้ายังไม่มี · race กับการย้ายเจ้าของ → ค้นใหม่แล้วผูก หรือ `historical_so_container_deal_race`) ·
  `append_historical_installments` (FOR UPDATE · ผลรวมห้ามเกินยอดใบ) · `historical_so_installments_total` (ตัวตรวจงวดชุดเดียว)
- งวดที่ RPC เขียน = `pending` + `frozenAt` (ตรึงแล้ว) ⇒ แจ้งชำระแล้วเข้าคิวบัญชี (`reported`) ตามสายเดิม · `financeStatus` NULL (ไม่เข้าคิวปิดใบของบัญชี)

## 3. ทางเข้าของคน

| ใคร | ทำอะไร | ด่าน |
|---|---|---|
| AE Supervisor / แอดมิน | คีย์ใบ · ยกเว้นด่านเงิน · คีย์งวดเพิ่ม · ย้ายเจ้าของดีลภาชนะ | `canKeyHistoricalSalesOrder` |
| ฝ่ายขาย / บัญชี | แจ้ง/รับรองงวดตามปกติ · ออกเอกสารแทนสัญญา + ผูกกับใบ | สิทธิ์เดิมของงวด/สัญญา |
| TS (Planner/หัวหน้า) | ผูกบรรทัดเข้าโซนในคิวงานเข้าใหม่ · เพิ่มไซต์ย้อนหลังถ้ายังไม่มี | `canEditService` |
| แอดมิน | undo = ลบใบ (ปกติได้เฉพาะยังไม่มีอะไรปลายน้ำ · บังคับลบผ่านพรีวิว) | §6.5 |

## 4. จุดตัดเรื่องเงิน (ทุกจุดที่ใบย้อนหลังต้องไม่ถูกนับ)

**SQL** — cache Actual/รออนุมัติของดีล (sync + enforce)

**ตัวอ่านใบสั่งขาย**
- `api/sales-planning/report` (ใบอนุมัติรายเดือน + ใบรออนุมัติ) — `pipelineRowsOnly`
- `lib/pm/productionJobsRepo` ร่างงานผลิตอัตโนมัติ — `pipelineRowsOnly` + `isHistoricalOrder` ใน `productionPlan`
- `lib/sales/handoffQueueData` คิวรอยื่นภาษี · `api/tax/orders/from-sales-order` (ตัวเลือก · GET "ไม่เข้าเกณฑ์" · POST ปฏิเสธ)
- `lib/sales/salesOrderWorkflow`: `salesOrderAmountKind` = `'excluded'` · `salesOrderActual` = 0 (ทะเบียน SO · หน้าดีล · หน้า SO)

**ตัวอ่าน KPI ระดับดีล** (ดีลภาชนะ = Won มูลค่า 0)
- `isWonAwaitingSo` ตัดดีลภาชนะ · dashboard/history กรองที่ query (history กันการเขียน `sales_history` ถาวรด้วย)
- `summarizeMyDeals` · การ์ด KPI หน้าดีล (`kpiDeals`) · DealDrillDownModal — `.filter(isKpiDeal)` (แถวยังโชว์ในตาราง แต่ไม่นับการ์ด "ดีลทั้งหมด")
- คำใต้การ์ด Won: kind `historical` — "ดีลของใบสั่งขายย้อนหลัง · ไม่นับยอดขายและ FC"

**การกระทำที่ปิด** — ย้อนการอนุมัติ · ออก Rev. · คืนเป็นร่าง · ยกเลิกแบบย้อน Won (ยกเลิกเฉย ๆ ได้) · สลับภาษาเอกสาร (พิมพ์ใบย้อนหลังยังไม่รองรับ) ·
ดีลภาชนะ: เปลี่ยนลูกค้า/สาย/ประเภท/ทีมว่าง · ไทม์ไลน์ · สร้าง/ผูกโครงการ · ลบดีลที่ยังถือใบ (CASCADE จะลบใบย้อนหลังทั้งลูกค้า × AE)

**ค้นหา** — ทะเบียน SO และทะเบียนการชำระค้นด้วยเลขเอกสารเดิมได้ · แถวทะเบียนการชำระพก `origin`

## 5. ยามกันไหลกลับ

| ยาม | ไฟล์ | กัน |
|---|---|---|
| SQL tripwire | `historicalSalesOrderMigration.test.mjs` | ฟังก์ชันที่อ่าน `sales_orders` + `'approved'` ต้องกรอง pipeline หรืออยู่ในรายการ "ตรวจสถานะอย่างเดียว" 7 ตัว (approve/cancel-reversal/capture-snapshot/finance-approve/revoke + สอง RPC ของ 0360) · ghost check |
| คำสั่งอ่าน SO | `historicalMoneyGuards.test.mjs` | ทุก `.from('sales_orders')` ที่แตะยอด/สถานะต้องอยู่ชั้นใดชั้นหนึ่ง: **MUST_FILTER** (report×2 · productionJobsRepo · handoffQueueData · tax picker) · **PER_ID_GUARDED** (tax by-id · SO `[id]` · installments) · **SEES_HISTORICAL** (nav counts · service intake · finance payments · ทะเบียน SO · overview ดีล) · **SCOPED_SAFE** (ของที่อ่านผ่าน projectId/quotationId/ร่าง) |
| import KPI ระดับดีล | เดียวกัน | ไฟล์ที่ import ตัวช่วย Won/FC ต้องตัดดีลภาชนะเอง หรืออยู่ใน `DEAL_KPI_ALLOWLIST` พร้อมเหตุผล |
| literal | เดียวกัน | `'historical'` และ `.eq('origin', …)` มีบ้านเดียวคือ `lib/sales/historicalOrders.js` (สะกดผิดที่เดียว = ตัวกรองว่างเงียบ) |
| สายไฟฝั่งบริการ | `lib/service/historicalServiceSide.test.mjs` | select ของคิว/bind/gateContext · bind เรียกด่านปลายทางก่อน insert · gateContext ไม่กลืน error · visitGate ยกเว้นเฉพาะข้อ② |
| copy paths | `serviceRoundsCopyPaths.test.mjs` | คอลัมน์ใหม่ของ `sales_orders` ต้องประกาศว่า Rev./ร่างไม่ก๊อป |

## 6. API

### 6.1 คีย์ใบ — `POST /api/sales-planning/sales-orders/historical`

พรีวิวก่อน (`preview:true`) แล้วบันทึกด้วย `intakeKey` เดิม · ห้าม `retry:true` · ออก `intakeKey` ใหม่หลังบันทึกสำเร็จ/หลังแอดมินลบใบ

```jsonc
{
  "preview": true, "intakeKey": "uuid", "running": true,
  "customerId": "…", "ownerId": "uuid (บังคับ)", "team": "… (ขอได้ แต่ทีมจริงตามเจ้าของ)",
  "orderDate": "2024-06-01", "amountsIncludeVat": true, "vatRate": 7, "docLanguage": "th",
  "refs": { "quote": "Q#…", "express": null, "invoice": "IV…" },
  "notes": "… (บังคับเมื่อยอด 0)",
  "lines": [{ "installationPoint": "…", "productId": "…", "qty": 2, "lineAmount": 60320, "serviceRounds": 36 }],
  "installments": [{ "label": "งวด 3/3", "amount": 30160, "dueDate": "…", "coversFrom": "…", "coversTo": "…" }],
  "paymentGateExemptReason": null, "acknowledgeDuplicates": false
}
```

| ผล | รูป |
|---|---|
| พรีวิว **ที่ผ่านทั้งใบ** | 200 `{ preview:true, plan:{ header, lines, installments, deal, exemption, duplicates, warnings, errors } }` — ไม่เรียก RPC · `errors` ว่างเสมอ |
| พรีวิว **ที่ยังไม่ผ่าน** | 🪤 **400 `{ error, errors:[{ field, message }] }` ไม่ใช่ 200** — `commitHistoricalOrder` ตอบ error ก่อนถึงสาขาพรีวิว ⇒ **ไม่มี** `plan`/`warnings`/`duplicates` ติดมา · จอต้องถือว่านี่คือผลพรีวิวปกติของฟอร์มที่ยังกรอกไม่ครบ แล้วแมป `field` กลับไปยังขั้น (§8A) |
| บันทึก | 201 `{ order, lines, installments, deal, dealCreated, replayed:false }` · ส่งซ้ำคำขอเดิม 200 `replayed:true` |
| ข้อมูลผิด | 400 `{ error, errors:[{ field, message }] }` (VAT 0 หรือ 7 เท่านั้น) |
| สิทธิ์ | 403 (ไม่ใช่ AE Sup/แอดมิน) |
| ซ้ำ | 409 `historical_so_duplicate_unacknowledged` + `duplicates` (ลูกค้าเดียวกัน วันที่เดียวกัน หรือเลขเดิมตรง) |
| รหัสคีย์ชน | 409 `historical_so_intake_key_conflict` + `existingOrderId` |
| ชนการย้ายเจ้าของดีล | 409 `historical_so_container_deal_race` — "กดบันทึกอีกครั้ง" |
| ยังไม่รัน 0360 | 503 |

### 6.2 ยกเว้นด่านเงิน — `PATCH /api/sales-planning/sales-orders/:id`
`{ action:"set_payment_gate_exemption", exempt, reason }` · AE Sup/แอดมิน · ใบย้อนหลังที่อนุมัติอยู่เท่านั้น · เหตุผล 10–500 ·
`exempt` ต้องเป็น boolean จริง (ไม่ส่ง / `"true"` / `1` = 400 — ไม่ตีความเป็นถอด) ·
ใบยอด 0 ถอนการยกเว้นไม่ได้ · สถานะเดิมซ้ำ = ไม่เขียน · audit เฉพาะสี่ช่อง

### 6.3 คีย์งวดเพิ่ม — `POST /api/sales-planning/sales-orders/:id/installments`
`{ action:"append", rows:[{ label, amount, dueDate, coversFrom, coversTo }] }` · AE Sup/แอดมิน + สิทธิ์แก้ดีล ·
201 `{ installments, appended, warnings }` · 400 `{ error, errors }` · 503 เมื่อยังไม่รัน 0360 ·
POST แบบอื่นบนใบย้อนหลัง = 400 (ไม่มีแผนจากใบเสนอราคาให้ยกมา)

### 6.4 สัญญา (เส้นเดิม)
`POST /contracts { dealId, kind:"service", source:"external", externalDocKind, externalRef, contractDate }` → แนบไฟล์ →
`approve-external` (ต้องมีวันเริ่ม + วันหมดอายุ) → `PATCH /sales-orders/:id { action:"set_service_contract", contractId }`
— ใบย้อนหลัง: สัญญาต้องมีทั้งสองวันและ **ครอบ `orderDate`** (ดีลภาชนะถือสัญญาได้หลายช่วง)

### 6.5 undo (แอดมิน)
`DELETE /sales-orders/:id?dryRun=1` → `DELETE` แบบปกติได้เมื่อยังไม่มีโซนที่ TS ผูก · รอบบริการ · งวดคอนเฟิร์ม · เลขใบกำกับ
(ไม่งั้น 409 → อ่านพรีวิวแล้วใช้ `?force=1`) · ด่านอ่านโซน/รอบบริการ/**งวด** ใหม่ทั้งสามก้อน อ่านไม่ขึ้น = 500 ไม่ลบ
(ไม่เชื่องวดของ `loadOrder` ที่กลืน error เป็นว่าง) · audit เก็บ zone terms + service plans + งวดดิบ · รอบบริการที่ชี้ใบถูกถอดชี้ ·
ดีลภาชนะที่ว่างแล้วลบต่อได้

### 6.6 ย้ายเจ้าของดีลภาชนะ (คำตอบข้อ 4)
- `GET /api/users/:id/transfer` → `{ historicalDeals, historicalDealList }` · `POST` (ย้ายดีลเปิดตามเดิม) คืน `historicalDeals` ·
  `historicalDealList` · `historicalDealsError` (ถ้านับไม่ได้) — **ไม่ย้ายดีลภาชนะให้**
- `PATCH /api/sales-planning/deals/:id { ownerId }` — AE Sup/แอดมิน · เจ้าของใหม่ต้องเป็น ae/senior_ae ปัจจุบัน ·
  ทีมของดีลถูกทับเป็นทีมของ AE ปลายทางเสมอ (ทีมตามดีล) · AE ที่ยังไม่มีทีม = 400 "ตั้งทีมที่หน้าจัดทีมก่อน" (ด่านเดียวกับตอนคีย์) ·
  AE คนนั้นมีดีลภาชนะของลูกค้ารายเดียวกันแล้ว = 409 บอกชื่อ AE + รหัสดีล (P1 ไม่รวมดีล) ·
  ใบย้อนหลังของดีลถูกย้าย `ownerId/ownerName` ตาม (ใบกำกับ/ทะเบียนการชำระแจ้งถูกคน) · audit
- ⚠️ ฟอร์มดีลล็อกช่องเจ้าของบนดีล Won ⇒ P1 ย้ายได้ทาง API เท่านั้น

## 7. การกระทำของงวดบนใบย้อนหลัง

| action | ใบย้อนหลัง |
|---|---|
| `schedule` · `coverage` · `report` (≥1 หลักฐาน) · `confirm` · `reject` · `withdraw` · `unconfirm` | ได้เหมือนใบปกติ — งวดตรึงแล้ว แจ้งชำระจึงเข้า `reported` |
| `tax-invoice` / `tax-invoice-clear` | ได้ — บันทึกเลขใบกำกับที่ออกไปแล้ว (writer เดียว `lib/sales/taxInvoice.js`) ไม่ได้ออกใบใหม่ |
| `link` | ปฏิเสธ (ไม่มี quotationId) |
| `unlink` | ไม่มีผล (ไม่มีอะไรผูก) |
| POST ยกงวดจากแผนใบเสนอราคา | 400 — ใช้ `append` |

## 8. ฝั่งบริการ (คิวงานเข้าใหม่ · ด่านเข้าไซต์)

รายละเอียดอยู่ใน [service-intake-phase4.md](service-intake-phase4.md) §ใบสั่งขายย้อนหลังในคิว — สรุป:
- แถวพก `origin` · เลขเดิม · จุดติดตั้ง · ใบย้อนหลังเรียงต่อท้ายใบปกติ · ป้ายบนเมนูนับรวม
- `fgSummary` แยกกลุ่มตามจุดติดตั้ง (ใบปกติไม่ขยับ) · wizard โชว์จุด + ชี้ "เพิ่มไซต์ย้อนหลัง"
- bind ตรวจปลายทางทุกใบ (ไซต์ของลูกค้าในใบ · ไซต์ลูกค้า · ไซต์/โซนเปิดใช้งาน) · ตัดวันของ term จาก body · audit รายแถว
- `visitGate` ข้อ② ผ่านเมื่อ `historicalGateExempt` และบอกว่าผ่านเพราะยกเว้น · **ข้อ① สัญญาไม่มีทางยกเว้น** —
  ทุกโซนของใบย้อนหลังติด "ผูกที่หน้าใบสั่งขาย" จนกว่าเอกสารแทนสัญญาที่ครอบ `orderDate` จะถูกผูก (P2 ต้องพาเดินขั้นสัญญา)
- `gateContext` เลิกกลืน error

## 8A. จอของผู้คีย์ (P2a) — โมดัล 4 ขั้น + ทะเบียน SO

ม็อกคือสเปก: `~/ss-team/mockups/legacy-so/index.html` (จอ `entry` · `lines` · `review`) + `design-brief.md` §3B

### โมดัลคีย์ใบ — `components/salesPlanning/HistoricalSalesOrderModal.js`

① ใบ → ② จุดติดตั้ง → ③ งวดและด่านเงิน → ④ ตรวจก่อนบันทึก → จอจบ · เปิดจากปุ่ม "SO ย้อนหลัง" บนหัวทะเบียน

- **ตัวตัดสินฝั่งจออยู่ที่ `lib/sales/historicalIntakeForm.js`** (บริสุทธิ์ ไม่มี React) — ตัวประกอบ body ตัวเดียว
  ที่ทั้งพรีวิวและบันทึกเรียก · ตัวแมป `errors[].field` → ขั้น · ตัวแปลรหัส error → ทางออก · เทสต์ `historicalIntakeForm.test.mjs`
- **ของที่ตรวจแล้ว = ของที่บันทึก** — เก็บ body ของพรีวิวที่ผ่านไว้ แล้วส่งก้อนเดิมตอนบันทึก (ลายนิ้วมือคำขอทำจาก `plan`
  ต่างแม้ช่องเดียว = 409 `intake_key_conflict`) · แก้ฟอร์มเมื่อไร แผนที่ตรวจไว้ถูกทิ้งทันที ยอดบนจอกลับเป็นขีด
- **รหัสการคีย์ = uuid ต่อโมดัลหนึ่งรอบ** ออกตอนเปิดโมดัล/กด "คีย์ใบถัดไปของลูกค้านี้" · กดบันทึกซ้ำใช้รหัสเดิมเสมอ ·
  ปิดโมดัล = ทิ้งข้อมูลทั้งหมด (ยืนยันก่อน) ⇒ เปิดใหม่ได้รหัสใหม่ · **ห้ามส่ง `retry: true`**
- **สี่ช่องที่จอบังคับเลือกเอง** เพราะ API เติมคำตอบให้เงียบ ๆ: `amountsIncludeVat` (ไม่ส่ง = true) ·
  `vatRate` (ไม่ส่ง = 7) · `team` เมื่อ AE อยู่ตั้งแต่ 2 ทีม (ไม่ส่ง = ทีมหลัก · ทีมอยู่ในลายนิ้วมือคำขอ ⇒ แก้ทีหลังไม่ได้) ·
  **เหตุผลยกเว้นด่านเงินเมื่อสวิตช์เปิด** — API ไม่มีบูลีนของสวิตช์เลย อ่านแต่ข้อความ ⇒ เหตุผลว่าง = `null` = "ไม่ยกเว้น"
  ซึ่งไม่ใช่ error ⇒ ใบลงฐานโดยไม่มีการยกเว้นเงียบ ๆ (นัดบริการติดด่านเงินทุกครั้ง · P2a ไม่มีจอยกเว้นย้อนหลัง ⇒
  ทางกลับเดียวคือแอดมินลบใบแล้วคีย์ใหม่) · จอใช้ `exemptReasonError` ตัวเดียวกับ server (10–500) ⇒ ข้อความตรงกัน —
  นอกจากสี่ตัวนี้ **ไม่มีกฎตรวจข้อมูลชุดที่สองฝั่งจอ** ข้อความไทยรายช่องมาจากพรีวิวเสมอ
- **ใบยอด 0** — จอส่ง `paymentGateExemptReason: null` เสมอ แม้ผู้คีย์เคยเปิดสวิตช์และพิมพ์เหตุผลไว้ก่อนยอดกลายเป็น 0
  (API ไม่บังคับข้อนี้: ส่งเหตุผลไป = เก็บเหตุผลนั้นแทนข้อความตายตัวและเลิกถือว่าอัตโนมัติ)
- **รายชื่อ AE ไม่กรองตามทีมของผู้คีย์** (`assignableOwners(directory, null)`) — ผู้คีย์คือ AE Sup/แอดมิน ซึ่ง
  `salesPlanningEditScope` = `all` ⇒ `validateDealOwner` ไม่มีด่านทีมให้คู่นี้ · กรองตามทีมของผู้คีย์ = ชีตที่มี AE ข้ามทีมคีย์ไม่ได้เลย
- **ขั้น ④ รู้ดีล/ทีมจริง/ยอดเป็นครั้งแรก** — พรีวิวที่ยังมี error คืนแค่ `{ error, errors }` (§6.1) ⇒ ขั้น ① ช่องดีลเป็น
  เส้นประ "รู้หลังตรวจก่อนบันทึก (ขั้น ④)"
- **ใบที่อาจซ้ำ** = รายการให้ยืนยัน ไม่ใช่ error · ปุ่มบันทึกโชว์แต่ติดด่านจนเปิดสวิตช์ "ตรวจแล้ว ไม่ใช่ใบซ้ำ" ·
  **กดแล้วเลื่อนไปที่สวิตช์ + โฟกัส** (สวิตช์อยู่เหนือตาราง "ยอดนี้ไปไหน" 10 แถว ⇒ คนที่ยืนอยู่ที่ปุ่มบันทึกเลื่อนผ่านไปแล้ว ·
  ตั้งข้อความไว้เฉย ๆ = ปุ่มอ่านเหมือนปุ่มตาย) · ตัวด่านเป็นฟังก์ชันบริสุทธิ์ `historicalDuplicateGate` ไม่ใช่เงื่อนไขใน JSX
- **จอจบ** พาเดินขั้นสัญญา: สร้างเอกสารแทนสัญญา (ชนิดบริการ · external · ไทล์ 4 ชนิด ไม่มีค่าตั้งต้น) ผ่าน route เดิม §6.4 ·
  ปุ่ม "คีย์ใบถัดไปของลูกค้านี้" ยกมาแค่ลูกค้า **AE ว่างเสมอ**
  ⚠️ **ร่างสัญญาไม่มีเลขที่** — `POST /contracts` ไม่เขียน `contractNo` เลย (0278 ออกเลขตอน issue/approve-external) ⇒
  แถบสำเร็จห้ามพิมพ์เลขที่ (เคยขึ้นเป็นขีดลอย ๆ ทุกครั้ง) · เขียนว่า "เลขที่ออกตอน AE Sup อนุมัติ" แล้วให้ปุ่ม "เปิดเอกสาร" พาไป
- **แถบท้ายของขั้นบอกสภาพจริง**: ขั้น ① ที่ยังไม่เคยผ่านพรีวิว = "ยังไม่ตรวจ" (ไม่ใช่ "ครบแล้ว" บนฟอร์มเปล่า — `issues` ว่าง
  แปลว่ายังไม่มีใครตรวจ) · ขั้น ③ ที่สวิตช์เปิดแต่เหตุผลยังไม่ครบ = "ยังไม่ยกเว้น — …" ไม่ใช่ "ยกเว้นแล้ว"
- **วันที่บนแถบบริบททุกขั้นใช้ `fmtDate`** — ค่าดิบของ `DateInput` เป็น ISO ซึ่งเป็นรูปที่ไม่มีใครในระบบนี้อ่าน

| ผลตอนกดบันทึก | ทางออกของโมดัล |
|---|---|
| 409 `historical_so_intake_key_conflict` | เปิดใบที่สร้างไว้ (`existingOrderId`) · ปิด — **ไม่มี "กลับไปแก้"** (รหัสรอบนี้ใช้ต่อไม่ได้) |
| 409 `historical_so_container_deal_race` | บันทึกอีกครั้ง (รหัสเดิม ข้อมูลเดิม) · ปิด — คำแนะนำ**ต่างจากเน็ตหลุด**: RPC raise ข้างในทรานแซกชัน ⇒ "ยังไม่มีอะไรลงฐาน" |
| 409 `historical_so_duplicate_unacknowledged` | กลับขั้น ④ พร้อมรายการใบที่อาจซ้ำชุดใหม่ + สวิตช์ปิด (**ไม่ไปจอ "บันทึกไม่สำเร็จ"** ซึ่งเหลือปุ่ม "ปิด" ปุ่มเดียว = เสียใบทั้งใบ) |
| 400 `errors[]` | ก้อนเดียวบอกทุกช่อง แล้วพาไปขั้นที่ช่องแรกอยู่ · กลับไปแก้ · ปิด |
| 400 ที่ไม่มี `errors[]` (`historical_so_money_mismatch` · CHECK ของฐาน …) | กลับไปแก้ · ปิด — **"กลับไปแก้" พกข้อความของ server ไปโชว์ที่ขั้นปลายทางด้วย** ไม่งั้นผู้คีย์ไปโผล่ที่ขั้น ① โดยไม่มีอะไรบอกว่าทำไม |
| 404 · 409 · 500 อื่น ๆ **ที่มี `code`** (`historical_so_deal_invalid` · `historical_so_customer_inactive` · `sales_deals_historical_shape` · `workflow_stale` …) | กลับไปแก้ · ปิด — **ห้ามเสนอ "บันทึกอีกครั้ง"**: ส่งก้อนเดิมได้รหัสเดิมวนไม่รู้จบ · แก้ฟอร์มแล้วส่งใหม่ได้เพราะ RPC raise = ทรานแซกชันถอย รหัสการคีย์ยังไม่ถูกใช้ |
| 403 · 503 · 4xx อื่นที่ไม่มีทั้ง `code` และ `errors[]` | ปิดอย่างเดียว |
| 500 · เน็ตหลุด (ไม่มี `code`) | บันทึกอีกครั้ง (รหัสเดิม — ถ้าใบลงไปแล้วได้ใบเดิมคืน) · ปิด |

### ทะเบียนใบสั่งขาย (`app/sales-planning/sales-orders/page.js`)

- ปุ่ม **"SO ย้อนหลัง"** ใน `headerRight` — สีกลาง · ไอคอนนาฬิกาย้อนหลัง · `canKeyHistoricalSalesOrder` (ไม่มีสิทธิ์ = ไม่เห็นปุ่มและไม่เรนเดอร์โมดัล)
- แถวใบย้อนหลัง: ชิป info **"ย้อนหลัง"** + **เลขเอกสารเดิม** แทนเลข QT · บรรทัดรองใต้ลูกค้า = **"AE {ชื่อ}"** แทนชื่อดีล (มติข้อ 22) ·
  ชื่อ AE เข้าชุดค้นแล้ว (ตาเห็นบนแถว = ต้องค้นเจอ)
- ตัวกรอง **"ที่มาของใบ"** เป็นกลุ่ม**สุดท้าย (ที่ห้า)** ใน `FilterPopover` เดิม ตามม็อก — สี่กลุ่มเดิมไม่ขยับตำแหน่ง (ค่ามาจาก `ORIGIN_PIPELINE`/`ORIGIN_HISTORICAL` · ตัดสินด้วย
  `isHistoricalOrder` ตัวเดียวกับตัวกรองเงิน) + ชิปล้างตัวกรองในแถบเครื่องมือ
  ⚠️ `FilterPopover` ไม่มีตัวเลขต่อตัวเลือก — ม็อกวาด "184 / 3" ไว้ แต่ไม่มีในของจริง (ต้องแก้ primitive กลาง = งานคนละก้อน)
- **รางสามขั้น** (`salesOrderListTrack`): ใบย้อนหลัง ขั้น "บัญชีปิดใบ" = **ข้าม** "ไม่เข้าคิวบัญชี" (`financeStatus` ว่างเสมอ
  โดยออกแบบ · ก่อนแก้ค้างเป็น todo "ยังไม่ส่งให้บัญชี" ตลอดกาล) · ใบที่ยกเว้นด่านเงินและไม่มีงวด ขั้นเก็บเงิน = **ข้าม** "ยกเว้นด่านเงิน"
- ยอดหรี่ "ยังไม่นับเป็น Actual" มาตั้งแต่ P1 (`salesOrderAmountKind` → `excluded`) — ไม่ได้แก้ในรอบนี้

### ยามของรอบนี้

| ยาม | ไฟล์ | กัน |
|---|---|---|
| ตัวตัดสินฝั่งจอ (**เทสต์จริง** เรียกฟังก์ชันตรง ๆ) | `lib/sales/historicalIntakeForm.test.mjs` | body ของพรีวิว = ของบันทึก · ไม่มีคีย์ `zoneId`/`status` · `running:true` · ใบยอด 0 ส่งเหตุผล null · สวิตช์ยกเว้นเปิดแต่เหตุผลว่าง = ด่านของขั้น ③ · แต่ละรหัส error มีทางออกของตัวเอง (รวมรหัสนอกสัญญา) · **ปุ่มที่โผล่จริงบนจอ failed** (`historicalExitActions`) · **ด่านใบซ้ำ** (`historicalDuplicateGate`) · ใบซ้ำไม่ไปจอ failed (`historicalSaveFailureState`) |
| ทะเบียน + โมดัล (🔴 **ยาม source** — regex บนตัวไฟล์ พิสูจน์ว่าข้อความอยู่ในไฟล์ ไม่ใช่ว่าโค้ดทำสิ่งนี้ · รีโปไม่มีตัวเรนเดอร์ React ⇒ ตรรกะต้องไปอยู่ในไฟล์แถวบน) | `lib/sales/historicalRegisterUi.test.mjs` | ชิป/เลขเดิม/บรรทัด AE · ชุดค้นมีชื่อ AE · ตัวกรอง origin ร้อยครบสี่จุด · ด่านสิทธิ์ของปุ่ม · โมดัลอยู่นอก Workspace · ไม่มี `retry:true` · รหัสการคีย์ออกใหม่ที่เดียว · ค่าที่ไหลเข้า `previewPayload` · โมดัลเรียกตัวตัดสินทั้งสามตัว · เลื่อน+โฟกัสสวิตช์ตอนติดด่าน · **ทุกชื่อที่ import จาก lib มี export อยู่จริง** |
| ราง | `lib/sales/salesOrderListTrack.test.mjs` | สอง `skip` ใหม่ · ใบ pipeline ที่มีร่องรอยยกเว้นปลอมต้องไม่ข้ามขั้นเก็บเงิน |

### ไม่อยู่ใน P2a

จุด "TS ไม่พบหน้างาน" (มติข้อ 23 · ต้องมี mig 0361) · งานที่จบแล้ว (`running:false`) · คำร้องขอใบวางบิล · พิมพ์ใบย้อนหลัง ·
ยกเว้นด่านเงินภายหลัง (`PATCH set_payment_gate_exemption`) · คีย์งวดเพิ่ม (`POST …/installments`) · ย้ายเจ้าของดีลภาชนะ ·
คำบนหน้ารายละเอียด SO ("ลบฉบับร่างถาวร" · "อ้างอิง QT" · การ์ดที่อยู่ "ยึดตามใบเสนอราคา")

## 8B. จุดที่ TS หาไม่เจอหน้างาน (P2b ข1 · มติข้อ 23 · mig 0362)

ม็อกคือสเปก: `~/ss-team/mockups/legacy-so/index.html` กรอบ "รอเคาะ" ของจอ 04 (วิซาร์ด) และจอ 05 (หน้าใบ) · บรีฟ §3B ข้อ 23

**ปัญหาเดิม** — ชีตตรงหน้างานแค่ ~25% · จุดที่ TS หาไม่เจอค้างในแท็บ "รอตั้งไซต์/โซน" และนับบนป้ายเมนูตลอดกาล ·
ทางออกที่มีเป็นแบบทั้งใบ (แอดมินลบ / AE Sup ยกเลิก) ซึ่งฆ่ารอบบริการของจุดที่ TS หาเจอในใบเดียวกันไปด้วย

### สองชุดคอลัมน์ คนละเจ้าของ (mig 0362 บน `sales_order_lines`)

| ชุด | ช่อง | ใครเขียน |
|---|---|---|
| ธง | `siteNotFoundAt/ById/ByName/Reason/Note` | **TS** (`canEditService`) ผ่าน `POST /api/service/intake/site-not-found` |
| ตราปิด | `siteClosedAt/ById/ByName/Note` | **ฝ่ายขาย** (`canKeyHistoricalSalesOrder`) ผ่าน `PATCH /sales-orders/:id` |

- CHECK สองตัว: ครบหรือว่างทั้งชุด · เหตุผล 4 รหัส (`name_mismatch` · `branch_closed` · `customer_dropped` · `other`) ·
  **หมายเหตุบังคับเฉพาะ `other`** (ม็อกวาดบังคับทุกไทล์ = ม็อกผิด · มติทับแล้ว) · **ปิดจุดได้เฉพาะจุดที่ติดธง**
- **หมายเหตุตอนปิดจุดไม่บังคับ** (เคาะ 16/09) — เหตุผลจริงมาจาก TS ซึ่งอยู่บนบรรทัดแล้ว และ audit เก็บใคร/เมื่อไรครบ
- บรรทัดไม่มี `origin` ของตัวเอง ⇒ ด่าน "เฉพาะใบย้อนหลัง" เป็น **trigger** ที่ไล่ขึ้นไปดูใบแม่
  (CHECK มองข้ามตารางไม่เห็น) · trigger เดียวกันกันการติดธงบนบรรทัดที่ผูกโซนไปแล้ว
- 🪤 **trigger ตัวที่สองอยู่ฝั่ง `service_zone_terms`** — บรรทัดที่ติดธงอยู่ ผูกโซนไม่ได้ ⇒ TS สองคนกดพร้อมกัน
  ทางไหนก่อนก็ได้ คนหลังชนเสมอ คิวและยอด `remaining` ไม่มีทางค้างครึ่ง ๆ
- ทั้ง 9 ช่อง **ไม่ถูกก๊อป** โดย `create_sales_order_draft` / `revise_approved_sales_order_atomic` (เทสต์ตรึง)

### จุดคอขวดเดียวของคิว

`lineNeedsAllocation` (`lib/service/terms.js`) ตัดบรรทัดที่ `siteNotFoundAt` ไม่ว่าง — `bindQueue` ตัวเดียวเลี้ยงทั้ง
**แท็บ "รอตั้งไซต์/โซน"** · **ป้ายตัวเลขบนเมนู** (`api/nav/counts`) · **รายการกลุ่มในวิซาร์ด** ⇒ สามที่ขัดกันไม่ได้เชิงโครงสร้าง
- ครอบทั้ง "รอฝ่ายขายตัดสิน" และ "ปิดแล้ว" — ทั้งคู่ไม่ต้องให้ TS ผูกโซน
- ใบที่ทุกจุดถูกแจ้ง ⇒ `pending` ว่าง ⇒ `bindQueue` ข้ามใบนั้น (`if (!pending.length) continue`) ⇒ หลุดแท็บและป้ายพร้อมกัน
- 🔴 **ตัวอ่านบรรทัดของคิวทุกตัวต้อง select ธง** — `api/nav/counts` เลือกคอลัมน์ผอมโดยตั้งใจ แต่ `"siteNotFoundAt"`
  ตัดออกไม่ได้ ไม่งั้นป้ายนับใบที่กดเข้าไปแล้วว่าง (ยาม `historicalServiceSide.test.mjs`)

### ทาง TS — วิซาร์ดงานเข้าใหม่

- แผงเดียวสองที่ (`components/service/SiteNotFoundPanel.js`): **ขั้น 1** "ไม่พบจุดของใบนี้หน้างาน" (เลือกจุดเองได้
  ทีละจุดหรือทุกจุด **โดยไม่ต้องเลือกไซต์ก่อน**) · **ขั้น 2** ปุ่มเงียบรายกลุ่มจุด (กรณีเจอบางจุด)
  ⚠️ ขั้น 1 ต้องมี เพราะปุ่ม "จัดสรรลงโซน" ปิดจนกว่าจะเลือกไซต์ ⇒ ลูกค้าที่สาขาปิดจนไม่มีไซต์เลยจะไปไม่ถึงขั้น 2
- **ถอนการแจ้งได้จนกว่าฝ่ายขายจะตัดสิน** — รายการ "จุดที่แจ้งไว้แล้ว" อยู่ที่ขั้น 1 · ปิดแล้วไม่มีปุ่มถอน
- แถวคิว (ตารางและการ์ด) ขึ้นชิป **"รอฝ่ายขายตัดสิน N จุด"** — นับจากทุกบรรทัดของใบ ไม่ใช่เฉพาะ `pending`
- 🐞 **แก้บั๊กเดิมในรอบนี้**: วิซาร์ดส่ง `order.id` ซึ่งไม่มีอยู่จริง (แถวคิวใช้ชื่อ `orderId`) ⇒ กด "บันทึกและตั้งรอบต่อ"
  ได้ 400 "ต้องระบุใบสั่งขาย" **ทุกครั้งตั้งแต่เฟส 4 ขึ้น** — ตรงกับที่ `service_zone_terms` ยังว่างเปล่าบนฐานจริง

### ทางฝ่ายขาย — ทะเบียนและหน้าใบ

- ทะเบียน SO: ชิป **"TS ไม่พบจุด N จุด"** บนแถว + ตัวกรองกลุ่มที่หก **"จุดติดตั้ง"** (ตัวเลือกเดียว) ·
  ทะเบียนส่ง `"siteNotFoundAt"` + `"siteClosedAt"` มาให้จอนับเอง
- หน้าใบ: การ์ด **"จุดที่ TS ไม่พบหน้างาน (N)"** เหนือรายการสินค้า (`components/salesPlanning/SiteDecisionCard.js`) ·
  โชว์เหตุผล/ผู้แจ้ง/เวลา และ **ยอดบรรทัด** เพื่อให้เห็นว่า "เก็บยอด" แปลว่าอะไร
- สองปุ่มต่อจุด ไม่มีทางไหนถูกเลือกไว้ก่อน · แต่ละปุ่มมีโมดัลที่ **บอกผลลัพธ์ก่อนกด**:

| การกระทำ | ผล |
|---|---|
| `rename_installation_point` | แก้ `installationPoint` + ล้างธงทั้ง 9 ช่อง ⇒ บรรทัดกลับเข้าคิว TS เอง (ไม่มีทางคืนแยก) |
| `close_installation_point` | ประทับตราปิด ⇒ ไม่กลับเข้าคิวอีก · **ไม่แตะยอดใบและงวด** (ข้อ 23.1) |

- ⚠️ `rename_installation_point` คือ **ข้อยกเว้นเดียว** ของกติกา "บรรทัดใบสั่งขายเป็นภาพนิ่ง" — แคบที่สุดเท่าที่ทำได้:
  ใบย้อนหลัง · เฉพาะบรรทัดที่ติดธงและยังไม่ถูกปิด · แก้ได้ช่องเดียว (สิ่งที่เปลี่ยนคือ "ไปหาที่ไหน" ไม่ใช่ "ขายอะไรไป")
- 🪤 การตัดสิน **ไม่ใช้ `requestAction`** ของหน้าใบ — ตัวนั้นเก็บ error ไว้ในแถบของหน้าซึ่งอยู่ *ใต้* โมดัลที่เปิดค้าง
  ⇒ กดแล้วจอเงียบสนิท (บทเรียนเดิมของ `ReasonDialog` 2026-08-19)
- ตัวกรองตอนเขียนทั้งสองทางบอกสถานะที่คาดไว้ด้วย (`.is('siteNotFoundAt', null)` / `.not(…)` + `.is('siteClosedAt', null)`)
  ⇒ TS ถอนการแจ้งพอดีตอนฝ่ายขายกด = 409 ไม่ใช่เขียนทับเงียบ ๆ

### ยามของรอบนี้

| ยาม | ไฟล์ | กัน |
|---|---|---|
| ตัวตัดสิน (**เทสต์จริง**) | `lib/sales/siteNotFound.test.mjs` | 4 รหัส · หมายเหตุของ `other` เท่านั้น · สามสถานะของบรรทัด · ก้อนเขียน/ล้างครบ 9 ช่อง · audit แคบ |
| คิว/ป้าย (**เทสต์จริง**) | `lib/service/intake.test.mjs` | จุดที่แจ้งหลุดจากคิว · จุดอื่นผูกต่อได้ · แจ้งครบ = ใบหลุดแท็บ+ป้ายพร้อมกัน · ปิดแล้วไม่กลับ · แก้ชื่อแล้วกลับเอง · **ใบ pipeline ไม่ขยับ** |
| migration | `lib/sales/siteNotFoundMigration.test.mjs` | รูป CHECK ทั้งสองตัวเทียบค่าคงที่ฝั่ง JS · trigger สองตัว · REVOKE · รันซ้ำได้ · ไม่ backfill · ทางก๊อป Rev./ร่างไม่พาธงไป |
| สายไฟฝั่ง TS | `lib/service/historicalServiceSide.test.mjs` | select ธงครบทุกตัวอ่าน (รวมป้ายเมนู) · ด่านของ route ครบ · ตัวกรองตอนเขียน · ไม่มี `retry:true` · `order.orderId` |
| สายไฟฝั่งขาย | `lib/sales/historicalWriterGuards.test.mjs` · `historicalRegisterUi.test.mjs` | ด่าน PATCH ครบ · การตัดสินห้ามแตะ qty/ราคา/ยอดใบ/`.delete()` · TS เขียนตราปิดไม่ได้ · ฝ่ายขายตั้งธงไม่ได้ · ตัวกรองร้อยครบสี่จุด · **ไม่มีปุ่ม "ถอดออกจากใบ"** |

### ไม่อยู่ใน P2b ข1

**ข2**: ถอดบรรทัดออกจากใบ + คิดเงินหัวใบใหม่ · **กระดิ่งถึง AE Sup ผู้คีย์** (ข้อ 23.2) —
ม็อกจอ 05 วาดปุ่ม "ถอดออกจากใบ" ไว้ **อย่าเติมปุ่มนั้นจนกว่าตัวคิดเงินหัวใบจะมี** (เทสต์ตรึงไว้ว่าปุ่มต้องไม่โผล่)

## 9. ลำดับ deploy

**P1 เดินตามลำดับนี้ครบแล้ว (16/09/2026 · #1723)** — P2b ข1 เดินแบบเดียวกันด้วย **mig 0362**
**P2a ไม่มี migration** ⇒ deploy ได้ตรง ๆ

🛑 **P2b ข1: รัน 0362 บน prod ก่อน merge คอมมิตโค้ด** — โค้ดรอบนี้ select คอลัมน์ใหม่ที่
คิวงานเข้าใหม่ · ป้ายเมนูของ TS · ทะเบียน SO และหน้ารายละเอียด SO ⇒ ขึ้นก่อนรัน = 42703 ⇒ ห้าจอนั้นตอบ 500
(คอมมิต migration แยกไว้แล้ว และไม่มีโค้ดที่อ่าน/เขียนฐานติดไปกับมัน)

1. commit 1: mig 0360 + เทสต์ของมัน (ไม่มี JS) · `npm test` + `check:migrations`
2. **รัน 0360 บนฐานจริงด้วยมือ** (SQL Editor · DDL) → คำสั่งตรวจหลังรันในหัวไฟล์ → smoke test BEGIN…ROLLBACK
3. `npm run check:columns` + `check:rowcap` บนเครื่อง
4. commit 2+: JS ทั้งหมด · PR เขียน "0360 รันบน prod วันที่ … — ต้องรันก่อน merge" · CI `check:columns` แดงจนกว่าจะรัน

🔴 สลับข้อ 2 กับ 4 = 500 ที่ dashboard · รายงาน · คิวผลิต · ตัวเลือกภาษี · ด่านเข้าไซต์ (visits/plans/renewals) · คิวงานเข้าใหม่ ·
และคิวรอยื่นภาษีว่างเงียบ (my-dashboard/overview กลืน error) · รัน 0360 ก่อนปลอดภัย

## 10. ค้าง / ต้องตัดสิน

- 🔴 **P2b ข2 — ถอดบรรทัดออกจากใบ + คิดเงินหัวใบใหม่ · กระดิ่งถึง AE Sup ผู้คีย์ (ข้อ 23.2)** ยังไม่ลงโค้ด ·
  ข1 เสร็จแล้ว (§8B · mig 0362) — **รัน 0362 ก่อน merge**
- 🐞 **`service_zone_terms` ว่างเปล่าบน prod** — วิซาร์ดส่งรหัสใบผิดชื่อมาตั้งแต่เฟส 4 (แก้ในรอบนี้) ⇒
  ไม่เคยมีใครผูกโซนสำเร็จเลย · ตรวจกับ TS ว่าเคยกดแล้วเจอ "ต้องระบุใบสั่งขาย" ไหม
- แจ้ง TS ก่อนเฟส 3 ว่าป้ายงานเข้าใหม่จะโตราว 180 บรรทัด
- ยังไม่มีจอ (API มีแล้ว): ยกเว้นด่านเงินภายหลัง (`PATCH set_payment_gate_exemption` §6.2) · คีย์งวดเพิ่ม (§6.3) ·
  ย้ายเจ้าของดีลภาชนะ (§6.6 — ฟอร์มดีลล็อกช่องเจ้าของบนดีล Won)
- คำบนหน้ารายละเอียด SO ที่ไม่จริงสำหรับใบย้อนหลัง: "ลบฉบับร่างถาวร" · "อ้างอิง QT" · การ์ดที่อยู่ "ยึดตามใบเสนอราคา" (ใบย้อนหลังไม่มี QT)
- พิมพ์ใบย้อนหลัง / สลับภาษาเอกสาร · ใบวางบิล — ยังไม่รองรับทั้งคู่
- `FilterPopover` ไม่มีตัวเลขต่อตัวเลือก ⇒ ตัวกรอง "ที่มาของใบ" ไม่มี "184 / 3" ตามม็อก (ต้องแก้ primitive กลาง = ต้องมีมติ)
- บรีฟ (`mockups/legacy-so/design-brief.md`) ยังเขียน `origin='legacy'` (§2) · "0357" (ข้อ 21) · ดีลเก่า 49 (จริง 51)
- `canSalesOrderTransition` (ตัดสินจากสถานะอย่างเดียว) ยังไม่ได้ไล่ผู้เรียก
