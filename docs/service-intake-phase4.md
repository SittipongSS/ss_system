# งานเข้าใหม่ (เฟส 4) — ทางที่ใบสั่งขายเดินมาถึงฝ่าย TS

**สถานะ: รอตรวจ** — โค้ดขึ้นครบ 2026-08-28 (ไม่มี migration ใหม่ · ใช้ตาราง
`service_zones` / `service_zone_terms` ที่ mig 0297 สร้างไว้ตั้งแต่เฟส 2) · ยังไม่ UAT
· 🔄 **แก้ §ใบสั่งขายย้อนหลังในคิว ทั้งย่อหน้า 2026-09-23 ตามมติ 22/09 (mig 0374)** — ใบย้อนหลังข้ามถังผูกโซน
และป้ายบนเมนูเปลี่ยนวิธีนับ ⇒ **ต้องรัน 0374 ก่อน merge**

- เขียน: 2026-08-28
- ต่อจาก: `docs/business-line-level-and-handoff.md` §5 (ลูกศรที่ 3) · `docs/service-field-operations.md`
- มติที่คุมไฟล์นี้: มติผู้ใช้ 2026-08-27 (Zone เป็น entity ถาวรเกิดโยงจากบรรทัดใบสั่งขาย)
  + มติผู้ใช้ 2026-08-28 (**TS ไม่ใช่ต้นทางของงาน**)

> ⚠️ **`docs/service-business-system-plan.md` ไม่ครอบเฟสนี้** — ไฟล์นั้นหยุดที่ 2026-08-01
> และยังเขียนว่า "ยังไม่เริ่มเขียนโค้ด" ทั้งที่เฟส 1–3 + F-1…F-5 + ด่านเข้าไซต์ merge ไปแล้ว
> (อาการ "สถานะเน่า" ที่ `INDEX.md` เตือนไว้เอง) · เฟส 4 อ่านจากไฟล์นี้

## ปัญหาที่หน้านี้มาปิด

ชีตของทีมมี **102 จุดที่ลูกค้าจ่ายเงินแล้วแต่ไม่มีคิวบริการ** งานพวกนั้นไม่ได้หายไป —
มันไม่เคยเดินมาถึงฝ่าย TS เลย เพราะไม่มีหน้าไหนพาใบสั่งขายมาให้
(`business-line-level-and-handoff.md:157`)

## สิ่งที่ตัดสินไปแล้ว

| ข้อ | มติ | ทำไม |
|---|---|---|
| หน่วยของคิว | **ใบสั่งขาย** (นับเป็นบรรทัด) | คนทำงานเปิดทีละใบ แต่ของที่ต้องผูกคือบรรทัด |
| ใบที่รับได้ | `status='approved'` และไม่ถูก Rev. ทับ | ใบที่ยังไม่อนุมัติมียอด/ของที่ขยับได้ ⇒ snapshot ที่ก๊อปไปจะเป็นของปลอม |
| สายธุรกิจ | ถาม `projects.line` ก่อน แล้วค่อย `sales_deals.line` | โครงการเป็นเจ้าของค่า · ดีลถือสำเนาไว้ตอนยังไม่มีโครงการ |
| ตอบไม่ได้ว่าสายอะไร | ขึ้น**ถังของมันเอง** เหนือคิว ไม่เดา ไม่เงียบ | เดาเมื่อไร ใบสายสินค้าไหลเข้าคิวบริการ หรือใบบริการหายเงียบ แย่พอกันทั้งคู่ |
| 1 บรรทัด = 1 รอบขาย | `salesOrderLineId` UNIQUE ที่ DB | จำนวนโซนที่ขายนับจากจำนวนบรรทัด |
| ต่อสัญญา | ใบใหม่ผูก**โซนเดิม** ไม่สร้างโซนใหม่ | ประวัติและยอดการใช้ของโซนเดินต่อไม่ขาดตอน |
| "รอบยังมีผลไหม" | คำนวณจากใบแม่ที่ `lib/service/terms.js` ที่เดียว | term ไม่มีคอลัมน์ status โดยเจตนา (mig 0297:83) · โรคเดียวกับ "live visit" ที่เคยมี 5 นิยาม |
| มาตรฐานต่อเดือน (ml) | **เสนอให้กดรับ ไม่เขียนเอง** และเสนอเฉพาะบรรทัดที่ขายเป็นแพ็ค | ไม่มีสูตรที่เป็นทางการ · หลักฐานเดียวคือชีตที่ลงตัว 10/13 แถวที่ 1 แพ็ค = 1 ลิตร/เดือน · บรรทัด "240 กิโลกรัม" ที่ขึ้นข้อเสนอ "240,000 ml" คือข้อเสนอที่ผิดกว่าถูก |
| หน้านี้สร้างงานไม่ได้ | ไม่มีปุ่ม "สร้างใหม่" เลยโดยตั้งใจ | TS ไม่ใช่ต้นทางของงาน (กติกาเดียวกับที่ถอดปุ่ม + ออกจากหน้าจัดคิวช่าง) |
| ไซต์ในตัว wizard | **เลือกได้อย่างเดียว** · สร้างใหม่เรียกฟอร์มเดิม | ที่อยู่ทางภาษีกับที่อยู่หน้างานเป็นคนละความจริง · ห้ามแก้ทะเบียนลูกค้าจากที่นี่ |

## สามถัง (แท็บ)

| แท็บ | เงื่อนไข | ปลายทาง |
|---|---|---|
| **รอตั้งไซต์/โซน** | ใบ approved สาย SERVICE ที่ยังมีบรรทัดไม่ผูกโซน | wizard 2 จังหวะ → เกิดแถว `service_zone_terms` |
| **รอตั้งรอบ** | โซนที่มีรอบขายมีผล แต่ไซต์ยังไม่มี `service_plans` ที่ active | หน้าไซต์ (ตั้งรอบ) |
| **ครบรอบยังไม่มีนัด** | รอบ active ที่ไม่มีนัด**ที่ยังมีชีวิต**ข้างหน้าเลย | หน้าไซต์ (เติมนัด) |

⚠️ ถังที่สามใช้ `isLiveVisit` ตัวเดียวกับด่านเข้าไซต์ — `visitQueue()` **โยน error ถ้าไม่ส่งมา**
เข้มกว่าด่านจริง = ซ่อนงานที่ทำได้ · หลวมกว่า = ชวนกดแล้วเด้ง

## ไฟล์

| ชั้น | ไฟล์ |
|---|---|
| ตัวตัดสิน | `lib/service/terms.js` (รอบมีผลไหม · snapshot · ข้อเสนอ ml) · `lib/service/intake.js` (สามถัง + สายธุรกิจ) |
| data access | `lib/service/termsRepo.js` |
| API | `api/service/intake` (คิว) · `api/service/intake/bind` (ผูกทั้งใบในคำขอเดียว) |
| จอ | `app/service/intake/` · `components/service/IntakeWizard.js` |

## ใบสั่งขายย้อนหลังในคิว (mig 0374 · มติ 2026-09-22 — **ทับของเดิมทั้งย่อหน้า**)

🔄 **มติข้อ 17 ของ 0360 ("TS ผูกบรรทัดของใบย้อนหลังเข้าโซนเองในคิวนี้") ถูกทับแล้ว** — ฝ่ายขายเลือกไซต์/โซน
จากทะเบียนตั้งแต่ตอนคีย์ใบ (`sales_order_lines."serviceZoneId"`) และ **รอบขายของโซนเกิดตอน AE Sup อนุมัติใบ**
⇒ ใบย้อนหลัง **ไม่ผ่านถัง "รอตั้งไซต์/โซน" อีกเลย** มันโผล่ที่ถัง **"รอตั้งรอบ"** ตรง ๆ · `installationPoint` เหลือเป็น
snapshot `"<รหัสไซต์> <ชื่อไซต์> · <ชื่อโซน>"` ที่ RPC เขียนเอง ไม่ใช่ข้อความที่คนพิมพ์
รายละเอียดทั้งสาย: [historical-sales-orders.md](historical-sales-orders.md)

| เรื่อง | ทำอะไร | ที่ไหน |
|---|---|---|
| ถังของใบย้อนหลัง | **รอตั้งรอบ** เท่านั้น (โซนผูกมาแล้ว) · ถังผูกโซนเหลือไว้ให้ใบ pipeline | `lib/service/intake.js` `planQueue` |
| 🔴 ป้ายบนเมนู | **นับ `bindQueue` + `planQueue`** — ของเดิมนับถังผูกโซนอย่างเดียว ซึ่งใบย้อนหลังไม่เคยเข้า ⇒ TS จะไม่ได้สัญญาณเลย · นับทุก origin ไม่ใช่เฉพาะใบย้อนหลัง · ถัง "ครบรอบยังไม่มีนัด" ยังไม่นับ (ต้องอ่านนัด) | `api/nav/counts` |
| แท็บตั้งต้น | ถังผูกโซนว่างแต่ถังตั้งรอบไม่ว่าง = **สลับไปถังตั้งรอบให้ครั้งเดียว** หลังโหลดสำเร็จครั้งแรก (เปิดมาเจอแท็บว่างทั้งที่ป้ายบอกว่ามีงาน = อ่านว่าป้ายโกหก) · คนเลือกแท็บเองแล้วรอบเบื้องหลังไม่ดึงกลับ | `app/service/intake/page.js` |
| แถวถังตั้งรอบ | ชิปเงินสามทาง: **"ไม่มีงวดให้เก็บ"** (ใบยอด 0 · `paymentNotRequired` · 🔄 แทนชิป "ยกเว้นด่านเงิน" ที่ถอดแล้ว) · **"เงินครอบถึง {วัน}"** (`paidThrough`) — นัดหลังวันนั้นจอดเป็นร่างรอบัญชีรับรองงวดถัดไป · **"ยังไม่มีงวดที่รับรอง"** (⚠️ ไม่ใช่ "ยังไม่มีเงินเข้า" — งวดยกมาเก็บเงินไปแล้ว บัญชียังไม่รับรองเท่านั้น) · โน้ต "โซนผูกจากฝ่ายขายตอนคีย์ใบแล้ว" | `app/service/intake/page.js` · `lib/service/intake.js` |
| **ด่านเข้าไซต์ข้อ②** | ผ่านเมื่อ `paymentNotRequired(totalAmount)` **หรือ** งวดครอบวันนัด และไม่มีงวดเลยกำหนดที่ยังไม่รับรอง ⇒ **ใบยอด 0 บาทผ่านเองทุก origin** · **สวิตช์ยกเว้นด่านเงินรายใบถูกถอด** (ร่องรอย `paymentGateExemptAt` ที่ค้างในแถวไม่มีใครอ่านแล้ว ปลอมมาก็ไม่ปลดด่าน) · **ข้อ① สัญญายังไม่มีทางยกเว้น** แต่ใบย้อนหลังผูกเอกสารแทนสัญญามาตั้งแต่คีย์ | `lib/service/visitGate.js` |
| บริบทด่าน | select `origin` + `totalAmount` (แทน `paymentGateExemptAt`) · **เลิกกลืน error** (โยนให้ route ตอบ 500 แทนการจอดนัดทั้งไซต์ด้วยเหตุผิดฝ่าย) | `lib/service/gateContext.js` |
| ด่านปลายทางของการผูก (**ใบ pipeline**) | `bindTargetError` เหมือนเดิม: ไซต์ต้องเป็นของลูกค้าในใบ · ชนิด `customer` · ไซต์/โซนเปิดใช้งาน · ใบย้อนหลังผ่านด่านชุดเดียวกันนี้ที่ RPC ตอนคีย์แทน (`historical_so_check_lines`) | `intake.js` + `api/service/intake/bind` |
| วันของ term | bind **ตัด `startDate`/`endDate` ที่ body ส่งมาทิ้ง** เหมือนเดิม · รอบขายที่เกิดตอนอนุมัติก็เว้นวันไว้ NULL | `api/service/intake/bind` · RPC อนุมัติ |
| 🔴 บังคับลบโซน/ไซต์ | โซนที่อยู่ในบรรทัดใบสั่งขาย **บังคับลบไม่ได้** — FK `serviceZoneId` เป็น RESTRICT และตัวลบลึกรันทีละก้อนนอกทรานแซกชันเดียว ⇒ ไม่กันไว้จะลบรอบขาย/ไฟล์สำรวจทิ้งก่อนแล้วไปล้มที่โซน · พรีวิวคืน `blocked` และตัวลบลึกโยนก่อนก้าวแรก · ลบแบบปกติเจอ 23503 = "โซนนี้อยู่ในใบสั่งขาย — ปิดใช้งานแทน" | `lib/service/forceDeleteService.js` · `api/service/sites/[id]/zones/[zoneId]` |

⚠️ คิว/ด่านเข้าไซต์ **ไม่ได้อ่านคอลัมน์ใหม่ของ 0374 เอง** (select ของ `api/service/intake` ตัดธงจุด 9 ช่องของ 0362 ออกแล้ว
และอ่าน `totalAmount` ซึ่งมีมานานแล้ว) — ตัวที่พังถ้า merge ก่อนรันคือ **ตัวกรอง `.in('serviceZoneId', …)` ของ
`forceDeleteService`** (42703 ⇒ พรีวิว/บังคับลบโซนกับไซต์ตอบ error) และฝั่งใบสั่งขายทั้งเส้น
⇒ **รัน 0374 ก่อน merge เสมอ**
🔒 ยามสายไฟ: `lib/service/historicalServiceSide.test.mjs` · `lib/service/forceDeleteService.test.mjs` · เทสต์หน่วยใน `terms` / `intake` / `visitGate` / `navCounts`

## ของที่ยังไม่ทำ

- **หน้าโซนยังไม่โชว์รอบขาย** — ม็อกออกแบบตารางไว้แล้ว (รอบทุกรอบ รวมที่จบแล้ว) แต่ยังไม่ลงจอ
- ปุ่ม "สร้างสัญญาบริการ" — 🅿 รอต้นฉบับสัญญาจ้างบริการ (`contractTemplates.service = null`)
- `startDate`/`endDate` ของรอบยังไม่มีที่กรอกบนจอ (คอลัมน์พร้อมแล้ว) — รอเฟสสัญญามา sync · ตั้งแต่ 2026-09-15 bind ตัดค่าที่ body ส่งมาทิ้ง (เส้นเขียนต้องมาจากเฟสสัญญาเท่านั้น)
- ~~**ใบย้อนหลัง: TS หาจุดติดตั้งไม่เจอ**~~ — **ปิดเรื่องด้วยการถอดทิ้ง 22/09/2026 (mig 0374)** ไม่ใช่ด้วยทางออกของมติข้อ 23
  ฝ่ายขายเลือกโซนจากทะเบียนตั้งแต่คีย์ใบ ⇒ ไม่มีบรรทัดที่ "หาไม่เจอ" ให้ TS แจ้งอีก · ปุ่ม "ไม่พบจุดนี้หน้างาน" ·
  แผงในวิซาร์ด · การ์ดตัดสินบนหน้าใบ · กระดิ่ง `sales_order_site_not_found` · RPC ถอดบรรทัด **ถอดออกหมดแล้ว**
  · คอลัมน์/CHECK/trigger ของ **mig 0362 ยังอยู่ในฐาน 0 แถว** (`siteNotFoundMigration.test.mjs` ตรึงรูปไว้) · **mig 0366 ไม่ต้องรันแล้ว**
- 🐞 **`service_zone_terms` ว่างเปล่าบน prod ตั้งแต่เฟส 4 ขึ้น** — วิซาร์ดส่ง `order.id` แต่แถวคิวใช้ชื่อ `orderId`
  ⇒ กด "บันทึกและตั้งรอบต่อ" ได้ 400 "ต้องระบุใบสั่งขาย" ทุกครั้ง (แก้แล้ว 16/09/2026 พร้อม mig 0362)
  · ตั้งแต่ 0374 ใบย้อนหลังไม่เดินทางนี้แล้ว (รอบขายเกิดจาก RPC อนุมัติ) — ทางนี้เหลือไว้ให้ใบ pipeline
- ยังไม่ UAT
