# ยอด SO "รออนุมัติ" — โชว์ได้ แต่แยกจาก Actual

> สถานะ: **เสร็จสมบูรณ์** · ตรวจกับโค้ด **และกับฐานจริง** เมื่อ 2026-09-16 · รอบแรกขึ้น production 2026-09-11 (#1703 · `099f4a3f`) · รอบสอง (คาดจบงวด + Won รอยื่น SO + ทีมตามดีล) ขึ้น production 2026-09-15 (#1717) · รอบสาม (ไม่นับดีลเก่าที่สร้างเป็น Won) merge #1722 · รอบสี่ (SO อนุมัติ 0 บาทยังนับรอยื่น + ตัดดีลมูลค่า 0) ขึ้น production 2026-09-16 (#1725 · `77833eea`) · รอบห้า (รีเช็คเงื่อนไขทั้ง 6 กอง) ขึ้น production 2026-09-16 (#1733 · `eafcb2b1`) · รอบหก (เอกสารเป็นตัวตั้งของยอดดีล · มติ 2026-09-16 บ่าย) ขึ้น production 2026-09-16 (#1736 · `85849279`) · **mig 0361 รันบนฐานจริงแล้ว** · backfill 38/38 ใบ · mig 0353 รันบนฐานจริงแล้ว (backfill 8 ดีล รวม 993,000 · wonValue คงเป็น 0 ครบ)

## ที่มา

ผู้ใช้แจ้ง 2026-09-11: *"SO ที่รออนุมัติ มันกลายเป็น 0 อยากให้โชว์ยอดด้วย แต่แยกให้รู้ว่า
รออนุมัติ กับ Actual แล้ว ทั้งระบบ"*

ดีลกลายเป็น **Won** ตั้งแต่กดรับใบเสนอราคา (mig 0284) แต่ **Actual** นับเฉพาะใบสั่งขายที่
**อนุมัติแล้ว** (mig 0110 / 0279) ⇒ ช่วงที่ SO ยื่นแล้วรอ AE Supervisor อนุมัติ ยอดของดีล
หายไปจากทุกจอ:

- หลุดจาก **FC คงเหลือ** เพราะดีล Won แล้ว
- ยังไม่เข้า **Actual** เพราะใบยังไม่อนุมัติ
- เหลือร่องรอยแค่ใน FC Total ซึ่งไม่มีใครอธิบายได้ว่าส่วนต่างมาจากไหน

ของจริงวันที่แจ้ง: SO รออนุมัติ 8 ใบ รวม 993,000 บาท (ก่อน VAT) · ทุกใบอยู่บนดีล Won ที่ Actual = 0

## มติ

| เรื่อง | มติ |
|---|---|
| โชว์ไหม | โชว์ทุกจอที่โชว์ Actual — เป็น **ตัวเลขแยก** มีคำว่า "รออนุมัติ" กำกับเสมอ |
| นับอะไร | เฉพาะ `status = 'pending_approval'` · ร่าง / ตีกลับ / ย้อนอนุมัติ / ออก Rev. แล้ว / ยกเลิก **ไม่ใช่** รออนุมัติ |
| ยอดไหน | `actualAmount` (ก่อน VAT) ตัวเดียวกับ Actual — ห้ามใช้ `totalAmount` |
| เดือนไหน | **เดือนปัจจุบัน (เวลาไทย) เสมอ** — อนุมัติย้อนหลังไม่ได้ ถ้าอนุมัติวันนี้ Actual ก็ลงเดือนนี้ · ใบที่ค้างข้ามเดือนเลื่อนมาอยู่เดือนใหม่เอง · เดือนที่ปิดแล้ว/ปีก่อนไม่มีวันเห็นยอดนี้ |
| นับให้ใคร | ดีล Won เท่านั้น (ดีลที่ยังเปิดมี FC อยู่ใน FC คงเหลือแล้ว) · รายคนตามเจ้าของดีล **ปัจจุบัน** (`sales_orders.ownerId` ว่างจนกว่าจะอนุมัติ — mig 0294) · ทีม = **ทีมของดีล** (`sales_deals.team` — มติ 2026-09-14 "ทีมตามดีล" ไม่ใช่ทีมในบัญชีเจ้าของ ⇒ แถวคน = (คน, ทีมของดีล) คนหลายทีมได้แถวละทีม) |

### ⛔ ห้ามบวกเข้า Actual — ทุกกรณี

wonValue · won · actual[] · เป้า/% ปิดได้/ขาด-เกิน · variance · ยอดทบ (carry) · YoY ·
แผนที่ความร้อน · ยอดที่ตัวช่วยวางเป้า (`/api/sales-planning/history`) และปุ่ม
"เติมยอดจากระบบ" (`/sa/targets/history`) **คัดลอกไปเก็บใน `sales_history` ถาวร** —
ถ้ายอดรออนุมัติหลุดเข้าไป มันจะกลายเป็น Actual ปลอมที่ทับยอดจริงตลอดไป

ข้อยกเว้นเดียวที่ตั้งใจ: **ยอดคาดการณ์ "คาดจบงวด" (`windowStat.projected` ของแท็บผลงานขาย)** —
ดูหัวข้อถัดไป · % · ขาด/เกิน · ทบยอด · ต้องทำเฉลี่ย/เดือน ที่อยู่ข้างกันยังคิดจาก Actual ล้วน

## ยอดคาดการณ์ "คาดจบงวด" + "Won รอยื่น SO" (มติผู้ใช้ 2026-09-14)

> 🐞 คำอธิบายรอบ 11/09 ผิดสามจุด — ตรวจซ้ำกับจอจริง 14/09 แล้วผู้ใช้เลือกใหม่ (ข้อ ก)

**คาดจบงวด = Actual + รออนุมัติ + Won รอยื่น SO + FC คงเหลือ** · คาดขาด = ต้องปิด − คาดจบงวด

| กอง | นับอะไร | เดือน |
|---|---|---|
| Actual | SO อนุมัติแล้ว | เดือนที่อนุมัติ |
| รออนุมัติ | SO `pending_approval` | เดือนปัจจุบันเวลาไทย |
| **Won รอยื่น SO** | ดีล Won ที่ **ยังไม่มีใบสั่งขายที่อนุมัติ และไม่มียอดใบรออนุมัติ** (ยังไม่ออก · ร่าง · ตีกลับ/ดึงกลับ · มีแต่ใบยกเลิก · **มีแต่ใบที่ยื่นแล้วยอด 0 บาท**) · ⛔ **ใบที่อนุมัติแล้วแม้ยอด 0 บาท = จบ ออกจากกอง** (ส่วนลด 100% เป็นเรื่องปกติ · มติ 2026-09-16 บ่าย) · มูลค่าดีลเต็ม · **มูลค่าดีล 0 ไม่นับ** · **ไม่นับดีลเก่าที่สร้างเป็น Won** (`isLegacyWonAtCreate` · มติ 2026-09-15 หัวข้อถัดไป) | เดือนที่ปิด Won (`wonMonthOf` ถังเดียวกับ FC Total) |
| FC คงเหลือ | ดีลที่ยังเปิด · **มูลค่าดีลเต็ม ไม่ถ่วงโอกาสปิด** (ช่องชื่อ `weighted` ในโค้ดหลอก) | เดือน FC |

- **ทำไมต้องมี Won รอยื่น SO**: รับใบเสนอราคา = ดีล Won ทันที (mig 0284) ⇒ หลุดจาก FC คงเหลือ
  แต่ SO เกิดเป็นร่าง (0285) ⇒ ไม่อยู่ในรออนุมัติ/Actual ⇒ คาดขาดพุ่งเต็มมูลค่าดีลจนกว่าจะยื่น SO
  (ของจริง 14/09: 42 ดีล FC 2,659,950 — ไม่มี SO 35 · มีแต่ร่าง 2 · มีแต่ใบยกเลิก 5)
  🐞 ตรวจ 15/09: 35 ดีล "ไม่มี SO" คือดีลเก่าที่สร้างเป็น Won ผ่านสวิตช์ทุกดีล — ยื่น SO ไม่ได้ จึงตัดออก
  (หัวข้อถัดไป) · กองวันนี้ 42 ดีล / 2,348,450 → **7 ดีล / 703,100** (2,659,950 เดิมลดเหลือ 2,348,450 เพราะ mig 0359 ล้างบล็อก A 311,500)
- ~~"มี SO อนุมัติแล้ว" ดูจาก `metadata.wonMonth`~~ → **มติผู้ใช้ 2026-09-16: ดูจาก Actual > 0** — SO อนุมัติ 0 บาทบนดีล
  FC > 0 คือเอกสารขั้นแรก (ใบ DEMO · ค่าออกแบบกลิ่นก่อนบรีฟ · ใบแทนใบจริงที่ยกเลิก) ออเดอร์จริงยังจะมา ⇒ ยังนับรอยื่น SO
  (ของจริง 16/09: 8 ดีล FC 1,468,366 — ใบเสนอราคารับแล้ว 0 บาท + SO อนุมัติ 0 บาท + FC กรอกมือทุกดีล) ·
  เดือนตาม `wonMonthOf` = เดือนที่อนุมัติใบ 0 บาท · หน้าดีลขึ้น "ใบสั่งขายที่อนุมัติยังเป็น 0 บาท" แทน "ต่าง ฿(FC เต็ม)"
- **มูลค่าดีล 0 บาทไม่นับ** ทั้งยอดและจำนวน (มติ 2026-09-16) — บวกคาดการณ์ 0 ได้แค่บรรทัด "฿0.00 · 1 ดีล"
- ✅ คิว/การ์ด "Won รอ SO" (แดชบอร์ดของฉัน · แบนเนอร์หน้าใบเสนอราคา · ความพร้อมปิดโครงการ) **ตรวจแล้วไม่ต้องแก้**
  — ดูหัวข้อ "คิว Won รอ SO ไม่ต้องแก้" ท้ายรอบหก · ข้อความเดิมตรงนี้เขียนใต้กติกา ① ของรอบสี่ ซึ่ง **ถูกกลับโดยรอบหก**
  ในวันเดียวกัน ⇒ ช่องว่างที่เคยกังวลปิดตัวเองไปแล้ว
- กองนี้บน prod 16/09: ก่อนแก้ 5 ดีล 632,000 → หลังแก้ 10 ดีล 2,100,366 (+8 ดีล SO 0 บาท · −3 ดีลมูลค่า 0)
- เดือนที่บริษัทกรอก Actual มือ (`sales_history`) `overlayHistory` ล้างเส้น Won รอยื่น SO ในช่องที่ทับ —
  ยอดของดีลพวกนั้นอยู่ในตัวเลขที่กรอกแล้ว
- ข้อความ "คาดจบงวด … — คาดขาด / คาดถึงเป้า" ขึ้นใต้แถบบริษัทเสมอในงวดที่ยังไม่จบ พร้อมบรรทัดบอกฐาน ·
  แถบเล็กในตารางติดตามบอกค่าเดียวกันผ่าน title/aria (งวดที่จบแล้วไม่พูดคาดการณ์)
- ข้อจำกัดที่รู้: ดีลที่มี SO อนุมัติบางส่วน + ส่วนที่เหลือยังร่าง ไม่นับส่วนที่เหลือ · ดีลเดินข้ามเดือน
  ได้ (Won เดือนหนึ่ง → ยื่น SO อีกเดือน) หน้าต่างรายเดือนจึงเห็นยอดย้ายเดือน ส่วนรายปีคงที่
- ตัวช่วย: `dashboardMetrics` (`isWonAwaitingSo` · `isLegacyWonAtCreate` · `wonAwaitingSoAmountOf` · `wonAwaitingSoMonthOf` ·
  `WON_AWAITING_SO_LABEL`) · API ช่อง `wonAwaitingSo` / `wonAwaitingSoCount` · ลิ้นชัก metric `wonAwaitingSo`

## Won รอยื่น SO ไม่นับดีลเก่าที่สร้างเป็น Won (มติผู้ใช้ 2026-09-15 "เอาที่แนะนำ")

ที่มา: ตรวจฐานจริง 15/09 — กอง Won รอยื่น SO 42 ดีล 2,348,450 เป็นดีลที่สร้างเป็น Won ผ่านสวิตช์
"ดีลเก่าจากระบบเดิม" 35 ดีล 1,645,350 (83% ของจำนวน · 70% ของยอด) · ดีลเหล่านี้ยื่น SO ไม่ได้เลย
(ออกใบเสนอราคาให้ดีล Won ไม่ได้ · SO ต้องมีใบเสนอราคาที่รับแล้ว · PATCH เปลี่ยนขั้นของดีล Won ไม่ได้)

| เรื่อง | มติ |
|---|---|
| ตัดอะไร | ดีลเก่าที่สร้างเป็น Won ออกจาก Won รอยื่น SO **ทั้งยอดและจำนวน ทุกจอ** (ทางเลือก A) — ตัดที่ `isWonAwaitingSo` จุดเดียว ถัง API · ลิ้นชัก · แท็บผลงานขาย รับผลเอง · แดชบอร์ดของฉันไม่มีกองนี้ ไม่เปลี่ยน (การ์ด/คิว "Won รอ SO" ของจอนั้นอ่านจากใบเสนอราคาที่รับแล้ว — ดีลเก่าที่สร้างเป็น Won ไม่เคยเข้า) |
| ตัวบ่งชี้ | `isLegacyWonAtCreate` (`lib/sales/dashboardMetrics.js`) คำนวณจากแถว ไม่มี migration: Won · `metadata.legacy === true` · ไม่มี `acceptedQuotationId` · `wonSource !== 'quotation'` — เหตุผลรายข้ออยู่ในคอมเมนต์ของตัวช่วย · ห้ามเขียนเงื่อนไขซ้ำที่อื่น (`legacyDealSwitch.test.mjs` สแกนหา) |
| ผลบนฐานจริง 15/09 | 42 ดีล / 2,348,450 → **7 ดีล / 703,100** · ตัด 35 ดีล = บล็อก B 11 ดีล 1,645,350 + ดีล ฿0 24 ดีล (บล็อก A 4 · พิมพ์ 0 ตอนสร้าง 20) · จับผิด 0 จาก 166 ดีลที่ Won ผ่านใบเสนอราคา · รายเดือนที่เหลือ: 2026-08 4 ดีล 640,600 · 2026-09 3 ดีล 62,500 |
| ดีลสวิตช์ที่ปิดผ่านใบเสนอราคา | ไม่โดน (มี `acceptedQuotationId` + `wonSource='quotation'`) 7 ดีล เช่น DL-26080394 · ถ้า SO ยังไม่อนุมัติก็นับตามปกติ |
| ไม่เปลี่ยนโดยตั้งใจ | Actual · % · ขาด/เกิน · FC Total · FC คงเหลือ · ลิ้นชัก fcTotal · ส่วนต่าง "ยอด Won" · FC Excel · `wonCount` · `projectRollup` · การล้างของ `sales_history` (`overlayHistory`) — บล็อก B ยังอยู่ใน FC ตามมติ 0359 จนเจ้าของยืนยัน |
| หน้าดีล | คำใต้การ์ด "มูลค่าปิดจริง (Won)" ของดีลแบบนี้ = **"ดีลเก่าจากระบบเดิม · ไม่มีใบสั่งขายในระบบนี้"** (`wonDealForecastHint` ชนิด `legacy_no_so`) แทน "คาดการณ์ ฿X · ยังไม่มีใบสั่งขายที่ยื่น" · บรรทัดบันทึกของ 0359 คงเดิม: บล็อก A ซ่อนคำ เหลือบันทึก · บล็อก B คำ + บันทึก "ยอดคาดการณ์ยังอยู่ใน FC รอเจ้าของยืนยัน" · ดีลพิมพ์ 0 (ไม่มีบันทึก) เหลือคำบรรทัดเดียว |
| ปิดทางหลบ | POST/PATCH ดีลถอด `wonSource` / `acceptedQuotationId` จาก metadata ที่ client ส่งมา (`SERVER_ONLY_DEAL_METADATA_KEYS`) · PATCH ถอดก่อน merge ค่าที่ RPC เขียนไว้จึงอยู่ต่อ · ธง `legacy`: PATCH ไม่รับค่าจาก client เลย คงค่าเดิม (`clientDealMetadataOnPatch` — ไม่งั้น `{legacy:false}` พาดีลกลับเข้ากอง) · POST รับเฉพาะ `true` จริง (`hasLegacySwitchFlag` ตัวอ่านเดียวกับตัวบ่งชี้ · เดิมด่านเช็ก truthy ⇒ `legacy: 1` สร้าง Won ได้แต่หลุดตัวบ่งชี้) · ⚠️ **ห้ามเพิ่ม `legacy`** ลงรายการ · ตรวจผู้เรียก 15/09 ไม่มีจอไหนส่งสองคีย์นี้ และมีแค่โมดัลสร้างที่ส่ง `legacy` (เป็น `true` เสมอ · prod 49/49 เป็น `true`) |

- ช่องบนแท็บผลงานขายที่ขยับ (หลัง overlay · จำลองในบรีฟตัดสินใจ 15/09): ก.ย. บริษัท ฿104,500 · 8 ดีล → ฿62,500 · 3
  (คาดจบงวด −42,000) · ส.ค. บริษัท 12 → 4 ดีล ยอดเท่าเดิม · ปี 2026 บริษัท ฿745,100 · 21 → ฿703,100 · 7 ·
  ปี 2026 ทีม KA −430,000 (บรรทัดหาย) · ODM −302,000 · บรรทัด ฿0 หายทั้งหมด · แถว "ยังไม่ได้แยกทีม" กลับเป็น 0
- API แคช 5 นาทีต่ออินสแตนซ์ — หลัง deploy ตัวเลขเปลี่ยนภายในอายุแคช
- **ข้อจำกัดที่รู้ (ไม่แก้รอบนี้)**: `overlayHistory` ล้างเส้น Won รอยื่น SO เฉพาะช่องที่ `sales_history` มีแถว —
  แถวบริษัทมีทุกเดือน 2023-01..2026-07 แต่แถวรายคนมีแค่ 2026-07 ⇒ แถวทีม/รายคนเดือน ม.ค.–มิ.ย. ยังแสดง
  Won รอยื่น SO ขณะที่แถวบริษัทถูกล้าง · หลังมตินี้ไม่มีดีลในกองนี้ที่เดือน ม.ค.–มิ.ย. แล้ว (ข้อมูล 15/09) แต่กติกา overlay ยังเหมือนเดิม
- **DL-26080133** (`confirmedAt` 2026-12-31) หายจากเดือน ธ.ค. เองภายใต้มตินี้ · วันที่ผิดยังเป็นงานแก้ข้อมูลของเจ้าของ
  ([legacy-deal-switch.md](legacy-deal-switch.md) §6) ไม่มีโค้ด

## ที่เก็บข้อมูล

**mig 0353** — trigger เดิมสองตัวเขียน cache เพิ่มสองคีย์ใน `sales_deals.metadata`

| คีย์ | ความหมาย |
|---|---|
| `soPendingAmount` | Σ `actualAmount` ของ SO รออนุมัติของดีล |
| `soPendingCount` | จำนวนใบ |

- ไม่มี SO รออนุมัติ = **ไม่มีคีย์** (ถอดทิ้ง ไม่เขียน 0 ค้างทุกดีล) · JS อ่านคีย์ที่ไม่มีเป็น 0
  ⇒ โค้ดขึ้น production ก่อนรัน migration ได้โดยไม่มีอะไรพัง
- `sync_sales_order_actual` กับ `enforce_sales_order_actual_on_deal` ต้องแก้ **คู่กันเสมอ**
  enforce เป็น BEFORE UPDATE ที่ไม่มีเงื่อนไข = คนเขียนคนสุดท้าย และ PATCH ดีลเอา metadata
  ทั้งก้อนของหน้าจอไป merge ทับ — enforce จึงเป็น "เจ้าของ" สองคีย์นี้ (ถอดแล้วใส่ใหม่ทุกครั้ง)
- **ไม่เก็บเดือนใน DB** — เดือนมาจากนาฬิกาตอนอ่าน cache ที่อิง `now()` เน่าเอง
- backfill วนเฉพาะดีลที่มี SO รออนุมัติ **ห้ามวนทุกดีล** (กับดัก 0110)
- 🔒 `src/lib/sales/soPendingApprovalMigration.test.mjs` อ่าน **นิยามล่าสุดในโฟลเดอร์** ของสอง
  ฟังก์ชัน — ใครคัดนิยามเก่า (0107/0108/0110 ที่ยังใช้ `orderDate`) ไปแก้ในไฟล์ใหม่ เทสต์แดง
- **mig 0360 (ใบสั่งขายย้อนหลัง · ยังไม่รัน ณ 2026-09-15)** นิยามสองฟังก์ชันนี้ใหม่ โดยเติม `origin = 'pipeline'`
  ในผลรวมทั้งสี่จุด (Actual + รออนุมัติ ของทั้งคู่) — นอกนั้นตรงกับ 0353 ทุกตัวอักษร (`historicalSalesOrderMigration.test.mjs`
  ตัดตัวกรองออกแล้วเทียบ) · ใบย้อนหลังเกิดเป็น "อนุมัติแล้ว" แต่ **ไม่เข้า** ทั้ง Actual และกองรออนุมัติ ·
  ใครคัดนิยามจาก 0353 ไปแก้ในไฟล์ใหม่ = ตัวกรองหาย ⇒ เทสต์แดง · รายละเอียด [historical-sales-orders.md](historical-sales-orders.md)

จอที่อ่านแถว `sales_orders` ตรง ๆ (หน้ารายการ/รายละเอียด SO · ตาราง SO ในดีล/โครงการ ·
ใบเสนอราคา · รายงานเป้า) ไม่ใช้ cache — คิดจากแถวด้วยตัวช่วยชุดเดียวกัน

## ตัวช่วยกลาง — ทุกจอต้องผ่านชุดนี้

| ที่ | ตัวช่วย |
|---|---|
| `lib/sales/salesOrderWorkflow.js` | `salesOrderPendingApprovalAmount(order)` · `salesOrderAmountKind(order)` → `actual` / `pending_approval` / `excluded` · `splitSalesOrderAmounts(orders)` · `dealPendingApprovalAmount(deal)` · `dealPendingApprovalCount(deal)` · `PENDING_APPROVAL_LABEL` |
| `lib/sales/dashboardMetrics.js` | `pendingApprovalAmountOf(d)` · `pendingApprovalCountOf(d)` (นับเฉพาะดีล Won) · `pendingApprovalMonthOf(d, now)` → `currentMonth(now)` เมื่อ **ยอด > 0 หรือมีใบ** (ใบ 0 บาทถูกกฎตั้งแต่ mig 0197 — นับเป็นใบทุกจอ ให้แดชบอร์ดตรงกับรายงานเป้า/หน้า SO ที่นับจากแถวใบ) |
| `lib/sales/projectRollup.js` | `rollupDeals()` คืน `pendingApproval` · `pendingApprovalCount` แยกจาก `actual` |
| `components/salesPlanning/PendingApprovalAmount.js` | ชิ้นแสดงผลเดียวทุกจอ: **"รออนุมัติ ฿X · N ใบ"** — คำว่ารออนุมัติเป็นสีสถานะ (amber เดียวกับป้ายสถานะ SO) ตัวเลขเป็นสีข้อความรอง **ไม่ใช้เขียวของ Actual** |

ชื่อช่องใน payload ทุก API = `pendingApproval` / `pendingApprovalCount` · ห้ามใช้ `pending` เปล่า ๆ
(ชนกับ financeStatus `pending` = รอบัญชีตรวจ · สถานะงวด `pending` · statusOf `pending` = รอปิดยอด)

กราฟ: `CHART_SERIES` ล็อกไว้ 3 สี ⇒ วาดรออนุมัติด้วย **สีของ Actual ที่จางลง + ลายเฉียง**
ไม่เพิ่มสีใหม่

## จอที่ครอบแล้ว (ตรวจกับโค้ด 2026-09-11)

| จอ | โชว์ตรงไหน | กติกา / ที่อยู่ |
|---|---|---|
| ภาพรวม › **ผลงานขาย** (`/sa/dashboard?tab=performance`) | แถบความคืบหน้า (ส่วนเขียวจางลายเฉียงต่อจาก Actual) · ตารางติดตาม (บรรทัดรองใต้ Actual — ไม่ใช่คอลัมน์ใหม่) · การ์ด "Actual สะสม" (บรรทัดที่สามของหมายเหตุ ไม่ตัดท้าย) · กราฟ Target/FC/Actual (แท่งซ้อนบน Actual) | API `/api/sales-planning/dashboard` ส่ง `pendingApproval*` ใน totals · byOwner · byTeam · byType (รอบแยกใน `lib/sales/pendingApprovalRollup.js` ลงถังคน/ทีมเดียวกับ Actual — ถังคน = (คน, ทีมของดีล) `lib/sales/ownerBucketKey.js` · แถวคนของ matrix คีย์ `personSliceKey`) · คีย์ cache ผูกเดือนไทย (`dashboardCacheKey`) · `buildMatrix` ถือเป็นเส้นแยก · ทบยอด · แผนที่ความร้อน · YoY · กราฟสะสม ไม่แตะ · "เดือนนี้" ของแท็บมาจาก `currentMonth()` |
| ลิ้นชักรายดีล (`DealDrillDownModal`) | metric `pendingApproval` (กดบรรทัดรองในตารางติดตาม — เปิดที่ **เดือนปัจจุบัน** เสมอ) · รายการ "ยอด Won" มีบรรทัดรองใต้ Actual | แถว "ไม่ระบุทีม" = ดีลไม่มีทีม (เดิมหลุดเป็นทั้งบริษัท) · metric ที่ไม่รู้จัก = รายการว่าง |
| **แดชบอร์ดของฉัน** | บรรทัดที่สองของหมายเหตุการ์ด "ยอดปิดได้" | `lib/sales/myDashboardTotals.js` · แถบยังเป็น 4 ช่อง · `targetGap` = เป้า − Actual |
| **รายการดีล** · รายละเอียดดีล · การ์ดดีลบนหน้าลีด | บรรทัดรองในคอลัมน์มูลค่า · ข้างยอดหัวกลุ่ม · หมายเหตุการ์ด Won · การ์ด "มูลค่าปิดจริง" · ตาราง SO สามทาง | `lib/sales/dealAmountDisplay.js` (Won = Actual · เปิด = FC · เรียงด้วยตัวเลขหลัก รออนุมัติแค่ตัดสินตอนเสมอ) · รายละเอียดดีลคิดสดจากแถว SO · คำใต้การ์ด Won ห้าชนิด (`wonDealForecastHint`: `actual` · `when_approved` · `awaiting_so` — รวมดีลที่มีแค่ใบอนุมัติ 0 บาท + คาดการณ์ > 0 ขึ้น "ใบสั่งขายที่อนุมัติยังเป็น 0 บาท" (มติ 2026-09-16 · เทสต์ล็อกให้ตรงกับ `isWonAwaitingSo` เมื่อดีลมีมูลค่า) · `legacy_no_so` = ดีลเก่าที่สร้างเป็น Won — มติ 2026-09-15 · `historical` = ดีลของใบสั่งขายย้อนหลัง) · API ลีดส่ง metadata แค่ `actualSource` / `soPendingAmount` / `soPendingCount` |
| **โครงการ** (`/sa/projects` + `ProjectDealsHub`) | หมายเหตุการ์ด Actual · บรรทัดรองในคอลัมน์ Actual · แถวดีล Won · การ์ดใบสั่งขายสามทาง | `lib/pm/projectPendingApproval.js` |
| **ใบสั่งขาย** รายการ / รายละเอียด · **ใบเสนอราคา** | การ์ด "รอตรวจอนุมัติ" · หัวกลุ่ม "Actual ฿X · รออนุมัติ ฿Y" · แถวสามทาง (คอลัมน์ชื่อ "ยอดก่อน VAT") · การ์ดสรุป/แถบท้ายรายการ/ข้อความยืนยันทุกขั้น (เดือนที่อนุมัติ = `currentMonth()`) · การ์ด DOWNSTREAM บนใบเสนอราคา | route ใบเสนอราคาเลือกใบที่ยังมีชีวิต (`isLiveSalesOrder`) แทน `maybeSingle` ที่พังเงียบเมื่อมีหลายใบ · การ์ด "สร้างใบสั่งขาย" ขึ้นเมื่อไม่มีใบที่ยังมีชีวิต (ออกใบใหม่หลังยกเลิก — mig 0169) |
| **รายงานเป้า** (`/sa/targets/report`) | ช่อง KPI ที่ 5 · บรรทัดรองใต้ขายจริงของเดือนนี้ / รายทีม / รายคน · แผง "ใบสั่งขายรออนุมัติ" (แยกจากแผงใบที่อนุมัติแล้ว · มีเฉพาะงวดที่คลุมวันนี้ · ตัวคิดย้ายไป `lib/sales/reportSummary.js` 2026-09-22) · ชีต "รออนุมัติ" ในไฟล์ Excel | ก้อน `pendingApproval` แยกจาก `actual[]` (`lib/sales/reportPendingApproval.js` อ่านแถวใบ + เจ้าของดีลปัจจุบัน + ทีมของดีล) · คีย์แถวคน = `personSliceKey` ตรงกับแถวของ route (`lib/sales/reportRows.js` — ใบอนุมัติแล้วก็ลงทีมตามดีลของใบ) · ดีลมีเจ้าของแต่ไม่ระบุทีม = บรรทัด `noTeam` คู่แถวทีม "ไม่ระบุทีม" (`team:-`) · ดีลไร้เจ้าของนับเฉพาะยอดบริษัท (footnote บอกส่วนต่าง) |

## จอที่ไม่แตะโดยตั้งใจ

- **ตัวช่วยวางเป้า** (`/sa/targets/plan`) และ **กรอกยอดย้อนหลัง** (`/sa/targets/history`) —
  เก็บยอดลง `sales_history` ถาวร ต้องเป็น Actual ล้วน
- **รายงาน FC รายหมวด (Excel)** — เป็นไฟล์ FC ล้วน (ยอดรวมไฟล์ = ผลรวม FC ล็อกด้วยเทสต์)
- **ทะเบียนการชำระ / งวด (`/finance`)** — งวดถูกตรึงตอนอนุมัติ (mig 0259) ยังไม่ใช่ลูกหนี้
- **ภาษีสรรพสามิต · คิวส่งต่อ ops · บริการ · สหมิตร** — อ่านเฉพาะใบอนุมัติแล้วโดยเจตนา

## ยังเปิดอยู่

ตัวเลข 11/09 ข้างล่างเป็นภาพ **ก่อน** มีกอง Won รอยื่น SO (รอบสอง) — ไม่ใช่สถานะวันนี้:
Won แต่ยังไม่ออก SO (33 ดีล FC 1,914,850) · มีแต่ SO ร่าง (2 ดีล 25,000) ·
มีแต่ SO ยกเลิก (5 ดีล 678,100) · SO ถูกย้อนอนุมัติรอออก Rev. — ตัวเลข ณ 2026-09-11

- 15/09: ดีล "ยังไม่ออก SO" ทั้งหมดคือดีลเก่าที่สร้างเป็น Won (35 ดีล) ซึ่งยื่น SO ไม่ได้ — ตัดออกจากกองแล้ว
  (มติ 2026-09-15) · กองที่เหลือ 7 ดีล 703,100 = มีแต่ SO ยกเลิก 5 (DL-26080214 · DL-26080235 · DL-26080399 ·
  DL-260900464 · DL-260900469) + มีแต่ SO ร่าง 2 (DL-26080303 · DL-260900448)
- ยังไม่ครอบ: ดีลที่มี SO อนุมัติบางส่วน + ส่วนที่เหลือยังร่าง (ไม่นับส่วนที่เหลือ) · แถวทีม/รายคน ม.ค.–มิ.ย. ที่ overlay ไม่ล้าง

## รอบห้า 2026-09-16 — รีเช็คเงื่อนไขทุกกองบนแท็บผลงานขาย (Target · FC Total · FC คงเหลือ · Won รอยื่น SO · SO รออนุมัติ · Actual)

ผู้ใช้ถาม *"มี deal ที่มี SO แล้ว ดันโชว์รอ SO ผิดมั้ย"* ⇒ ตรวจข้อมูลจริง + ไล่เงื่อนไขทุกกองด้วยตัวตรวจแยก 14 ชุด

**ข้อมูลจริง 16/09 — ตัวเลขไม่เพี้ยน:** cache (`wonValue` · `soPendingAmount/Count`) ตรงกับแถวใบจริงครบ 470 ดีล ·
แถวบริษัท = Σ แถวทีม = Σ แถวคน ทุกเดือนทุกช่อง · ไม่มีดีลถูกนับสองกอง · กอง Won รอยื่น SO 10 ดีล 2,100,366 มีเหตุผลครบ
(8 ดีลมีใบอนุมัติ 0 บาท · DL-26080235 ยื่นแล้วยกเลิกทั้ง 3 ใบ · DL-260900448 มีใบร่าง)

| # | ของที่พัง | แก้ที่ไหน |
|---|---|---|
| 1 | **ลิ้นชักรายดีลไม่ตรงกับตัวเลขที่กด** — ตัวเลขบนแท็บเป็นทั้งบริษัทโดยตั้งใจ แต่ลิ้นชักยิง `/api/sales-planning/deals` ที่กรองตามสิทธิ์ ⇒ AE เห็นแค่ดีลตัวเอง · Senior AE/AC แค่ทีม | **มติผู้ใช้: คงขอบเขตเดิม เขียนบอกบนจอแทน** — ลิ้นชักขึ้นบรรทัด "รายการนี้แสดงเฉพาะดีลของทีมคุณ / ที่คุณเป็นเจ้าของ — ตัวเลขบนแผงเป็นยอดทั้งบริษัท" ตัดสินจาก `salesPlanningViewScope` ตัวเดียวกับ API · ⛔ ทางเลือกที่ปฏิเสธ: ปลดขอบเขตให้ลิ้นชัก (`?scope=overview`) — รีวิวจับได้ว่าเปิดให้ ae/senior_ae/ac อ่านแถวดีลของทุกทีมครบทุกช่อง (ชื่อลูกค้า · หมายเหตุ · เหตุผลแพ้) |
| 2 | **งวดไตรมาสเปิดลิ้นชักเป็นทั้งปี** (ส่ง `month = null` ⇒ ตกกิ่งปี) และหัวลิ้นชักเขียนว่า "ทั้งปี" | `MorningBoard` ส่ง `months` = เดือนของงวด + `periodText` = คำของงวด · `periodMatcher` จับรายชื่อเดือนก่อนกิ่งปี |
| 3 | **ใบยื่นแล้วยอด 0 บาทเตะดีลหายจากทุกกอง** — กองรออนุมัติได้ 0 บาท กองรอยื่นตัดด้วย*จำนวนใบ* ⇒ มูลค่าดีลทั้งก้อนหายจากคาดจบงวด | `isWonAwaitingSo` ตัดด้วย**ยอด**อย่างเดียว (ถอด `pendingApprovalCountOf <= 0`) · หน้าดีลขึ้น "ใบสั่งขายที่ยื่นยังเป็น 0 บาท" |
| 4 | **เป้าทีมกลืนเป้ารายคน** — ทีมที่มีทั้งเป้าระดับทีมและรายคน ใช้เป้าระดับทีม ⇒ แถวทีม ≠ Σ แถวคน (ต.ค.–ธ.ค. 2026: SV/ODM ต่างทีมละ 650,000–750,000/เดือน) | **มติผู้ใช้: ยึดเป้าระดับทีมเหมือนเดิม แต่ต้องเตือนบนจอ** ⇒ API ส่ง `byTeam[].targetPersonSum` · matrix/`windowStat` พาไปถึงจอ · แถวทีมขึ้น "รายคนรวม ฿X" · ⛔ ห้ามบวกเข้า target/gap/% |
| 5 | **ยอดกรอกมือโหลดพลาด = เงียบ** — `fetchHistory` แปลง error เป็น "ไม่มีประวัติ" ⇒ ม.ค.–ก.ค. 2026 (78.5 ล้าน) กลายเป็น ฿0 ไม่มีอะไรบอก | แยก `failed` ออกจาก "ว่างจริง" + แถบเตือน `.perf-partial-warning` (รวมถึงปีก่อนที่เป็นฐาน YoY) |
| 6 | **หน้ารายการดีลอ่าน `project_tasks` ไร้ขอบเขต** — ไม่เช็ก error · `.in()` ไม่ซอย · ไม่ไล่หน้า (คอลัมน์ "ขั้นตอน" หายเงียบ) | `fetchInChunks` + `fetchAllResult` + เช็ก error (เพดาน rowcap 20→19) |
| 7 | **ล้างวันที่คาดปิด = ดีลหลุด FC ทุกเดือน** (ของจริง DL-26070088 110,000) | PATCH ปฏิเสธการล้างค่า แทนการเขียน `forecastMonth = null` |
| 8 | **เดือนที่ปิด Won คิดจากนาฬิกา UTC** — ดีลที่ยังไม่มีใบอนุมัติ ปิดช่วง 00:00–06:59 ไทย ตกไปเดือนก่อน (DB คิดเวลาไทยตั้งแต่ 0279) | `wonMonthOf` ใช้ `businessDayKey(confirmedAt)` ก่อนตัดเดือน (วันนี้ยังไม่มีดีลที่ขยับ) |

- ผลบนข้อมูลจริงหลังแก้: กองรอยื่น SO ไม่ขยับ (ยังไม่มีใบยื่นยอด 0 บาทอยู่ ณ วันนี้) · ไม่มีดีลไหนเปลี่ยนเดือน ·
  คำเตือนเป้าจะขึ้นที่ SV/ODM เดือน ต.ค.–ธ.ค. 2026
- **ไม่แก้โดยตั้งใจ** (บันทึกไว้ให้รู้ว่าเลือกแล้ว): คิว "Won รอ SO" ของแดชบอร์ดของฉัน/แบนเนอร์ใบเสนอราคา/ความพร้อมปิดโครงการ
  ยังอ่าน `handoffQueue.quotesAwaitingSalesOrder` · `overlayHistory` ล้างเส้นรอยื่นของแถวทีม/บริษัทแต่ไม่ล้างแถวคนในทีม (ยังไม่เกิดจริง) ·
  ถ้าเป้ารวมบริษัทน้อยกว่าผลรวมทีม แถว "ยังไม่ได้แยกทีม" จะติดลบ (ตอนนี้บวก 830,000–1,430,000)

## รอบหก 2026-09-16 (บ่าย) — เอกสารเป็นตัวตั้งของยอดดีล

ผู้ใช้ทักว่ากอง "Won รอยื่น SO" มีดีลที่ **ยื่น SO ไปแล้ว** ⇒ ตรวจของจริงทั้ง 9 ดีลของ ส.ค.: ยื่นจริงทุกใบ
(6 ใบอนุมัติแล้วยอด 0 บาท · 2 ใบยอดจริงถูกยกเลิกแล้วออกใบ 0 บาทแทน · 1 ใบยอดเต็มถูกยกเลิกทั้งสามใบ)

**มติ:** *"นับจากใบที่ลูกค้ารับ"* และ *"ใบ 0 บาทถูกต้องแล้ว เป็นไปได้ เพราะให้ส่วนลด 100%"*

| เรื่อง | มติ / สิ่งที่ทำ |
|---|---|
| ยอดของดีล | = **ใบเสนอราคาที่ลูกค้ารับ** (ก่อน VAT) — `accept_quotation_atomic` เขียน `projectValue` + `forecastSource='quotation'` + `forecastQuotationId` ตอนรับใบ (**mig 0361**) · เลขที่ AE กรอกมือย้ายไปเก็บที่ `forecastManualValue` ไม่ทับทิ้ง |
| ดีลเก่า | `scripts/backfill-deal-fc-from-accepted-quote.mjs` (ซ้อมก่อน · `--apply` เขียนจริง) — ซ้อมของจริง 16/09: **38 ดีล** ขึ้น 1,179,563 · ลง 2,207,858 · สุทธิ −1,028,295 (ใน 38 ใบนั้น 17 ใบมีตารางรายหมวดที่รวมได้เท่ายอดเดิม) |
| กู้คืน backfill | สำรองค่าเดิมสี่ช่องลงไฟล์ `backfill-deal-fc-<เวลา>.json` **ก่อน** เขียน · บันทึก `audit_logs` รายใบ (`before`/`after` · `entityType='sales_deal'`) · เขียนไม่ครบ = `exit 1` · รันซ้ำได้ (ใบที่ตรงแล้วถูกข้าม) · กติกาคัดดีล/ค่าที่เขียนอยู่ใน `lib/sales/dealValueBackfill` (มีเทสต์ครอบ) ไม่ใช่ในสคริปต์ |
| ย้อนรับใบ | `unaccept_quotation_atomic` **ไม่คืน** สี่ช่องที่ 0361 เขียน ⇒ route ย้อนรับใบเรียก `applyForecastSource` ต่อท้าย ให้ดีลที่กลับมาเปิดเลือกใบใหม่ตามกติกาของดีลเปิด (คิดยอดไม่สำเร็จ = เตือนใน log ไม่ล้มคำขอ — การย้อนรับใบเสร็จไปแล้ว) |
| กอง Won รอยื่น SO | ใบที่ **อนุมัติแล้ว** (แม้ 0 บาท) = จบ ออกจากกอง · ใบที่ **ยื่นแล้วยอด 0** ยังอยู่ (ยังไม่มีเงินเข้าที่ไหนเลย) ⇒ ส.ค. 2,075,366 → 637,000 · ทั้งกองเหลือ 2 ดีล ฿632,000 (DL-26080235 ททท 607,000 · DL-260900448 25,000) |
| หน้าดีล | ใบอนุมัติ 0 บาท ขึ้น "ใบสั่งขายที่อนุมัติเป็น 0 บาท (ส่วนลด 100%)" · ใบที่ยื่นยอด 0 ขึ้น "ใบสั่งขายที่ยื่นยังเป็น 0 บาท" |
| การ์ดรายหมวดบนหน้าดีล | หัวการ์ด "รวม" = **ผลบวกของแถวในตารางนั้น** ไม่ใช่ยอดดีล (เดิมเขียน `projectValue` ⇒ หัวขัดกับแถวของตัวเองทันทีที่ยอดดีลเดินตามใบ) · ไม่ตรงกันเมื่อไหร่ ขึ้นบรรทัดบอกยอดดีลตอนนี้ **พร้อมที่มาตามจริง** — ใบที่ลูกค้ารับ / ใบที่ FC เดินตาม (ดีลเปิด) / ไม่ระบุ ถ้าไม่รู้ที่มา |
| รายงาน FC รายหมวด (Excel) | 🐞 อ่านบรรทัดสินค้าจาก **ใบที่ FC ชี้** (ใบที่อนุมัติยอดต่ำสุด) ⇒ วางแผนผลิตตามใบที่ลูกค้าไม่ได้ซื้อได้ · แก้ให้ **ใบที่ลูกค้ารับมาก่อน** (`lib/sales/reportQuotation`) · `forecastBreakdownOfDeal` ถือว่าดีลที่มี `metadata.acceptedQuotationId` เดินตามบรรทัดของใบ แม้ `forecastSource` ยังเป็น `manual` (129 จาก 171 ดีล Won เป็นแบบนั้น) · ดีลยอด 0 ที่มีใบ **ไม่ถูกตัดออกจากไฟล์** — ของยังต้องผลิต ยอดเงิน 0 ไม่กระทบยอดรวมไฟล์ |

- ที่ตรวจแล้วดีอยู่แล้ว: **Actual = เอกสารล้วน** — ทริกเกอร์คิดใหม่ทุกครั้งจาก Σ `actualAmount` ของ SO สถานะ approved
  สาย pipeline ⇒ ยอดที่พิมพ์เองอยู่ไม่ได้ · หลายใบบวกถูก · ของจริงไม่มีดีลไหนมีใบ accepted เกิน 1 หรือ SO อนุมัติเกิน 1
- 🪤 กับดักที่เพิ่งพบ: FC ที่ "เดินตามใบ" ในระบบเดิมผูกกับ **ใบที่อนุมัติภายในซึ่งยอดต่ำสุด** ไม่ใช่ใบที่ลูกค้ารับ ·
  ตอนรับใบไม่มีใครเขียน `projectValue` ⇒ ดีล Won ถือเลขที่บังเอิญอยู่ตอนนั้นแล้วแช่แข็ง (38 ดีลไม่ตรงใบ)
- 🔴 `forecastManualValue` ต้องเขียนด้วย **`CASE WHEN "forecastSource" = 'manual' THEN "projectValue" ELSE "forecastManualValue" END`**
  ไม่ใช่ `COALESCE` — คอลัมน์นี้ `NOT NULL DEFAULT 0` (mig 0337) กิ่งสำรองของ `COALESCE` จึงไม่มีวันทำงาน
  แล้วเลขที่ AE กรอกมือหายเงียบทั้งใน RPC และใน backfill (เทสต์ตรึงไว้ทั้งสองที่)
- ยอดที่ใช้ทุกจุดคือ `lib/sales/quotationWonAmount` = `totalAmount − vatAmount` (ก่อน VAT **หลัง** ส่วนลด)
  ⛔ ห้ามใช้ `subtotal` — นั่นคือยอด**ก่อน**ส่วนลด
- backfill **ไม่เขียนแถว `sales_deal_forecasts`** — ตารางนั้นเป็นประวัติ "ตอนนั้นพยากรณ์ไว้เท่าไหร่"
  ที่ใช้วัดความแม่นยำ ไม่ใช่ยอดปัจจุบัน · ร่องรอยของการแก้ย้อนหลังอยู่ใน `audit_logs` + ไฟล์สำรอง
- 🪤 ขอบที่รู้ไว้: ดีลที่ AE **ปัก** ที่มา FC ไว้เป็น manual แล้วรับใบ ⇒ 0361 เปลี่ยนที่มาเป็น `quotation`
  โดยหมุดยังอยู่ · ระหว่างเป็น Won ไม่มีผล (FC แช่แข็ง) · ถ้าย้อนรับใบทีหลัง ดีลกลับมาเปิดโดยเดินตามใบ
  ไม่ใช่เลขกรอก — AE เลือกใหม่ได้ที่การ์ดที่มาของ FC (`resolveForecastSource` ไม่มีทางชี้ใบที่ไม่มีสิทธิ์)
- ⚠️ mig 0361 **ไม่ backfill ในตัวเอง** โดยตั้งใจ — ไฟล์นี้ก๊อปไปรันมือทั้งก้อน จึงต้องเห็นรายการที่จะเปลี่ยน
  ก่อนเขียน · เทสต์เทียบ **ทั้งไฟล์** 0361 กับ 0284 (ไม่ใช่แค่ตัวฟังก์ชัน) เพื่อกันคำสั่งที่แอบต่อท้ายหลัง `$$;`

### ลงมือจริงแล้ว (2026-09-16 เย็น)

| ขั้น | ผล |
|---|---|
| โค้ด | PR #1736 squash `85849279` · ขึ้น production (ยืนยันจาก `/api/version`) |
| mig 0361 | รันมือบน Supabase SQL Editor แล้ว — ยืนยันด้วย `SELECT prosrc LIKE '%"projectValue" = v_won_value%' FROM pg_proc WHERE proname = 'accept_quotation_atomic';` = `true` |
| backfill | `--apply` แล้ว **38/38 ใบ** (ขึ้น 1,179,563 · ลง 2,207,857.9 · สุทธิ −1,028,294.9) · รันซ้ำตอบ "ต้องแก้ 0 ใบ" · `audit_logs` 38 แถว (`actorName = 'backfill-deal-fc-from-accepted-quote'`) |
| ไฟล์สำรอง | `~/ss-team/archive/backfill/backfill-deal-fc-2026-09-16T14-17-53-294Z.json` — **ทางกู้คืนเดียวนอกจาก `audit_logs`** · PR #1738 (`d7400405`) ย้ายที่เก็บตั้งต้นของสคริปต์ออกนอกรีโปถาวร + `.gitignore` กันอีกชั้น |

### ผลจริงบนแท็บผลงานขาย — ไม่ใช่ที่คาดไว้

เทียบค่าเดิมจากไฟล์สำรองกับฐานหลัง `--apply` ด้วยโค้ดชุดที่ deploy อยู่:

| เดือน 2026 | FC คงเหลือ | Won รอยื่น SO | **ส่วนต่าง Won (คาด − จริง)** |
|---|---|---|---|
| ส.ค. | 0 → 0 | 607,000 → 607,000 | **1,795,090 → 607,000** |
| ก.ย. | 14,334,582 → เท่าเดิม | 269,500 → เท่าเดิม | **674,205 → 834,000** |
| ต.ค.–ธ.ค. | เท่าเดิมทุกเดือน | 0 → 0 | 0 → 0 |

🪤 **ระหว่างทางเคยสรุปผิดว่า "ยอดคาดการณ์บริษัทลดลง 1.03 ล้าน เห็นทันทีบนแดชบอร์ด"** — ไม่จริง
ดีลที่ backfill แตะทั้ง 38 ใบเป็น **Won ทั้งหมด** (ดีลเปิด 0 ใบ · อยู่ในกองรอยื่น SO 0 ใบ) ⇒
**FC คงเหลือ · กองรอยื่น SO · `projected` ไม่ขยับสักบาท** · ที่ขยับคือ **ส่วนต่างระหว่างคาดการณ์กับปิดจริงของดีล Won**
(ส.ค. ดีขึ้น 1,188,090 · ก.ย. แย่ลง 159,795 · สุทธิ −1,028,295 ตรงกับยอด backfill พอดี) ซึ่งเป็นผลที่ถูกต้อง:
ตัวเลขคาดการณ์ของดีลที่ปิดไปแล้วเดินมาตรงกับเอกสารที่ลูกค้ารับ ส่วนต่างจึงสะท้อนของจริงแทนเลขที่กรอกมือ

### ยามที่รันกับฐานจริงหลังขึ้น (ผ่านทั้งหมด)

- Σ ทีม = Σ คน = ยอดบริษัท: **0 ผิด จาก 48 ช่อง** (12 เดือน × Actual / FC / รอยื่น SO / Lost)
- ดีลนับซ้อนกองรอยื่น SO + กองรออนุมัติ: 0 · ดีลที่เป็นทั้ง Won และเปิด: 0 · ดีลที่มี Actual > 0 แต่ยังค้างในกองรอยื่น SO: 0
- ตัวเลขปี 2026 ณ 16/09: Actual 19,561,162 (ส.ค. 15,563,448 · ก.ย. 3,997,714) · FC คงเหลือ 45,040,283 ·
  Won รอยื่น SO 876,500 · รออนุมัติ 522,500 (4 ใบ)
- กอง "Won รอยื่น SO" เหลือ **4 ดีล ฿876,500**: DL-26080235 607,000 (ODM · ไม่มีใบที่ลูกค้ารับ จึงยัง `manual`) ·
  DL-26090004 234,000 (SV) · DL-260900448 25,000 (ODM) · DL-260900466 10,500 (SV)

### คิว "Won รอ SO" ไม่ต้องแก้ (ตรวจ 16/09 หลัง backfill)

`quotesAwaitingSalesOrder` กับ `isWonAwaitingSo` **ถามคนละคำถามโดยตั้งใจ** — ทำให้ "ตรงกัน" คือทำให้คิวโกหก:

| | ถามว่าอะไร | ตัวตัดสิน |
|---|---|---|
| คิว "Won รอ SO" | ยังไม่มีใครสร้าง SO (ลูกศรที่ 1) | `isLiveSalesOrder` ต้องตรงกับด่าน `create_sales_order_draft` เป๊ะ: `status <> 'cancelled' AND "supersededById" IS NULL` — เข้มกว่า DB = ซ่อนใบที่กดได้จริง · หลวมกว่า DB = ชวนกดแล้วเด้ง 409 |
| กอง "Won รอยื่น SO" | ยังไม่มีเงินอยู่ที่ไหนเลย | `!dealHasApprovedSalesOrder && pendingApprovalAmountOf <= 0 && projectValue > 0` |

ของจริง 16/09: คิว 3 ดีล · กอง 4 ดีล — ต่างกัน 5 ใบ ตรวจทีละใบแล้วถูกทุกใบ
- **อยู่ในคิว ไม่อยู่ในกอง**: DL-26080214 · DL-260900469 — SO ถูกยกเลิก ต้องออกใบใหม่จริง · ไม่อยู่ในกองเพราะ FC = 0 (กติกา "มูลค่าดีล 0 ไม่นับ" ของรอบสี่ ข้อ ②)
- **อยู่ในกอง ไม่อยู่ในคิว**: DL-260900448 · DL-260900466 มี SO **ร่าง** · DL-26090004 มี SO **`approval_revoked`** ฿234,000 —
  ทั้งสามใบ "สร้างแล้ว" งานถัดไปคือยื่น/ออก Rev. ไม่ใช่สร้างใบ ⇒ ไม่ควรอยู่ในคิวสร้าง SO และไม่ได้หายไปไหน
  `projectCloseReadiness.salesOrdersInFlight` รับไว้ครบ (`SALES_ORDER_IN_FLIGHT_STATUSES` รวม `approval_revoked`)
- ป้ายบนจอแยกคำอยู่แล้ว: คิวเขียน "ออกใบสั่งขาย / Won แล้ว รอออก SO" · กองเขียน "Won รอยื่น SO" — **ออก** ≠ **ยื่น**

🪤 **บทเรียน: โน้ตที่เขียนใต้กติกาเก่ากลายเป็นเท็จได้เมื่อกติกาถูกกลับ** — ข้อกังวลเรื่องคิวนี้เขียนไว้ใต้กติกา ① ของรอบสี่
("ออกจากกองเมื่อ Actual > 0") ซึ่งรอบหกกลับในวันเดียวกัน · ก่อนอ้างโน้ตเก่า ต้องตรวจกับโค้ด + ฐานจริงก่อนเสมอ

🪤 **ด่าน `check:thaiwrap` พังได้จากการ *ลบ* คำ ไม่ใช่แค่เพิ่ม** — ตัวนับตัดสินจากความถี่คำทั่วคอร์ปัส ·
ตัดคำว่า "รออนุมัติ" ออกจากทูลทิปหนึ่งที่ ทำให้ `freq("รอบ")` แซง `freq("รอ")` แล้วรอยต่อที่ถูกอยู่แล้วของ
**ข้อความอื่น 8 จุด** (เช่น `รอ|บัญชีกดปิดใบ`) ถูกฟ้อง 31/23 · วัด baseline บน `origin/main` ก่อนสรุปว่าของตัวเองพัง

🪤 **ยิง `gh workflow run "Deploy to production"` ทันทีหลัง merge = ล้ม** (`CI for main is 'pending', not success — refusing to deploy`)
ต้องรอ CI ของ **main** (คอมมิต squash) เขียวก่อน
