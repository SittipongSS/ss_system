# ยอด SO "รออนุมัติ" — โชว์ได้ แต่แยกจาก Actual

> สถานะ: **เสร็จสมบูรณ์** · ตรวจกับโค้ดเมื่อ 2026-09-15 · รอบแรกขึ้น production 2026-09-11 (#1703 · `099f4a3f`) · รอบสอง (คาดจบงวด + Won รอยื่น SO + ทีมตามดีล) ขึ้น production 2026-09-15 · mig 0353 รันบนฐานจริงแล้ว (backfill 8 ดีล รวม 993,000 · wonValue คงเป็น 0 ครบ)

## ที่มา

ผู้ใช้แจ้ง 2026-09-11: *"SO ที่รออนุมัติ มันกลายเป็น 0 อยากให้โชว์ยอดด้วย แต่แยกให้รู้ว่า
รออนุมัติ กับ Actual แล้ว ทั้งระบบ"*

ดีลกลายเป็น **Won** ตั้งแต่กดรับใบเสนอราคา (mig 0284) แต่ **Actual** นับเฉพาะใบสั่งขายที่
**อนุมัติแล้ว** (mig 0110 / 0279) ⇒ ช่วงที่ SO ยื่นแล้วรอ AE Supervisor อนุมัติ ยอดของดีล
หายไปจากทุกจอ:

- หลุดจาก **FC คงเหลือ** เพราะดีล Won แล้ว
- ยังไม่เข้า **Actual** เพราะใบยังไม่อนุมัติ
- เหลือร่องรอยแค่ใน FC Total ซึ่งไม่มีใครอธิบายได้ว่าส่วนต่างมาจากไหน

ของจริงวันที่แจ้ง: SO รออนุมัติ 8 ใบ รวม 993,000 บาท (ก่อน VAT) · ทุกใบอยู่บนดีล Won ที่ Actual = 0

## มติ

| เรื่อง | มติ |
|---|---|
| โชว์ไหม | โชว์ทุกจอที่โชว์ Actual — เป็น **ตัวเลขแยก** มีคำว่า "รออนุมัติ" กำกับเสมอ |
| นับอะไร | เฉพาะ `status = 'pending_approval'` · ร่าง / ตีกลับ / ย้อนอนุมัติ / ออก Rev. แล้ว / ยกเลิก **ไม่ใช่** รออนุมัติ |
| ยอดไหน | `actualAmount` (ก่อน VAT) ตัวเดียวกับ Actual — ห้ามใช้ `totalAmount` |
| เดือนไหน | **เดือนปัจจุบัน (เวลาไทย) เสมอ** — อนุมัติย้อนหลังไม่ได้ ถ้าอนุมัติวันนี้ Actual ก็ลงเดือนนี้ · ใบที่ค้างข้ามเดือนเลื่อนมาอยู่เดือนใหม่เอง · เดือนที่ปิดแล้ว/ปีก่อนไม่มีวันเห็นยอดนี้ |
| นับให้ใคร | ดีล Won เท่านั้น (ดีลที่ยังเปิดมี FC อยู่ใน FC คงเหลือแล้ว) · รายคนตามเจ้าของดีล **ปัจจุบัน** (`sales_orders.ownerId` ว่างจนกว่าจะอนุมัติ — mig 0294) · ทีม = **ทีมของดีล** (`sales_deals.team` — มติ 2026-09-14 "ทีมตามดีล" ไม่ใช่ทีมในบัญชีเจ้าของ ⇒ แถวคน = (คน, ทีมของดีล) คนหลายทีมได้แถวละทีม) |

### ⛔ ห้ามบวกเข้า Actual — ทุกกรณี

wonValue · won · actual[] · เป้า/% ปิดได้/ขาด-เกิน · variance · ยอดทบ (carry) · YoY ·
แผนที่ความร้อน · ยอดที่ตัวช่วยวางเป้า (`/api/sales-planning/history`) และปุ่ม
"เติมยอดจากระบบ" (`/sa/targets/history`) **คัดลอกไปเก็บใน `sales_history` ถาวร** —
ถ้ายอดรออนุมัติหลุดเข้าไป มันจะกลายเป็น Actual ปลอมที่ทับยอดจริงตลอดไป

ข้อยกเว้นเดียวที่ตั้งใจ: **ยอดคาดการณ์ "คาดจบงวด" (`windowStat.projected` ของแท็บผลงานขาย)** —
ดูหัวข้อถัดไป · % · ขาด/เกิน · ทบยอด · ต้องทำเฉลี่ย/เดือน ที่อยู่ข้างกันยังคิดจาก Actual ล้วน

## ยอดคาดการณ์ "คาดจบงวด" + "Won รอยื่น SO" (มติผู้ใช้ 2026-09-14)

> 🐞 คำอธิบายรอบ 11/09 ผิดสามจุด — ตรวจซ้ำกับจอจริง 14/09 แล้วผู้ใช้เลือกใหม่ (ข้อ ก)

**คาดจบงวด = Actual + รออนุมัติ + Won รอยื่น SO + FC คงเหลือ** · คาดขาด = ต้องปิด − คาดจบงวด

| กอง | นับอะไร | เดือน |
|---|---|---|
| Actual | SO อนุมัติแล้ว | เดือนที่อนุมัติ |
| รออนุมัติ | SO `pending_approval` | เดือนปัจจุบันเวลาไทย |
| **Won รอยื่น SO** | ดีล Won ที่ **ยังไม่มี SO อนุมัติ และไม่มี SO รออนุมัติ** (ยังไม่ออก · ร่าง · ตีกลับ/ดึงกลับ · มีแต่ใบยกเลิก) · มูลค่าดีลเต็ม | เดือนที่ปิด Won (`wonMonthOf` ถังเดียวกับ FC Total) |
| FC คงเหลือ | ดีลที่ยังเปิด · **มูลค่าดีลเต็ม ไม่ถ่วงโอกาสปิด** (ช่องชื่อ `weighted` ในโค้ดหลอก) | เดือน FC |

- **ทำไมต้องมี Won รอยื่น SO**: รับใบเสนอราคา = ดีล Won ทันที (mig 0284) ⇒ หลุดจาก FC คงเหลือ
  แต่ SO เกิดเป็นร่าง (0285) ⇒ ไม่อยู่ในรออนุมัติ/Actual ⇒ คาดขาดพุ่งเต็มมูลค่าดีลจนกว่าจะยื่น SO
  (ของจริง 14/09: 42 ดีล FC 2,659,950 — ไม่มี SO 35 · มีแต่ร่าง 2 · มีแต่ใบยกเลิก 5)
- "มี SO อนุมัติแล้ว" ดูจาก `metadata.wonMonth` ไม่ใช่ยอด Actual > 0 — ใบอนุมัติ 0 บาทมีจริง 28 ดีล
- เดือนที่บริษัทกรอก Actual มือ (`sales_history`) `overlayHistory` ล้างเส้น Won รอยื่น SO ในช่องที่ทับ —
  ยอดของดีลพวกนั้นอยู่ในตัวเลขที่กรอกแล้ว
- ข้อความ "คาดจบงวด … — คาดขาด / คาดถึงเป้า" ขึ้นใต้แถบบริษัทเสมอในงวดที่ยังไม่จบ พร้อมบรรทัดบอกฐาน ·
  แถบเล็กในตารางติดตามบอกค่าเดียวกันผ่าน title/aria (งวดที่จบแล้วไม่พูดคาดการณ์)
- ข้อจำกัดที่รู้: ดีลที่มี SO อนุมัติบางส่วน + ส่วนที่เหลือยังร่าง ไม่นับส่วนที่เหลือ · ดีลเดินข้ามเดือน
  ได้ (Won เดือนหนึ่ง → ยื่น SO อีกเดือน) หน้าต่างรายเดือนจึงเห็นยอดย้ายเดือน ส่วนรายปีคงที่
- ตัวช่วย: `dashboardMetrics` (`isWonAwaitingSo` · `wonAwaitingSoAmountOf` · `wonAwaitingSoMonthOf` ·
  `WON_AWAITING_SO_LABEL`) · API ช่อง `wonAwaitingSo` / `wonAwaitingSoCount` · ลิ้นชัก metric `wonAwaitingSo`

## ที่เก็บข้อมูล

**mig 0353** — trigger เดิมสองตัวเขียน cache เพิ่มสองคีย์ใน `sales_deals.metadata`

| คีย์ | ความหมาย |
|---|---|
| `soPendingAmount` | Σ `actualAmount` ของ SO รออนุมัติของดีล |
| `soPendingCount` | จำนวนใบ |

- ไม่มี SO รออนุมัติ = **ไม่มีคีย์** (ถอดทิ้ง ไม่เขียน 0 ค้างทุกดีล) · JS อ่านคีย์ที่ไม่มีเป็น 0
  ⇒ โค้ดขึ้น production ก่อนรัน migration ได้โดยไม่มีอะไรพัง
- `sync_sales_order_actual` กับ `enforce_sales_order_actual_on_deal` ต้องแก้ **คู่กันเสมอ**
  enforce เป็น BEFORE UPDATE ที่ไม่มีเงื่อนไข = คนเขียนคนสุดท้าย และ PATCH ดีลเอา metadata
  ทั้งก้อนของหน้าจอไป merge ทับ — enforce จึงเป็น "เจ้าของ" สองคีย์นี้ (ถอดแล้วใส่ใหม่ทุกครั้ง)
- **ไม่เก็บเดือนใน DB** — เดือนมาจากนาฬิกาตอนอ่าน cache ที่อิง `now()` เน่าเอง
- backfill วนเฉพาะดีลที่มี SO รออนุมัติ **ห้ามวนทุกดีล** (กับดัก 0110)
- 🔒 `src/lib/sales/soPendingApprovalMigration.test.mjs` อ่าน **นิยามล่าสุดในโฟลเดอร์** ของสอง
  ฟังก์ชัน — ใครคัดนิยามเก่า (0107/0108/0110 ที่ยังใช้ `orderDate`) ไปแก้ในไฟล์ใหม่ เทสต์แดง

จอที่อ่านแถว `sales_orders` ตรง ๆ (หน้ารายการ/รายละเอียด SO · ตาราง SO ในดีล/โครงการ ·
ใบเสนอราคา · รายงานเป้า) ไม่ใช้ cache — คิดจากแถวด้วยตัวช่วยชุดเดียวกัน

## ตัวช่วยกลาง — ทุกจอต้องผ่านชุดนี้

| ที่ | ตัวช่วย |
|---|---|
| `lib/sales/salesOrderWorkflow.js` | `salesOrderPendingApprovalAmount(order)` · `salesOrderAmountKind(order)` → `actual` / `pending_approval` / `excluded` · `splitSalesOrderAmounts(orders)` · `dealPendingApprovalAmount(deal)` · `dealPendingApprovalCount(deal)` · `PENDING_APPROVAL_LABEL` |
| `lib/sales/dashboardMetrics.js` | `pendingApprovalAmountOf(d)` · `pendingApprovalCountOf(d)` (นับเฉพาะดีล Won) · `pendingApprovalMonthOf(d, now)` → `currentMonth(now)` เมื่อ **ยอด > 0 หรือมีใบ** (ใบ 0 บาทถูกกฎตั้งแต่ mig 0197 — นับเป็นใบทุกจอ ให้แดชบอร์ดตรงกับรายงานเป้า/หน้า SO ที่นับจากแถวใบ) |
| `lib/sales/projectRollup.js` | `rollupDeals()` คืน `pendingApproval` · `pendingApprovalCount` แยกจาก `actual` |
| `components/salesPlanning/PendingApprovalAmount.js` | ชิ้นแสดงผลเดียวทุกจอ: **"รออนุมัติ ฿X · N ใบ"** — คำว่ารออนุมัติเป็นสีสถานะ (amber เดียวกับป้ายสถานะ SO) ตัวเลขเป็นสีข้อความรอง **ไม่ใช้เขียวของ Actual** |

ชื่อช่องใน payload ทุก API = `pendingApproval` / `pendingApprovalCount` · ห้ามใช้ `pending` เปล่า ๆ
(ชนกับ financeStatus `pending` = รอบัญชีตรวจ · สถานะงวด `pending` · statusOf `pending` = รอปิดยอด)

กราฟ: `CHART_SERIES` ล็อกไว้ 3 สี ⇒ วาดรออนุมัติด้วย **สีของ Actual ที่จางลง + ลายเฉียง**
ไม่เพิ่มสีใหม่

## จอที่ครอบแล้ว (ตรวจกับโค้ด 2026-09-11)

| จอ | โชว์ตรงไหน | กติกา / ที่อยู่ |
|---|---|---|
| ภาพรวม › **ผลงานขาย** (`/sa/dashboard?tab=performance`) | แถบความคืบหน้า (ส่วนเขียวจางลายเฉียงต่อจาก Actual) · ตารางติดตาม (บรรทัดรองใต้ Actual — ไม่ใช่คอลัมน์ใหม่) · การ์ด "Actual สะสม" (บรรทัดที่สามของหมายเหตุ ไม่ตัดท้าย) · กราฟ Target/FC/Actual (แท่งซ้อนบน Actual) | API `/api/sales-planning/dashboard` ส่ง `pendingApproval*` ใน totals · byOwner · byTeam · byType (รอบแยกใน `lib/sales/pendingApprovalRollup.js` ลงถังคน/ทีมเดียวกับ Actual — ถังคน = (คน, ทีมของดีล) `lib/sales/ownerBucketKey.js` · แถวคนของ matrix คีย์ `personSliceKey`) · คีย์ cache ผูกเดือนไทย (`dashboardCacheKey`) · `buildMatrix` ถือเป็นเส้นแยก · ทบยอด · แผนที่ความร้อน · YoY · กราฟสะสม ไม่แตะ · "เดือนนี้" ของแท็บมาจาก `currentMonth()` |
| ลิ้นชักรายดีล (`DealDrillDownModal`) | metric `pendingApproval` (กดบรรทัดรองในตารางติดตาม — เปิดที่ **เดือนปัจจุบัน** เสมอ) · รายการ "ยอด Won" มีบรรทัดรองใต้ Actual | แถว "ไม่ระบุทีม" = ดีลไม่มีทีม (เดิมหลุดเป็นทั้งบริษัท) · metric ที่ไม่รู้จัก = รายการว่าง |
| **แดชบอร์ดของฉัน** | บรรทัดที่สองของหมายเหตุการ์ด "ยอดปิดได้" | `lib/sales/myDashboardTotals.js` · แถบยังเป็น 4 ช่อง · `targetGap` = เป้า − Actual |
| **รายการดีล** · รายละเอียดดีล · การ์ดดีลบนหน้าลีด | บรรทัดรองในคอลัมน์มูลค่า · ข้างยอดหัวกลุ่ม · หมายเหตุการ์ด Won · การ์ด "มูลค่าปิดจริง" · ตาราง SO สามทาง | `lib/sales/dealAmountDisplay.js` (Won = Actual · เปิด = FC · เรียงด้วยตัวเลขหลัก รออนุมัติแค่ตัดสินตอนเสมอ) · รายละเอียดดีลคิดสดจากแถว SO · API ลีดส่ง metadata แค่ `actualSource` / `soPendingAmount` / `soPendingCount` |
| **โครงการ** (`/sa/projects` + `ProjectDealsHub`) | หมายเหตุการ์ด Actual · บรรทัดรองในคอลัมน์ Actual · แถวดีล Won · การ์ดใบสั่งขายสามทาง | `lib/pm/projectPendingApproval.js` |
| **ใบสั่งขาย** รายการ / รายละเอียด · **ใบเสนอราคา** | การ์ด "รอตรวจอนุมัติ" · หัวกลุ่ม "Actual ฿X · รออนุมัติ ฿Y" · แถวสามทาง (คอลัมน์ชื่อ "ยอดก่อน VAT") · การ์ดสรุป/แถบท้ายรายการ/ข้อความยืนยันทุกขั้น (เดือนที่อนุมัติ = `currentMonth()`) · การ์ด DOWNSTREAM บนใบเสนอราคา | route ใบเสนอราคาเลือกใบที่ยังมีชีวิต (`isLiveSalesOrder`) แทน `maybeSingle` ที่พังเงียบเมื่อมีหลายใบ · การ์ด "สร้างใบสั่งขาย" ขึ้นเมื่อไม่มีใบที่ยังมีชีวิต (ออกใบใหม่หลังยกเลิก — mig 0169) |
| **รายงานเป้า** (`/sa/targets/report`) | ช่อง KPI ที่ 5 · บรรทัดรองใต้ขายจริงของเดือนนี้ / รายทีม / รายคน · การ์ด "ใบสั่งขายรออนุมัติ" | ก้อน `pendingApproval` แยกจาก `actual[]` (`lib/sales/reportPendingApproval.js` อ่านแถวใบ + เจ้าของดีลปัจจุบัน + ทีมของดีล) · คีย์แถวคน = `personSliceKey` ตรงกับแถวของ route (`lib/sales/reportRows.js` — ใบอนุมัติแล้วก็ลงทีมตามดีลของใบ) · ดีลมีเจ้าของแต่ไม่ระบุทีม = บรรทัด `noTeam` คู่แถวทีม "ไม่ระบุทีม" (`team:-`) · ดีลไร้เจ้าของนับเฉพาะยอดบริษัท (footnote บอกส่วนต่าง) |

## จอที่ไม่แตะโดยตั้งใจ

- **ตัวช่วยวางเป้า** (`/sa/targets/plan`) และ **กรอกยอดย้อนหลัง** (`/sa/targets/history`) —
  เก็บยอดลง `sales_history` ถาวร ต้องเป็น Actual ล้วน
- **รายงาน FC รายหมวด (Excel)** — เป็นไฟล์ FC ล้วน (ยอดรวมไฟล์ = ผลรวม FC ล็อกด้วยเทสต์)
- **ทะเบียนการชำระ / งวด (`/finance`)** — งวดถูกตรึงตอนอนุมัติ (mig 0259) ยังไม่ใช่ลูกหนี้
- **ภาษีสรรพสามิต · คิวส่งต่อ ops · บริการ · สหมิตร** — อ่านเฉพาะใบอนุมัติแล้วโดยเจตนา

## ยังเปิดอยู่

ดีล Won ที่ Actual เป็น 0 ด้วยเหตุอื่น ยังดูเหมือนเดิม (ไม่อยู่ในขอบเขตคำขอนี้):
Won แต่ยังไม่ออก SO (33 ดีล FC 1,914,850) · มีแต่ SO ร่าง (2 ดีล 25,000) ·
มีแต่ SO ยกเลิก (5 ดีล 678,100) · SO ถูกย้อนอนุมัติรอออก Rev. — ตัวเลข ณ 2026-09-11
