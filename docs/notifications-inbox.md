# กล่องแจ้งเตือนรายคน — กระดิ่ง + หน้า "ดูทั้งหมด"

> สถานะ: **เสร็จสมบูรณ์** · ตรวจกับโค้ดเมื่อ 2026-08-12 · หน้า `/notifications` + ถอดท่อ Google Chat
> ตารางและกติกาผู้รับอยู่ที่ mig `0185_notifications.sql` (มติผู้ใช้ 2026-07-28 ข้อ 14–15)

> ⭐ **มติผู้ใช้ 2026-08-12: "เอา webhook ออก ใช้กระดิ่งกับป้ายตัวเลขก็พอ"**
> Google Chat ถูกถอดออกทั้งระบบ (mig 0236) ⇒ กล่องนี้คือ**ช่องทางแจ้งเตือนเดียวที่เหลือ**
> อะไรที่ไม่ได้ลงที่นี่ = ไม่มีใครรู้ · ของที่ยอมให้เงียบอยู่ใน §7

ตัวตารางกับกฎ "ใครได้รับ" ไม่ได้เปลี่ยนในรอบนี้ — เอกสารนี้บันทึกว่า **ของที่มีอยู่ถูกอ่าน
ยังไงบนหน้าจอ** และช่องโหว่ที่ตรวจเจอจากข้อมูลจริง

---

## 1. มีแจ้งเตือนอะไรบ้าง

สองแหล่ง เขียนผ่าน `src/lib/notifications.js` เท่านั้น:

| ที่มา | `kind` | ยิงเมื่อ |
|---|---|---|
| เธรดของ entity (`appendUpdate` → `notifyThreadUpdate`) | `thread_update` | มีคนโพสต์ในเธรดของ 13 entity ที่ประกาศใน `UPDATE_ENTITIES` |
| จุดส่งมอบลีด (`notifyLeadHandoff`) | `lead_create` · `lead_screen` · `lead_assign` · `lead_bounce` | ลีดเปลี่ยนมือ |
| สรุปเช้า (`/api/cron/daily-digest`) | `lead_overdue` | ลีดค้างเกิน SLA 1 วันทำการ |

ผู้รับ = คนที่ผูกกับแถวแม่ (`updateAccess`) + คนที่เคยโพสต์ในเธรดนั้น + คนที่ถูก `@`
− คนที่เพิ่งลงมือเอง · **ห้ามใช้ "ทุกคนในฝ่าย"** (มติ 14 — ซ้ำกับ Chat webhook แล้วกล่องจะตาย)

## 2. นับยังไง

- **ตัวเลขบนกระดิ่ง** = `COUNT(*) WHERE userId = ฉัน AND readAt IS NULL` — ทั้งหมด ไม่จำกัด
  (แสดงเกิน 99 เป็น `99+`)
- **กล่องในกระดิ่ง** = 30 แถวล่าสุด ปนกันทั้งอ่านแล้วและยังไม่อ่าน
- **หน้า `/notifications`** = 30 แถวต่อหน้า กดโหลดเพิ่มได้จนหมด · กรอง "ทั้งหมด / ยังไม่อ่าน"
  ได้ที่ฐานข้อมูล ไม่ใช่กรองหลังดึงมา

⚠️ **ตัวเลขบนกระดิ่งกับสิ่งที่กล่องแสดงไม่เท่ากันโดยธรรมชาติ** — ตรวจข้อมูลจริง 2026-08-12
พบ 6 คนที่ยังไม่อ่านเกิน 30 แถว คนหนึ่ง 173 แถว ⇒ ก่อนมีหน้า "ดูทั้งหมด" ของที่เหลือ
**เข้าไม่ถึงเลย** เหลือทางเดียวคือกด "อ่านทั้งหมด" ซึ่งล้างของที่ยังไม่ได้อ่านทิ้งไปด้วย
หน้านี้มีไว้ปิดช่องนั้น

## 3. หน้าจอที่มี

| ที่ | ไฟล์ | ทำอะไรได้ |
|---|---|---|
| กระดิ่งบนแถบบน (ทุกหน้า) | `components/notifications/NotificationBell.js` | 30 แถวล่าสุด · อ่านทั้งหมด · **ดูทั้งหมด** (เกาะล่างกล่อง) |
| หน้าเต็ม | `app/notifications/page.js` | กรอง · จัดกลุ่มตามวัน · โหลดเพิ่ม · อ่านทั้งหมด · อ่านทีละแถว |

- ดึงข้อมูล: ตอนโหลด · ตอนเปิดกล่อง · ทุก 2 นาที · **ไม่มี realtime โดยเจตนา**
  (งานวัดกันเป็นชั่วโมง และทุกแท็บที่เปิดค้างจะเผาโควตา Supabase)
- **หน้าเต็มไม่ใช่ของระบบไหน** — `systemForPathname('/notifications')` คืน `null` แล้ว
  `AppLayout` ถอยไปใช้ระบบล่าสุดที่จำไว้ ⇒ เปลือกเมนูไม่กระตุก
  ⚠️ ถ้าลบกฎนั้นทิ้ง มันจะตกไป `return 'tax'` เงียบ ๆ = หน้าแจ้งเตือนสวมเมนูภาษีสรรพสามิต
  (บั๊กตัวเดียวกับที่ `/requests` เคยเจอ)

## 4. อ่านแล้วนับยังไง

- **อ่านแล้วไม่หายจากรายการ** เปลี่ยนแค่พื้นหลัง/แถบซ้าย · หายเมื่อ entity ต้นทางถูกลบ
  (`purgeNotificationsMany`) เท่านั้น · **ไม่มีการกวาดตามอายุ**
- กดแถวในกล่อง **ไม่** ทำให้อ่านแล้ว — การ mark read เกิดตอนหน้าปลายทางโหลดเธรด
  (`UpdateThread`) แล้วเคลียร์ทั้ง entity ก้อนเดียว (มติ 15 · ตั้งใจไม่ทำ watermark
  รายข้อความแบบ Slack)
- **ปุ่มอ่านแล้วรายแถว** (หน้าเต็ม, `read_one`) ไม่ได้ขัดมติ 15 — มันทำงานกับ *แถวในกล่อง*
  ไม่ใช่ watermark ในเธรด และจำเป็นเพราะบางชนิดปลายทางไม่มีเธรดให้เปิด:
  `lead_overdue` ชี้ไปหน้าคิว `/sa/leads` · `service_visit` ชี้ไปตาราง `/service/schedule`
  (ต้องกดชิปเปิดโมดัลก่อนเธรดถึงโหลด) ⇒ ถ้าไม่มีปุ่มนี้ แถวพวกนั้นค้าง "ยังไม่อ่าน" ตลอดไป

## 5. 🐞 cron ตายมาตั้งแต่ 2026-07-15 — proxy ปิดประตูใส่เอง (แก้แล้วในรอบนี้)

`lead_overdue` ไม่เคยเขียนสักแถวนับตั้งแต่โค้ดเข้า main (2026-08-08 · #1083) ไล่จนจบได้ว่า:

| ตรวจอะไร | ผล |
|---|---|
| `CRON_SECRET` ใน env Production | **มี** (สร้าง 29 วันก่อน — เก่ากว่าฟีเจอร์) |
| `webapp/vercel.json` อยู่ใน Root Directory (`webapp`) | **ใช่** · crons ประกาศครบตั้งแต่ 2026-07-15 |
| Vercel เรียก cron จริงไหม | **เรียก ตรงเวลาเป๊ะ** |
| แอปตอบอะไร | **401 ทุกครั้ง** |
| ตรรกะ + การเขียน DB | **ปกติ** — รันมือกับข้อมูลจริงได้ 5 เด้ง / 7 แถว ถูกทุกช่อง |

log ของ production 2026-08-12:

```
01:30:34Z  /api/cron/daily-digest            401   (ตั้งไว้ 30 1 * * 1-5)
02:00:02Z  /api/cron/close-resolved-issues   401   (ตั้งไว้ 0 2 * * *)
```

**ต้นเหตุ**: `src/proxy.js` (Next.js 16 เปลี่ยนชื่อ `middleware` → `proxy`) กินทุก request
ตาม matcher แล้วตีตกด้วยกฎ `!user && isApi → 401` · Vercel Cron ยืนยันตัวด้วย header
`Authorization: Bearer $CRON_SECRET` **ไม่มี cookie session** ⇒ ถูกปัดตกก่อนถึง route
⇒ ตัว `cronOk` ใน handler ไม่เคยถูกรันเลยสักครั้ง

**แก้แล้ว**: `bypassesSessionGate()` ปล่อย `/api/cron/` ข้ามด่าน session — ไม่ได้ลดสิทธิ์
อะไร เพราะสอง route นั้นตรวจ Bearer/`master:manage` ของตัวเองครบอยู่แล้ว · มีเทสต์คุม
ทั้งขา "ต้องผ่าน" และขา "ต้องไม่กว้างเกิน" (`/api/cron` เปล่า ๆ กับ `/api/crontab` ต้องตก)

⚠️ **ยังไม่ได้ยืนยันบน production** — ต้องรอ merge แล้วดู log เช้าวันทำการถัดไปว่า
`/api/cron/daily-digest` ขึ้น 200 · ผลข้างเคียงที่จะกลับมาพร้อมกัน: **การ์ดสรุปเช้าเข้า
Google Chat 3 ห้อง** (webhook ตั้งไว้ครบ แต่ไม่เคยได้ยิงเพราะ 401) และ **ปิดเรื่องแจ้ง
ปัญหาที่เงียบเกิน 7 วันอัตโนมัติ** จะเริ่มทำงานจริงครั้งแรก

## 6. ถอดท่อ Google Chat ออกทั้งระบบ (2026-08-12 · mig 0236)

ถอดอะไรบ้าง: `lib/chat.js` · `/api/chat-webhooks` · หน้า `/settings/chat-webhooks` ·
ตาราง `chat_webhooks` · จุดยิง 33 จุดใน 20 ไฟล์ · การ์ดสรุปเช้า 4 ใบใน cron ·
`lib/tax/filingNotify.js` + `filingHandoff.js` · `lib/master/approvalNotify.js` ·
`notifyNewIssue` · `leadDigestRows`

**ไม่มีอะไรที่ยังทำงานอยู่ถูกตัด** — ทุกห้องถูกปิด (`enabled = false`) ตั้งแต่ 2026-07-30
และตาราง `chat_webhooks` มาก่อน env เสมอ ⇒ ไม่มีการ์ดใบไหนถูกส่งจริงมาสองสัปดาห์แล้ว
(ยิ่งกว่านั้น cron ก็โดน proxy ตอบ 401 อยู่ ดู §5)

⚠️ ตัวแปร `CHAT_WEBHOOK_*` บน Vercel ยังอยู่ ตอนนี้ไม่มีโค้ดอ่าน — ลบได้ที่แดชบอร์ด

## 7. เหตุการณ์ที่เงียบลงเพราะการถอน (ตั้งใจ — มติผู้ใช้ 2026-08-12)

ผู้ใช้เลือก "ลบทิ้งเลย ไม่ต้องต่อเข้ากระดิ่ง" · ถ้าวันหนึ่งอยากได้กลับ ต่อผ่าน
`notifyUsers()` ได้ทันที (ต้องตัดสินก่อนว่า "ใครคือผู้รับ" ของแต่ละชนิด)

| เหตุการณ์ | เดิมเข้าห้อง | ตอนนี้ |
|---|---|---|
| ลูกค้าใหม่รออนุมัติ | approvals | เงียบ — ผู้อนุมัติต้องเปิด `/database/customers` เอง |
| สินค้าใหม่รออนุมัติ | approvals | เงียบ — เปิด `/database/products` เอง |
| แก้ข้อมูลหลักแล้วตกกลับรออนุมัติ | approvals | **ยังมีในเธรดของแถวนั้น** (คนที่ผูกกับแถวได้กระดิ่ง) แต่ผู้อนุมัติที่ไม่ได้ผูกไม่รู้ |
| ขออัปเดตงานส่งมอบจากฝ่ายจัดซื้อ | pc | เงียบ |
| รอยต่อใบยื่นชำระภาษี SA ↔ LG | ตามขั้น | เงียบ |
| เรื่องแจ้งปัญหาระบบเข้าคิวใหม่ | admin | เงียบ — แอดมินเปิดคิว `/support` เอง |
| การ์ดสรุปเช้า: งานค้างอนุมัติ · งานโครงการใกล้ครบกำหนด · รอยต่อเอกสารค้าง | 3 ห้อง | เงียบ (ใบลีดยังอยู่ — เข้ากระดิ่งรายคนแทน) |

## 8. ชนิดที่ลงเธรดแต่ไม่เด้ง (`quiet`)

ตรวจข้อมูลจริง 2026-08-13 พบว่า **อนุมัติสินค้าหนึ่งครั้งเด้งสองใบ**:

```
product | อนุมัติ        109 แถว · 84% ไม่อ่าน · 7 คน · 69 ใบ
product | ยกเว้นเอกสาร   109 แถว · 84% ไม่อ่าน · 7 คน · 69 ใบ   ← ตัวเลขเท่ากันเป๊ะทุกช่อง
```

เพราะสินค้าบังคับแนบ "Artwork สินค้า" แต่บน prod ยังไม่มีใบไหนแนบเลย ⇒ **ต้องกดยกเว้น
ทุกครั้งที่อนุมัติ** ⇒ `approve` + `override` เขียนติดกัน fan-out ให้คนกลุ่มเดียวกัน
ในวินาทีเดียวกัน · 218 จาก 259 แถวของสินค้า (84%) มาจากคู่นี้ และเป็นกลุ่มที่ไม่ถูกอ่าน
มากที่สุดในระบบ

**แก้ด้วยธง `quiet: true`** ในทะเบียนชนิด (`lib/master/updateTypes.js`) — `notifyThreadUpdate`
ข้ามการ fan-out ให้ชนิดนั้น · วันนี้ตั้งไว้สองที่: `customer.override` · `product.override`

⚠️ **ไม่ได้ลบเหตุการณ์** — แถวยังอยู่ในเธรดครบ เปิดใบแล้วยังอ่านได้ว่าใครยกเว้นเอกสาร
ด้วยเหตุผลอะไร (ต้องตรวจย้อนได้) · ที่ตัดคือ *การเด้ง* เท่านั้น

⚠️ ใส่ `quiet` ให้ชนิดใหม่ได้เฉพาะเมื่อมันมาคู่กับเหตุการณ์อื่นที่เด้งแทนอยู่แล้ว — ไม่ใช่
ใช้เป็นวิธี "ลดเสียง" ของเหตุการณ์ที่ไม่มีใครแจ้งซ้ำให้ (ไม่งั้นกลายเป็นเงียบหายเหมือน §7)

## 9. ของที่ยังค้าง

1. `sahamit_po` · `costing_request` · `excise_order` ลงทะเบียนไว้แต่ไม่เคยมีแจ้งเตือนเลย
   (`service_visit` มี 1) — ยังไม่ได้แยกว่าเป็นเพราะไม่มีกิจกรรม หรือไม่ได้ต่อสาย
2. แถวสะสมไปเรื่อย ๆ ไม่มี TTL — 890 แถว ณ 2026-08-12 ยังไม่เป็นปัญหา แต่ไม่มีใครกวาด
